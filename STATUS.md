# Project status

Running log of where the HubDB Importer project is, what's in flight, and what's next. Update as we go.

## Current state — 2026-09-17

**Phase:** 1 — MVP (in progress; core lib layer complete, F1 + F2 + F3 + F4 + F5 + F6 + F7 + F8 shipped end-to-end, full-pipeline integration validated against sandbox via both the spike and the `/import` UI endpoints)

**Phase-1 addendum finding (F0-13):** HubDB's `POST /tables` uniqueness applies to **label**, not just `name`. Trying to create a new table with a label that matches any existing table returns 409 `TableValidationError.DUPLICATE_NAME_AND_LABEL`. Namespacing the label alongside the name is the safe pattern; provisioning UI should probably surface this if a user picks a label that collides.

**Blocked on:** nothing. F8 (execute + optional publish) shipped this evening — the full loop runs at `/import`: pick portal → upload CSV/JSON → map columns → configure FK relationships → see toposort order → dry run → execute + push-live. Underlying pieces: `lib/execution.ts::synthesizeExecution` (translates wizard state into `importRows`'s `{schema, tableIds, source}` triple, re-keying into "target space"), `POST /api/portals/[id]/execute` (calls `importRows` + optional `pushLive`), and `ExecutePanel` client component. Verified against the sandbox via `/execute` on fresh namespaced tables: 5 rows created, 0 errors, foreign-only publish worked.

**Next up (unblocked):**
- **F10 — results & logging** UI surface (per-table stats + row-error log + downloadable log). `ExecutePanel` renders the raw result today; F10 formalizes it.
- **F11 — persist mapping profiles + job history** so state survives page reload and past runs are inspectable.
- **Delete-table endpoint** — sandbox is accumulating scratch tables (`f3demo_*`, `exec_*`) from verification runs; no way to clean them up yet. Also useful for `/portals/[id]/schema`.
- **Job cursor persistence + rate-limit throttle** — the two F8 items still open (deferred; the current single-request execute path works fine at MVP scale).
- Rate-limit stress test (still-open Phase-0 question).
- Node 22 upgrade to retire the `ws` polyfill and silence the supabase-js deprecation warning.

**Scratch state left behind in the sandbox:** F3 verification left `f3demo_1789646817` (draft, 0 rows). F8 verification left `exec_1789651861_{brands,categories,products}` (some published). No delete endpoint yet; harmless.

### What exists
- Next.js 16 App Router scaffold (TypeScript, Tailwind v4, ESLint 9, pnpm)
- shadcn/ui initialized (base-nova / Base UI) — `Button` under `components/ui/`
- PRD (`hubdb-importer-prd.md`) and phase plans (`phases/phase-0…phase-3.md`)
- Phase-0 spike harness — `scripts/spike/*.ts`, runs via `pnpm spike <path>` with `.env.local` loading; all migrated onto `lib/hubdb/`
- Phase-1 foundations:
  - `lib/hubdb/` — typed HubDB wrapper: `createHubdbClient()` factory with 429/5xx retry + `Retry-After` handling; id normalization (all ids returned as strings); typed operations for tables (`list/get/create/patch/pushLive`) and rows (`listAll{Draft,Live}Rows`, `batch{Create,Update,Purge}DraftRows`); `/rows/draft/batch/*` path shape hard-enforced (F0-5), 100-row cap asserted (F0-6)
  - `lib/graph.ts` — directed dependency graph shared by provisioner/importer/publisher: `toposort()`, `toposortOrThrow()` (F6 "reject with clear message"), `breakCycles()` (F6 "offer two-phase write") with deferred-edge output for column-less create + PATCH-in strategy; converters from `HubdbTableInput` and `HubdbTable`; iterative Tarjan's SCC for cycle detection
  - `lib/schema.ts` — schema-file parser (zod) + cross-ref validator (surfaces all issues at once, not fail-first) + `resolveDefaults` (FK `foreignColumn` defaults to target's single-column `naturalKey`) + `diffSchema` returning per-table `create | match | update | conflict` (never emits drop or retype — F3)
  - `lib/hubdb/provision.ts` — topological two-phase provisioner. Takes an injectable `ProvisionOps` adapter (not the raw client) for testability; `opsFromClient(client)` builds the real one. Self-references included in the graph so `breakCycles` catches them. Phase 2 groups deferred edges by source and PATCHes the missing FK columns in per table. **Sends `portal.columns + new columns` on every PATCH** (existing ids preserved) — validated as REQUIRED by F0-11: HubDB PATCH is full-replace; sending only new columns silently destroys the schema
  - `lib/resolve.ts` — low-level FK resolver (F8): `normalizeKey` (trim + collapse ws + casefold, each toggleable), `buildKeyMap` (natural-key → row-id with duplicate detection; returns `{ok:false, duplicates, map}` on collision), `resolveForeignValue` (all-or-nothing; dedupes repeated values by default; empty → `[]` per PRD), `splitMultiValue` helper (`,` `|` `;` `\n`), `HUBDB_MAX_ROWS_PER_TABLE = 10_000` constant. onMissing policies (fail/skip/null/stub) live at the caller — resolver just reports missing
  - `lib/hubdb/import.ts` — F8 execution layer. `importRows(ops, {schema, tableIds, source})` runs in topological order (`toposortOrThrow` on the schema graph): per table, fetch existing draft rows → build keyMap on naturalKey (dupe + 10k-cap pre-flight via `ImportPreflightError`) → for each source row, resolve FK columns using previously-populated tables' key maps → split into insert/update by naturalKey lookup → batch update, then batch insert at 100/call. Newly-inserted row ids fold back into the key map so downstream tables resolve correctly. Injectable `ImportOps`; `opsFromClientForImport(client)` builds the real one. `onMissing='fail'` per-row (skip + log, other rows continue); other policies + stale-ID retry + cancel signal noted as follow-ups. Assumes `foreignColumn === target.naturalKey` (the `resolveDefaults` happy path)
  - `lib/crypto.ts` — AES-256-GCM `encrypt(plaintext) → base64` / `decrypt(base64) → plaintext`. Single output string encodes `iv || tag || ciphertext`. Reads `PORTAL_TOKEN_ENCRYPTION_KEY` (base64, 32 bytes) from env; throws with a clear message on missing / wrong length. `generateEncryptionKey()` helper for one-shot key generation. 10 vitest cases (roundtrip, unicode, IV freshness, bit-flip rejection via GCM auth tag, truncation, wrong-key rejection, missing-env, wrong-length-key)
  - `lib/db/supabase.ts` — `createSupabaseServerClient()` factory using `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`. Server-only (bypasses RLS); `auth: { persistSession: false, autoRefreshToken: false }` for request-scoped use. Untyped `SupabaseClient` for now — typed repos will layer on top in F1
  - `supabase/migrations/20260915000000_init.sql` — full DDL for the 6 Phase-1 tables (`portals`, `mappings`, `jobs`, `job_batches`, `job_errors`, `key_maps`) plus a shared `set_updated_at()` trigger. Applied against the cloud project on 2026-09-15. Paste-into-SQL-Editor workflow; needs the role selector on `postgres` (F1-scaffold finding). Filename follows the Supabase CLI's timestamp convention for later adoption
  - `.env.example` — SUPABASE_* + PORTAL_TOKEN_ENCRYPTION_KEY blocks with generation instructions; the old HUBSPOT_* vars are scoped as "spike scripts only" (app code reads tokens from the `portals` table via `getPortalToken`)
  - `lib/hubdb/introspection.ts` — `validateHubdbToken(token)` probes `/tables?limit=1` with retry disabled. Returns `{ok:true, scopes:["hubdb"]}` on 200; `{ok:false, error, status:401}` on 401 with a specific "invalid or missing hubdb scope" message; generic error with status + body preview for other 4xx/5xx. 3 vitest cases via fetch injection
  - `lib/db/portals.ts` — typed portal repo. `PortalRow` (server-only, includes ciphertext) vs `PortalSummary` (client-safe, no token). Functions: `createPortal(client, input)` (encrypts token via `lib/crypto`), `listPortals(client)`, `getPortalById(client, id)`, `getPortalToken(client, id)` (server-only decrypt), `deletePortal(client, id)`. All take `SupabaseClient` as first arg
  - `lib/source/csv.ts` — CSV parser. Byte input, BOM sniff (UTF-8 / UTF-16 LE/BE) with manual `encoding` override; papaparse for tokenization with auto delimiter detection (`,` `;` `\t` `|`) + manual override; header dedup (`col_2`, `col_3`) + empty-header filler (`column_N`) with warnings; `headerRow` override for preamble rows. Throws `CsvParseError` on empty input or header row past EOF
  - `lib/source/json.ts` — JSON parser. Accepts either `{ tableName: rows[] }` or `[{ table, rows }]`; scalars coerced to strings (booleans, numbers, bigints, null → ""), nested objects **or arrays** rejected with `{ kind: "nested-value", path: "table[i].col" }` per PRD ("cells are scalars or delimited strings"); backfills missing columns to keep row shapes uniform
  - `lib/source/validate.ts` — warning collector (`Warning` union with `kind + table + message + detail`). Fires: `row-count-cap` (> 10k via `HUBDB_MAX_ROWS_PER_TABLE`), `cell-too-long-text` (> 10k), `cell-too-long-richtext` (> 65k for columns in `richTextColumns`), `empty-required-column` (per requested col), `duplicate-natural-key` (single or composite key; uses `normalizeKey` so `Acme` and `acme` collide; empty key parts are skipped instead of colliding). natural-key + required-col checks stay quiet at F2 stage; they fire once F4 supplies the args
  - `lib/source/index.ts` — barrel + `SOURCE_PREVIEW_ROWS = 20` constant
  - `lib/hubdb/portal-schema.ts` — `fetchPortalSchema(client, now?)` for F3. Runs `listTables` then `getDraftTable` per table in parallel; overwrites `columns` with the draft view and captures per-table failures in `draftFetchError` (one 403 doesn't nuke the whole snapshot). Returns `{ tables, fetchedAt }`
  - `lib/execution.ts` — F8 synthesizer. `synthesizeExecution({sources, mappings, portalTables})` returns `{schema, tableIds, source, issues}` — translates the wizard state into the triple `importRows` expects, renaming source columns to target column names in both the schema and the row payloads. FK column translation: `foreignTable` becomes the sibling source name, `foreignColumn` is the sibling's target-column name for the picked matchKey. Returns issues (rather than throwing) for missing-target, missing/composite/unmapped natural key, and fk-target-column-missing. 6 vitest cases
  - `lib/dry-run.ts` — F7 pure. `computeDryRun({sources, mappings, portalTables, existingRowsByTarget})` returns `{order, tables, projectedApiCalls, ok}`. Per source table: existing draft-row count, source-row count, planned create/update/skipped (natural-key intersection with existing rows, `normalizeKey`-folded), unresolved-FK list with `{sourceTable, sourceColumn, foreignSource, matchKey, rowIndex, value}`, coercion warnings via `detectTypeMismatch`, API-call estimate (`ceil(existing/1000)` list-page GETs + `ceil(create/100)` POSTs + `ceil(update/100)` PATCHes). Reuses `deriveImportOrder` for the traversal order. No writes; caller fetches portal rows. 7 vitest cases
  - `lib/mapping.ts` — F4 + F5 + F6 pure logic. F4: `normalizeIdent` (identifier-fold: lowercase + strip separators, distinct from `normalizeKey` which is for cell values), `autoMap(sourceHeaders, targetColumns)` returning per-source `mapped/ignored/unmapped` with claim-once (a target can only be assigned once), `detectTypeMismatch(sample, targetType)` (NUMBER/CURRENCY require numeric, BOOLEAN accepts true/false/yes/no/1/0 case-insensitively, DATE requires `YYYY-MM-DD`, DATETIME requires `Date.parse`-able, TEXT/RICHTEXT/URL permissive), `validatePathColumn(rows, column)` for `hs_path` (lowercase + URL-safe chars + duplicate + empty). F5: `ForeignKeyConfig` shape + `initialForeignKeyConfig()` + `normalizeOptionsFor(matching)` (bridges the UI's `default/strict` to `lib/resolve`'s NormalizeOptions), and `countResolvable(mainRows, mainColumn, foreignRows, matchKey, cfg)` returning `{matched, unmatched, empty, totalValues, missingValues}` for the live-resolvability preview. F6: `MappingState` type (moved out of `mapping-editor.tsx` so lib code can consume it), `initialMappingState()`, and `deriveImportOrder(sourceNames, mappings)` → `{nodes, order, cycles, deferred, cycleBreakOrder, ok}` powered by `lib/graph.ts::toposort`+`breakCycles`. Self-refs and out-of-set FK targets are ignored. 26 vitest cases
  - `app/api/portals/route.ts` — `GET` returns `{ portals: PortalSummary[] }`; `POST` validates body with zod, calls `validateHubdbToken` (400 on failure), inserts, returns `201 { portal }`. `runtime = "nodejs"` (needs `node:crypto` and the `ws` polyfill). Unique-index violation on `(label, env)` is rewritten to a 409 with a friendly message
  - `app/api/sources/csv/route.ts` — F2 CSV endpoint. `runtime = "nodejs"`. Accepts `multipart/form-data`, one or more `file` fields (max 20 MB each); global `delimiter` / `encoding` / `headerRow` overrides via form fields. Table name derived from filename (extension stripped, non-word chars → `_`). Response per file: `{ name, filename, headers, preview (first 20), totalRows, detected, parseWarnings, validationWarnings }`. `CsvParseError` → 400 with file context
  - `app/api/sources/json/route.ts` — F2 JSON endpoint. `runtime = "nodejs"`. `{ payload: string }` body (zod-validated). Dispatches on `parseJson` outcome: `nested-value` → 400 with `path`; other error kinds → 400 with `kind`. Success → same table-summary shape as CSV endpoint plus top-level `warnings` (e.g. duplicate table names in the payload)
  - `app/portals/page.tsx` + `app/portals/portal-form.tsx` — server component lists portals directly via `listPortals`; client `PortalForm` posts to `/api/portals`, calls `router.refresh()` on success. Env badge is red for production, muted for sandbox. `runtime = "nodejs"` + `dynamic = "force-dynamic"` on the page
  - `app/import/page.tsx` + `app/import/source-uploader.tsx` — F2 page. Server component pre-fetches portals (target-portal picker) + renders empty-state prompt when none exist. Client `SourceUploader` toggles between CSV upload and JSON paste forms, posts to the two endpoints, renders a `TableCard` per parsed table with detected encoding/delimiter, parse + validation warnings, and a preview grid (first 20 rows)
  - `app/portals/[id]/schema/page.tsx` + `refresh-button.tsx` — F3 portal introspection page. Server component fetches the portal, decrypts the token, builds a HubDB client, and runs `fetchPortalSchema`. Renders per-table cards with published/draft badge, row count, and column table showing name/type/FK target ids. Client `RefreshButton` uses `useTransition` + `router.refresh()`
  - `app/portals/[id]/schema/schema-planner.tsx` — F3 diff/provision client component. Textarea + `.json` upload, per-table verdict cards (create/update/match/conflict), Provision button, expandable result panel with toposort order + new table ids + event log
  - `app/api/portals/[id]/schema/route.ts` — F4 dependency: `GET` returns `{ portal, fetchedAt, tables }` for client consumption (MappingEditor calls this when a portal is picked)
  - `app/api/portals/[id]/diff/route.ts` + `provision/route.ts` — F3 write side (diff and provision endpoints, `SchemaValidationError` → 400 with issue list, `ProvisionConflictError` → 409)
  - `app/api/portals/[id]/execute/route.ts` — F8 endpoint. Body `{sources, mappings, publish}`. Fetches portal snapshot, calls `synthesizeExecution` (400 if issues), runs `importRows` (409 on `ImportPreflightError`), optionally `pushLive`s in dep order per the publish mode (`none` / `foreign-only` / `all`). Returns `{result, events, published}`
  - `app/import/execute-panel.tsx` — F8 client component. Publish selector + Execute button, per-table result cards (created/updated/skipped/errors with a collapsible errors list), publish result list, collapsible event log
  - `app/api/portals/[id]/dry-run/route.ts` — F7 endpoint. Body: `{sources, mappings}`. Fetches existing draft rows via `listAllDraftRows` for every target a mapping references (parallel), calls `computeDryRun`, echoes back the report plus any per-table read errors
  - `app/import/dry-run-panel.tsx` — F7 client component. "Run dry run" button; per-table cards with source/existing/create/update metrics + coercion warnings + collapsible unresolved-FK list (first 100 inline, with a "…and N more" line); "Download CSV" button assembles a blob of unresolved refs client-side
  - `app/import/import-order-panel.tsx` — F6 client component. Numbered vertical list of the toposort order (or the cycle-break order when cycles exist), with each row showing its dependencies + any deferred edges. Header shows a green "no cycles" or yellow "two-phase write" badge
  - `app/import/mapping-editor.tsx` — F4 + F5 client component. F4 pieces as above. F5: `ForeignKeyPanel` inserted per FK column with (source picker, match-key picker, multi + delimiter, onMissing, matching mode) and a live "matched / unmatched / empty" resolvability preview computed via `countResolvable`. State is a `foreignKeys: Record<sourceCol, ForeignKeyConfig>` map on `MappingState`; the editor keeps it in sync on target change and per-row assignment change (drops orphaned configs, seeds defaults when a FOREIGN_ID target gets a new mapping)
  - `app/portals/page.tsx` — now also renders a "schema →" link per portal row that navigates to the introspection page
  - `app/layout.tsx` + `app/globals.css` — font bug fix: `--font-sans` in `@theme` was `var(--font-sans)` (self-reference → undefined → serif fallback), now correctly points at `--font-geist-sans`. Metadata title updated from "Create Next App" → "HubDB Importer"
  - `app/page.tsx` — home page linking to `/portals` and `/import` (both ready) and disabled placeholders for Runs / Results
  - `lib/db/supabase.ts` — Node 20 polyfill for the WebSocket global: `import { WebSocket } from "ws"` set as `globalThis.WebSocket` when missing. Required by Supabase-js 2.116+'s RealtimeClient constructor. Retire when we upgrade to Node 22
  - `ws` (+ `@types/ws`) added as dependencies for the polyfill above
  - `vitest` — 5.0.1; suites colocated with source (`lib/graph.test.ts`, `lib/schema.test.ts`, `lib/resolve.test.ts`, `lib/crypto.test.ts`, `lib/mapping.test.ts`, `lib/dry-run.test.ts`, `lib/execution.test.ts`, `lib/hubdb/introspection.test.ts`, `lib/hubdb/provision.test.ts`, `lib/hubdb/portal-schema.test.ts`, `lib/hubdb/import.test.ts`, `lib/source/csv.test.ts`, `lib/source/json.test.ts`, `lib/source/validate.test.ts`). `pnpm test` / `pnpm test:run`. **169 cases across 14 suites, all green**. tsc clean
- `papaparse` (+ `@types/papaparse`) — CSV parser used by `lib/source/csv.ts`. Small, delimiter-detection built in
- Git: `main` tracking `origin/main` at https://github.com/junelapera/hubspot-importer

### In flight — Phase 1 foundations
| # | Task | Status |
|---|---|---|
| 1 | `lib/hubdb/client.ts` — factory, retry/backoff, `HubdbError` with rate-limit surface | done |
| 2 | `lib/hubdb/types.ts` — normalized shapes (string ids), `isPublished()` treating epoch as unpublished (F0-9) | done |
| 3 | `lib/hubdb/tables.ts` — list/get/create/patch/pushLive | done |
| 4 | `lib/hubdb/rows.ts` — draft/live pagination + `batch/{create,update,purge}` under `/rows/draft/` (F0-5), 100-row cap asserted | done |
| 5 | Migrate spike scripts onto `lib/hubdb/` (regression check) | done — 00-ping re-passes against sandbox |
| 6 | `lib/graph.ts` — toposort + cycle detection + two-phase decomposition | done — 25 hand-verified assertions |
| 7 | Test runner — Vitest installed; suites colocated with source | done — `pnpm test:run`, 40 cases green |
| 8 | `lib/schema.ts` — parse + validate schema file, diff vs portal | done — 21 cases (parse failure modes + every diff verdict incl. FK conflicts) |
| 9 | `lib/hubdb/provision.ts` — compose wrapper + graph into topological provisioner (name-based FK happy path from F0-2) | done — self-loop + 2-cycle break paths tested via fake ops |
| 10 | Validate PATCH-as-full-replace assumption in `provision.ts` against the sandbox | done — F0-10/11/12; `patchTable` path bug fixed, `getDraftTable` added, end-to-end integration spike passes |
| 11 | `lib/resolve.ts` — key map builder + FK resolver (F8) | done — 24 cases (normalize + split + build map w/ duplicates + resolve all-or-nothing + dedupe + multi-value) |
| 12 | `lib/hubdb/import.ts` — F8 execution (pass-1 upsert foreign, pass-2 main w/ resolved FKs, batching, retry) | done — 11 cases; ImportPreflightError for dupes + 10k cap; RowError for per-row failures |
| 13 | End-to-end sandbox spike for the importer (`provision → importRows → push-live`) | done — `scripts/spike/13-import-end-to-end.ts`; 3 tables provisioned, 30 rows imported, FK resolved (product.brand[0].id === brand-1.id), pushed live, live row ids stable, re-import fully idempotent, cleanup deleted the namespaced tables |
| 14 | Supabase project + 6 phase-1 tables applied | done — verified via `12-supabase-ping.ts`, all 6 tables reachable |
| 15 | `lib/crypto.ts` — AES-256-GCM encryption for portal tokens | done — 10 cases |
| 16 | `lib/db/supabase.ts` — server-side client factory | done — includes Node 20 `ws` polyfill |
| 17 | F1 portal-connection API + typed portal repo + picker UI | done — validated end-to-end against real Supabase + real HubSpot (POST 201, GET 200, dupe → 409, bad token → 400) |
| 18 | F2 — source ingestion (CSV/JSON upload + parse + preview) | done — validated end-to-end via curl (2 CSVs w/ auto delimiter, JSON happy + nested-object 400 w/ path, 10001-char cell → cell-too-long-text) |
| 19 | F3 — portal introspection UI (schemas, published/draft state, row counts) | done — read side + diff/provision write side both validated against the real sandbox (match / update / create / conflict all correct, then a new `f3demo_*` table provisioned to id `412491169` and re-diff flipped to match) |
| 23 | F4 — column mapping UI (auto-match, type mismatch, ignore, natural key, hs_name/hs_path) | done — lib fully unit-tested; `MappingEditor` wired under each parsed source table on `/import`; new `GET /api/portals/[id]/schema` endpoint verified against sandbox |
| 24 | F5 — foreign relationship config (source table + match key + multi + delimiter + onMissing + matching) | done — `ForeignKeyPanel` per FOREIGN_ID mapping; `countResolvable` powers a live matched/unmatched/empty preview; 5 vitest cases on the helper |
| 25 | F6 — dependency ordering (import order UI) | done — `deriveImportOrder` derives a graph from the mapping FK configs and reuses `lib/graph.ts::toposort`/`breakCycles`; `ImportOrderPanel` renders the numbered plan + deferred edges when cycles are broken; 5 vitest cases |
| 26 | F7 — dry run (create/update counts, unresolved FKs, coercion warnings, API call estimate, CSV download) | done — `lib/dry-run.ts::computeDryRun` (pure) + `POST /api/portals/[id]/dry-run` fetches existing draft rows + `DryRunPanel` on /import. 7 vitest cases. Sandbox verified: brands+products source, 1 unresolved FK caught, 4 projected API calls (matches ceil arithmetic) |
| 27 | F8 — UI wiring (execute + optional publish) | done — `lib/execution.ts::synthesizeExecution` (6 vitest cases) turns mapping state into an `importRows` triple; `POST /api/portals/[id]/execute` runs it + optional pushLive; `ExecutePanel` on /import. Sandbox verified: provisioned 3 fresh namespaced tables, executed 5 rows (2+1+2) with FK resolution, publish=foreign-only pushed the two foreign tables live and left products draft. Composite naturalKeys + multi-value FK translation are not supported in the execute path yet |
| 28 | Sandbox cleanup — delete-table endpoint | pending — verification runs left `f3demo_1789646817` and `exec_1789651861_*` (brands/categories/products) sitting in the sandbox portal. Would also want a "Drop table" affordance on `/portals/[id]/schema` |
| 20 | Node 20 → 22 upgrade (retire the `ws` polyfill) | pending |
| 21 | Rate-limit stress test (still-open Phase-0 question) | pending |
| 22 | Long-job runner strategy (still-open Phase-0 question) | pending |

### Archived — Phase 0 tasks
| # | Task | Status |
|---|---|---|
| 1 | Scaffold spike harness (env, tsx, client) | done |
| 2 | Confirm API path (`v3` vs dated `2026-03`) | done — both functionally equivalent at `/tables`; defaulting to `v3` |
| 3 | Provision `brands` + `categories` foreign tables | done — `brands` id `412407343`, `categories` id `412407344` (both draft) |
| 4 | Reproduce `Foreign table id must be defined` error | done — actual message: *"Foreign table id **or foreign table name** must be defined"* |
| 5 | Provision `products` main table w/ two FK cols | done — id `412407345` (draft); name-based FK accepted, server-resolved to ids |
| 6 | Batch-insert 200 rows into foreign tables, capture IDs | done — 200 rows in each of `brands` + `categories`, two 100-row batches per table, slug→id maps built |
| 7 | Batch-insert 200 linked rows into `products` | done — 200 rows, FK cells stored verbatim; UI displays blank until foreign tables published |
| 8 | Push-live all three tables in dependency order | done — all three live, 200 rows each, row ids stable draft→live |
| 9 | Document findings in `phases/phase-0-spike.md` | done — 9 findings + 6 locked-in decisions + open questions rolled into Phase 1 |

HubL render (last item in phase-0 checklist) deferred — do manually in HubSpot page editor after API chain is proven.

### Open questions still un-answered (PRD §13)
1. Single main table per mapping, or multiple?
2. Rows in HubDB but absent from source — leave / flag / delete?
3. Prod-write gating (second confirmation or approval)?
4. Verification read-back (auto-generated HubL join snippet)?
5. Schema file authoring: hand-committed vs UI-built?
6. Schema file + import mapping — one file or two?

### Decisions locked in during scaffold
- **Package manager:** pnpm (pinned in `package.json`)
- **Framework version:** Next.js **16** (PRD said 15; `create-next-app` shipped 16 — App Router surface compatible)
- **UI kit:** shadcn/ui on Base UI (`base-nova` preset)
- **Repo layout:** no `src/` — `app/` `components/` `lib/` `workers/` at root, matches PRD §9
- **Spike code shape:** standalone `scripts/spike/` CLI (tsx), not API routes
- **Test runner:** Vitest 5, colocated `*.test.ts` next to source (not a `tests/` dir). Peer warning: vitest wants `@types/node ^22 || >=24` but Next.js pins `^20` — warning only, suite runs

---

## Log

### 2026-09-17
- **F8 UI wiring shipped.** Wrote `lib/execution.ts::synthesizeExecution` — the crucial translator between our wizard state and `importRows`'s expected shape. Key insight: `lib/hubdb/import.ts::planRow` uses each schema column's `name` as BOTH the source-row lookup key AND the HubDB values key. When the mapping renames a column (e.g. source `Product Name` → target `productName`), the synthesizer keys everything in "target space": schema columns use target names, source rows are transformed to `{targetCol: source[sourceCol]}`, natural key + FK columns are translated too (FK's `foreignColumn` becomes the sibling's target column name for the picked matchKey). Returns `{schema, tableIds, source, issues}` — issues rather than throws so the endpoint can 400 cleanly
- 6 vitest cases: column renaming happy path, FK translation to sibling's target column, `fk-target-column-missing` when sibling doesn't map the matchKey, missing-target / composite-natural-key / natural-key-not-mapped guardrails
- Wrote `POST /api/portals/[id]/execute` — decrypts token → fetches portal → `synthesizeExecution` (400 if issues) → `importRows` (409 on `ImportPreflightError`, 502 on generic HubSpot errors, both echo the event log so the UI can show what happened before failure) → optional `pushLive` in `result.order` filtered by publish mode. `publish: "foreign-only"` publishes everything except the last table in dep order (rough heuristic that fits products→brands+categories; richer selection lives in F9)
- Wrote `ExecutePanel` client component. Publish selector (`none | foreign-only | all`), Execute button, per-table result cards showing `N created / N updated / N skipped / N errors` with a collapsible row-errors list (first 50), publish results list with per-table success/failure, collapsible event log. Guard `disabled` prop plus a `disabledReason` hint — the API also refuses execution independently if synthesis issues remain
- Wired into `SourceUploader`: shows below the `DryRunPanel` under the same "has target + natural key" condition. `showDryRun` is reused as the gate
- **Sandbox verification via curl** (Dev sandbox portal):
  - Provisioned 3 fresh namespaced tables via `POST /api/portals/[id]/provision`: `exec_1789651861_brands` (id `412493376`) + `_categories` (`412492315`) + `_products` (`412492316`)
  - Called `POST /api/portals/[id]/execute` with 2 brands + 1 category + 2 products (each product FKing brand + category by slug), `publish: "foreign-only"`
  - Response: `ok: true`, dep order `[brands, categories, products]`, 5 rows created total (2/1/2), 0 errors, 12 events emitted, publish result: brands + categories published live, products left draft ✓
- **Scope-limited choices in this pass:** (a) composite natural keys still unsupported (guard-railed via issue); (b) multi-value FKs aren't split in the transformer yet — the executor path assumes single-value cells. Both flagged as followups. Job persistence + rate-limit throttle also still open per PRD
- **Scratch state left in sandbox:** the `exec_1789651861_*` tables from this verification join the earlier `f3demo_1789646817` — no delete endpoint yet. Added to the "next up" list as a small follow-up item
- 169 cases across 14 files, all green. tsc + lint clean

- **F7 dry run shipped** (earlier this session). Wrote `lib/dry-run.ts::computeDryRun` — pure function that classifies source rows as create vs update by natural-key intersection with existing draft rows, tallies unresolved FKs by looking up each cell value (with multi-value split when configured) against the sibling source's match-key set, emits coercion warnings via `detectTypeMismatch`, and estimates API calls (list-page GET + create batches + update batches). Reuses `deriveImportOrder` from F6 for the traversal order so composite v5/6/7 stays consistent
- 7 vitest cases covering: create/update classification, natural-key normalization (BETA↔beta), unresolved FKs (single + multi-value split), coercion warning, dep-order propagation, API-call estimate (250 rows → 1 GET + 3 POST batches). Suite now 163 across 13 files
- Wrote `POST /api/portals/[id]/dry-run` — reads portal, decrypts token, `fetchPortalSchema` for target-table lookup, then parallel `listAllDraftRows` for every table referenced by a mapping (uses `Promise.all`; per-target errors captured so one 403 doesn't nuke the whole report), calls `computeDryRun`, returns the report + optional `readErrors`
- Wrote `DryRunPanel` client component. "Run dry run" button; per-table cards with 4-metric grid (source rows / existing draft rows / will-create / will-update), coercion warnings inline, collapsible unresolved-FK list showing first 100 rows + a "…and N more" line for larger sets. CSV download button assembles a client-side blob (`escape()` quotes any value containing `,`, `"`, or `\n`) and drops the file as `unresolved-fks.csv`
- Wired into `SourceUploader`: appears above the per-table cards whenever any mapping has both a target and a natural key picked. Passes `portalId + allSources + mappings` down; the panel POSTs everything on Run
- **End-to-end verification via curl** against the sandbox (Dev sandbox portal, 200 existing brands + 200 existing products from Phase-0): brands source (2 rows) + products source (3 rows, one row's `brand` = "unknown"), FK config `brand` → brands.slug default matching. Response: order `["brands", "products"]`, brands planned `create: 2, update: 0` (source slugs `acme`/`beta` don't match Phase-0's `brand-1..brand-200`), products planned `create: 3` + `unresolved: 1` (the `unknown` value), API estimate `4` = 1 GET brands + 1 batch-create brands + 1 GET products + 1 batch-create products ✓. `ok: false` because of the unresolved FK
- 163 cases across 13 files, all green. tsc + lint clean
- **Next step:** F8 UI wiring (turn the mapping + FK config + dry-run pass into a click-to-run execute + push-live), or F11 mapping-profile persistence so the state survives page reload

- **F6 dependency-ordering UI shipped** (earlier this session). User picked F6 next. Tracked 4 subtasks (`TaskCreate` #18–#21). Small refactor first: `MappingState` + `initialMappingState` moved out of `app/import/mapping-editor.tsx` into `lib/mapping.ts` so a shared derivation can consume it. `mapping-editor.tsx` re-exports both for local callers, no changes needed at any call site
- Added `deriveImportOrder(sourceNames, mappings)` to `lib/mapping.ts`. Builds `GraphNode[]` from each mapping's `foreignKeys` (a source depends on any sibling listed as a `sourceTable` on any FK config), then delegates to `lib/graph.ts::toposort` / `breakCycles`. Returns `{nodes, order, cycles, deferred, cycleBreakOrder, ok}`. Self-references and out-of-set FK targets are ignored — clean semantics
- 5 vitest cases: happy-path products→brands+categories ordering, identity order when no FKs, out-of-set target ignored, 2-cycle detected + deferred edges emitted + `cycleBreakOrder` populated, self-ref ignored
- Wrote `ImportOrderPanel` — numbered vertical list of the resolved order. Each row shows its dependencies (as inline chips) and any deferred edges callouts. Header badge is green "no cycles" or yellow "two-phase write". When cycles exist, an extra callout section enumerates each cycle as `a → b → c → a` and notes the deferred-edge count
- Wired into `SourceUploader`: appears above the per-table cards only when at least one mapping has FK configs (so the panel doesn't render for the pure-single-table upload case). Reuses the existing `mappings` state and `allSources` list — no new state, no extra fetches
- 156 cases across 12 files, all green. tsc clean, lint clean
- **Next step:** F7 (dry run — the config we already have has everything it needs; dry run reports what would happen without writing) or F8 UI wiring (hook `/import` up to `importRows` + push-live for a click-to-run pipeline; lib layer already proven by the sandbox spike)

- **End-to-end importer spike passed** (earlier this session). Wrote `scripts/spike/13-import-end-to-end.ts` — the full `provision → importRows → push-live` pipeline against the sandbox, using unique namespaced table names (`spike_it_<epoch>_{brands,categories,products}`) to avoid Phase-0 collision. 7 steps: diff → provision → import (5+5+20 rows) → verify FK cell → push-live in dep order → verify live row-id stability → re-import for idempotency check → cleanup
- **First run failed with 409 → F0-13 finding:** creating a table with label `"Brands"` fails when any existing table in the portal already has that label — HubDB uniqueness applies to LABEL, not just name. Error body: `TableValidationError.DUPLICATE_NAME_AND_LABEL`, `duplicateTables: ["412407343"]` (the Phase-0 brands table). Fix: namespace both `name` and `label`. Rolled the finding into the current-state block; provision UI should probably surface this if a user picks a label that collides with an existing table
- **Second run: all steps green.** Provision created 3 tables in dep order (foreign first), `products.brand` came back with `foreignTableId=412492145 foreignColumnId="1"` — the "slug" column of the new brands table (F0-4 in action: column ids are per-table sequential, so both FK targets landed on id `1` of their respective tables). Import: 5+5+20 rows, zero row errors. `sku-001.brand[0].id === brand-1.id` (FK resolution correct). push-live promoted all three (~600 ms each); live product row-id `222107301655` matched the draft id — publish is promotion not duplication (reconfirms F0-8). Live product row's `brand` cell now includes the resolved `name: "brand-1"` field, since the display column is `foreignColumnId=1` = `slug` and live introspection joins it. Re-import: 0 created, 30 updated across the three tables → fully idempotent. Cleanup deleted all three namespaced tables cleanly
- Added the new spike script to CLAUDE.md's command list

- **F5 foreign-relationship config shipped** (earlier this session). User picked F5 to continue. Tracked 5 subtasks (`TaskCreate` #11–#15). Design decision: F5 lives inside `MappingEditor` as a per-column sub-panel rather than a separate route — `foreignKeys: Record<sourceColumn, ForeignKeyConfig>` on `MappingState` keeps everything cohesive, and the editor auto-syncs configs on target change and per-row assignment change (drops orphans, seeds defaults). Also needed access to sibling parsed sources, so `SourceUploader` now passes `allSources` down alongside the single `source` prop
- Extended `lib/mapping.ts` with `ForeignKeyConfig` (sourceTable / matchKey / multi / delimiter / onMissing / matching), `initialForeignKeyConfig()`, `normalizeOptionsFor(matching)` (bridges the UI's `default | strict` to `lib/resolve`'s NormalizeOptions — `strict` disables trim + collapseWhitespace + casefold), and `countResolvable(mainRows, mainColumn, foreignRows, matchKey, cfg)` returning `{matched, unmatched, empty, totalValues, missingValues}` for the live preview. Reuses `normalizeKey` + `splitMultiValue` from `lib/resolve` — no duplication
- 5 vitest cases on `countResolvable`: single-value case-insensitive default matching (all-hits), single-value with mixed empty/missing/hit, strict rejects casefold matches, multi-value comma-split with per-cell hit-and-miss (verified `totalValues` counts every value across every row), multi-value pipe delimiter
- Bug shaken out during test-writing: my first multi-value expectation was wrong (`totalValues: 4`) — actually `2 + 2 + 1 = 5`. The failing test caught that my mental model was undercounting the split cells. Fixed the expectation, not the code
- Wrote `ForeignKeyPanel` in `mapping-editor.tsx`. One card per FOREIGN_ID mapping. Two-row layout: foreign source + match key on top, then 4 columns (multi / delimiter / onMissing / matching). Delimiter select is disabled until multi is on. Live resolvability preview shows matched/unmatched/empty + `totalValues` + `missingValues`, and calls out unresolved count in yellow when non-zero. If source or matchKey unset, we render a placeholder line prompting the user
- Threaded `allSources` from `SourceUploader` into each `MappingEditor` — one small refactor of the render loop to build the list once and hand each editor its slice
- **Suite now 151 cases across 12 files, all green.** tsc clean, lint clean (the pre-existing `SchemaColumn` warning in `lib/hubdb/import.ts` still there)
- Interactive UI can't be scripted here (no chromium-cli / playwright). Verification via unit tests + tsc + prior curl checks. When the user drives it in a real browser: pick a portal → upload brands.csv + products.csv → for the mapped `brand` column in products (target FOREIGN_ID), the FK panel appears and offers `brands` as the sibling source and `slug` as the match key
- **Next step:** end-to-end importer spike (`provision → importRows → push-live`) to prove the pipeline with real FK resolution, or F6/F7 (dependency-order display + dry run — dry run is where the F5 config becomes user-visible instead of just captured state)

- **F4 column mapping UI shipped** (earlier this session). After F3 write side landed the user asked to continue and picked F4 via AskUserQuestion. Tracked five subtasks (`TaskCreate` #6–#10). Big design decision up front: `MappingEditor` lives inside the existing `SourceUploader` on `/import` (matches PRD §9 `/import/[id]` wizard shape) rather than a separate `/mapping` route — no persistence layer yet, so a single-page state machine keeps everything in sync
- Wrote `GET /api/portals/[id]/schema` — a client-consumable JSON version of the F3 introspection page. Same `fetchPortalSchema` under the hood; used by the mapping UI to seed the target-table dropdown when the user picks a portal
- Wrote `lib/mapping.ts` — pure functions, no DOM. `normalizeIdent` (lowercase + strip whitespace/underscore/hyphen/dot; distinct from `normalizeKey` because identifier folding shouldn't collapse to spaces — `product_name` and `productName` must both fold to `productname`), `autoMap(sourceHeaders, targetColumns)` returning `mapped/ignored/unmapped` per source with **claim-once** semantics (if two source headers fold to the same target, only the first wins; later ones stay `unmapped` — prevents silent double-assignment), `detectTypeMismatch(sample, targetType)` (only fires on high-confidence mismatches: NUMBER/CURRENCY require numeric, BOOLEAN accepts true/false/yes/no/1/0 case-insensitively, DATE requires `YYYY-MM-DD` **and** `Date.parse`-able, DATETIME just `Date.parse`-able, TEXT/RICHTEXT/URL permissive), `validatePathColumn(rows, column)` for `hs_path` (lowercase + URL-safe chars + duplicate + empty)
- 16 vitest cases for `lib/mapping.ts` — happy paths + claim-once + all TypeMismatch kinds + all PathIssue kinds + empty/whitespace tolerance. Suite now 146 across 12 files
- Modified `POST /api/sources/csv` + `/json` to include full `rows` in the response (not just the first-20 `preview`) so the client can re-run `validateSource` when the user picks a natural key or required columns. Trade-off: payloads grow ~1KB/row (10 MB at the 10k cap) — fine for MVP; F11 persistence would replace this with a source-id + server-side re-validation
- Wrote `app/import/mapping-editor.tsx` — the client component. Target-table dropdown → auto-map → per-row source column table: source col name + sample values (first 3 non-empty, truncated) + three-way assignment select (`— unmapped —` / `— ignored —` / target col name, with claimed targets disabled) + type badge showing target column type + mismatch badge + natural-key checkbox (only enabled when mapped). Below: `hs_name` + `hs_path` fields appear when target `useForPages`; a summary panel showing mapped/ignored/unmapped counts + chosen natural key + page fields; a live warnings panel that re-runs `validateSource` on every state change (so `duplicate-natural-key` and `empty-required-column` fire the moment a key is picked) plus `validatePathColumn` bucketed by issue kind when `hs_path` is set
- Wired the editor into `SourceUploader`: parent now owns `portalTables` state, fetches `/api/portals/[id]/schema` in a `useEffect` on portal-id change (with `startTransition` around the setStates to satisfy React 19's new `set-state-in-effect` lint rule — earlier attempt without transitions failed lint), stores `mappings` keyed by source-table name, and passes each `MappingEditor` its own slice
- **End-to-end verification** — API side clean:
  - `GET /api/portals/{portal}/schema` → 200, 5 tables with columns (all 4 sandbox tables + the leftover `f3demo_*` from F3 verification)
  - `POST /api/sources/csv` → 200 with the new `rows` field present, `totalRows === rows.length === 4` on a 4-row brands.csv (including 2 duplicated slugs, so the F2 duplicate-natural-key warning is ready to fire once the user picks `slug` as the key)
  - UI screenshot of `/import` renders unchanged when no upload has happened — MappingEditor mounts on parse success, which needs a user click I can't script without chromium-cli / playwright installed. Compensating with the 16 unit tests on `lib/mapping.ts`, tsc-verified prop wiring, and the parent's `useEffect` fetch verified via curl
- Lint churn: `react-hooks/set-state-in-effect` errored on the fetch-then-setState pattern until I wrapped the state updates in `startTransition`; also had to prune an unused `useState` import in mapping-editor.tsx after refactoring. Both clean now
- **Suite now 146 cases across 12 files, all green.** tsc clean. lint clean (pre-existing `SchemaColumn` unused warning still there)
- **Next step:** F5 (foreign relationship config — the actual "solve the manual-linking problem" feature) slots naturally into `MappingEditor` beneath each `FOREIGN_ID` target column. Or the end-to-end importer spike (`provision → importRows → push-live`) to prove the pipeline before more UI layers land

- **F3 diff + provision shipped** (earlier in the day). After the read-side landed the user asked to continue; picked "F3 diff-plan UI" via AskUserQuestion. Set up 5 tracked tasks (`TaskCreate`) since this had discrete steps and I wanted the progress visible
- Wrote `POST /api/portals/[id]/diff` — validates body has `{ schema }`, `parseSchema` (400 on `SchemaValidationError` with the issue list echoed back), `fetchPortalSchema` (502 wrapped), `diffSchema`, returns `{ portal, fetchedAt, schema, plan }`
- Wrote `POST /api/portals/[id]/provision` — same pipeline then `provision(opsFromClient(client), plan, { onEvent })`. Captures each `ProvisionEvent` in a local array and echoes them back for the UI to render. `ProvisionConflictError` → 409 with `conflictTables` + the offending plan; other errors → 502 with the plan + partial events so the UI can show what happened before failure
- Wrote `app/portals/[id]/schema/schema-planner.tsx` — client component with a state machine (`idle / diffing / planned / provisioning / provisioned / error`). Accepts JSON via textarea or `.json` file upload; parses locally first (fast error surface); POSTs to diff, renders per-table cards with action badges (create=green, update=blue, match=muted, conflict=red), and shows a Provision button when `plan.ok && (create+update) > 0`. Provision result includes toposort order, deferred edges count, new table ids, and the event log in a collapsible `<details>`; after success, `router.refresh()` re-fetches the introspection snapshot so the newly-provisioned table appears at the top of the page
- Wired the planner into `app/portals/[id]/schema/page.tsx` between the table list and the footer. Also updated the existing schema page — no changes needed to `page.tsx` beyond one import + one component invocation
- **End-to-end verification against the real sandbox (Dev sandbox, Hub 51706903):**
  - **match**: schema of `brands + categories + products` (with explicit `foreignColumn: "name"` matching the Phase-0 setup) → `plan.ok: true`, all three `match`. First attempt with `resolveDefaults` filling `foreignColumn: "slug"` from naturalKey surfaced a real conflict — products' actual FK display column is `name` (col id 1), not slug. Good validation of the resolver + conflict logic against portal reality
  - **update**: schema adding a `logo_url URL` column to brands → `plan.ok: true`, `action: "update", addColumns: ["logo_url"]`
  - **create**: schema declaring a new `f3demo_<epoch>` table → `plan.ok: true`, `action: "create"`
  - **conflict**: schema declaring `brands.slug` as NUMBER (portal has TEXT) → `plan.ok: false`, one `column-type-mismatch` conflict emitted
  - **provision**: POSTed the create schema → HTTP 200, `order: ["f3demo_1789646817"]`, `tableIds: {"f3demo_1789646817": "412491169"}`, 2 events. Re-diff of the same schema → `action: "match"`. Refreshed screenshot showed the new table at the top of the introspection list with the DRAFT ONLY badge
- **Suite still 130 cases across 11 files.** Provisioning + diff routes are integration-tested end-to-end but not unit-tested — the underlying ops are already covered
- **Scratch state left in the sandbox**: `f3demo_1789646817` (draft, 0 rows) is still there. No delete endpoint in v1; harmless. Could be a followup to add "Drop table" to the introspection UI, though PRD is silent on this
- **Next step:** F4 (column mapping UI — unlocks F2's deferred natural-key / required-col warnings and starts wiring the import wizard shape) or the end-to-end importer spike (`provision → importRows → push-live`) to prove the full lib pipeline before F4 stacks more UI on top

### 2026-09-17-morning
- **F3 read side shipped.** After F2 landed the user asked to continue; picked F3 (portal introspection UI) via AskUserQuestion. Scope: read/render only — the diff-plan + provisioning trigger remain
- Wrote `lib/hubdb/portal-schema.ts::fetchPortalSchema(client)` — `listTables` for enumeration + `getDraftTable(id)` per table in parallel (`Promise.all`) to pick up pending draft columns. Per-table failures are caught and stashed in `draftFetchError` so a single 403 doesn't nuke the snapshot; the whole thing returns `{ tables, fetchedAt }`. Injected `now` factory keeps the vitest deterministic
- 3 vitest cases for portal-schema (draft columns merged over live, per-table error isolation with the failing table falling back to live columns, empty-portal). Suite now 130 cases across 11 files
- Wrote `app/portals/[id]/schema/page.tsx` — server component. Fetches portal → decrypts token via `getPortalToken` → `createHubdbClient` → `fetchPortalSchema`. Renders each table as a card with published/draft badge (uses `isPublished` from lib), row count, and a column table showing name / label / type / FK target ids. `dynamic = "force-dynamic"` so refresh always hits fresh data. Empty-portal + fetch-error branches handled
- Wrote `app/portals/[id]/schema/refresh-button.tsx` — client. `useTransition` + `router.refresh()` for the F3 "manual refresh" requirement. No explicit per-session cache — the tradeoff is a re-fetch per view for correctness; if portals grow past a few dozen tables we can add `unstable_cache` here
- Added a `schema →` link on each row of `/portals` so the introspection view is reachable from the portals list
- **End-to-end verification against the real sandbox portal (Dev sandbox, Hub 51706903):**
  - GET `/portals/{id}/schema` → **200** in ~3s (list + 4 draft fetches in parallel), rendered all 4 tables
  - **Products** (id 412407345, 200 rows, PUBLISHED): sku + title as TEXT; brand + category as FOREIGN_ID with target-table ids `412407343` / `412407344` and display-column id `1` on each — visually confirms F0-4 (column ids are per-table sequential, both FKs point at column id 1 of their respective target tables)
  - **Categories** (412407344, 200 rows, PUBLISHED), **Brands** (412407343, 200 rows, PUBLISHED), and **trst** (377110628, 0 rows, DRAFT ONLY — the pre-existing table from before the spike, showing the badge works)
- **Font bug fix (Times New Roman → Geist).** User flagged that everything was rendering in Times. `app/globals.css` had `--font-sans: var(--font-sans)` in the `@theme` block — a self-reference that resolved to undefined, so `font-sans` in Tailwind fell back to serif. Changed to `--font-sans: var(--font-geist-sans)` (the CSS variable that `next/font/google`'s `Geist({variable: "--font-geist-sans"})` actually exports in `layout.tsx`). Also updated the layout `metadata.title` from "Create Next App" → "HubDB Importer"
- **Suite now 130 cases across 11 files, all green.** tsc clean. lint clean (pre-existing `SchemaColumn` unused warning still there)
- **Next step:** F3 completion — diff-plan UI (upload a schema definition → `diffSchema` + render per-table verdicts + provision button). Or F4 (column mapping UI, which unlocks the natural-key / required-col warnings F2 defers)

---

- **F2 shipped end-to-end** (earlier in the day). User asked to proceed with what phase 1 has planned next; picked F2 (source ingestion) via AskUserQuestion. Aligned on scope up front (three questions): in-memory only (no Supabase persistence yet — that's F11's problem), papaparse for CSV, new `/import` route
- Installed `papaparse` + `@types/papaparse`. `pnpm install` had to re-link first — snap `code/263` → `code/264` (VS Code snap bump, same failure mode as 2026-09-15's `263` bump). One-time relink, harmless
- Wrote `lib/source/csv.ts` — byte-in, `{ headers, rows, detected, warnings }` out. BOM sniff for UTF-8 / UTF-16 LE/BE with manual encoding override; papaparse's auto-delimiter with manual override (`,` `;` `\t` `|`); header dedup with `_N` suffixes and empty-header filler `column_N` (both emit warnings); `headerRow` override for preamble rows; short rows padded with empty strings so every row has every header key. Throws `CsvParseError` on empty input or header row past EOF
- Wrote `lib/source/json.ts` — accepts either `{ tableName: rows[] }` or `[{ table, rows }]`; scalars stringified; nested objects **or arrays** rejected as `nested-value` with `path: "table[i].col"` (PRD F2: "cells are scalars or delimited strings" — arrays too, since multi-value is authored as delimited strings). Backfills missing columns per row
- Wrote `lib/source/validate.ts` — `Warning` union (`row-count-cap` / `cell-too-long-text` / `cell-too-long-richtext` / `duplicate-natural-key` / `empty-required-column`). Reuses `HUBDB_MAX_ROWS_PER_TABLE` + `normalizeKey` from `lib/resolve`. natural-key + required-col checks are generic — F2 stage doesn't know these so they stay quiet; F4 will pass them in
- Wrote three vitest suites (39 cases): CSV happy paths + BOM/encoding + header dedup + preamble rows + RFC-4180 quoted fields + error paths; JSON both shapes + scalar coercion + nested-value rejection with path pointers + malformed-JSON handling; validate cap + length + required-col + natural-key (single + composite) + empty-key-skip
- Convention snag: first draft imported `@/lib/resolve` (works in Next but no vitest config → import error). Rest of `lib/` uses relative imports, so switched. Also worth noting for future: no `vitest.config.ts` exists; adding a `resolve.alias` for `@/` would let both worlds share the same import style
- Wrote `POST /api/sources/csv` (multipart, one or more `file` fields, 20 MB cap each, global delimiter/encoding/headerRow overrides) and `POST /api/sources/json` (`{ payload: string }` body). Both return the same per-table shape: `{ name, filename, headers, preview (20), totalRows, detected, parseWarnings, validationWarnings }`. JSON errors surface `path` for nested-value, `kind` for others
- Wrote `app/import/page.tsx` (server component, pre-fetches portals + empty-state) and `app/import/source-uploader.tsx` (client). Tabs for CSV/JSON, form controls for delimiter/encoding/header row, per-table `TableCard` with detected encoding/delimiter, warning list, preview grid (first 20 rows, cell values truncated at 120 chars). Home page updated to link to `/import` alongside `/portals`
- **End-to-end verification against a running `pnpm dev`:**
  - GET `/import` → 200, page renders
  - POST `/api/sources/csv` with `brands.csv` + `products.csv` → 200; brands auto-detected as comma, products as semicolon, both preview + totalRows correct
  - POST `/api/sources/csv` with `-F 'delimiter=;'` override on the semicolon file → 200 with detected: `";"` echoed
  - POST `/api/sources/json` with `{brands:[…], categories:[…]}` → 200, two tables
  - POST `/api/sources/json` with a nested-object cell → **400** `kind: "nested-value"`, `path: "products[0].meta"` ✓
  - POST `/api/sources/csv` with no `file` field → 400 with clear message
  - POST `/api/sources/csv` with a 10001-char cell → 200 with `validationWarnings: [{ kind: "cell-too-long-text", … }]` ✓
- Bug shaken out during verification: `validateSource({ table: file.name })` was passing the raw filename (`toolong.csv`), while the response used the derived name (`toolong`). Fixed to hoist `tableName` and reuse it. One-line change
- **Suite now 127 cases across 10 files, all green.** tsc clean. lint clean (pre-existing `SchemaColumn` unused warning still there, not from this change)
- **Next step:** F3 (portal introspection UI) or F4 (column mapping UI — would also unlock the natural-key/required-col validation warnings that F2 defers). The end-to-end importer spike is still pending too

### 2026-09-15
- Kicked off Phase 1. Started with the item STATUS+CLAUDE.md called out first: `lib/hubdb/` typed wrapper hardening
- Wrote `lib/hubdb/client.ts` — `createHubdbClient({ token, ... })` factory (per-portal, no global env read). Retries 429 + 5xx with `Retry-After` honored, else exponential backoff with jitter capped at 30s (default 5 attempts). `HubdbError` exposes status, parsed rate-limit headers, and attempt count
- Wrote `lib/hubdb/types.ts` — normalized shapes. Table `id`, row `id`, `foreignTableId`, `foreignColumnId` are all `string` (F0-4 wrapper contract). Added `isPublished()` that treats the `1970-01-01T00:00:00Z` epoch as "never published" (F0-9)
- Wrote `lib/hubdb/tables.ts` (`listTables/getTable/createTable/patchTable/pushLive`) and `lib/hubdb/rows.ts` (`listAllDraftRows/listAllLiveRows`, `batch{Create,Update,Purge}DraftRows`). Batch mutation paths are hard-coded to `/rows/draft/batch/{create,update,purge}` (F0-5). 100-row cap asserted before the call (F0-6)
- Migrated all 7 spike scripts onto the new wrapper. `scripts/spike/client.ts` shrank to ~30-line shim that builds one `HubdbClient` from `HUBSPOT_TOKEN` and re-exports `log`/`runSpike`/`HubdbError`. Spikes now act as regression checks of the wrapper — any break in id normalization, retry, or batch paths surfaces when re-running them
- Smoke-tested `pnpm spike scripts/spike/00-ping.ts` against the sandbox. Green: both `v3` and `dated` bases return 4 tables (~800ms each), all ids as strings
- Wrote `lib/graph.ts` — the topological sort shared by provisioner + importer + publisher. Kahn's for order, iterative Tarjan's for SCC / cycle detection. Three public entry points: `toposort()` (partial order + cycles report), `toposortOrThrow()` (F6 "reject with clear message"), `breakCycles()` (F6 "offer two-phase write"). Converters: `nodesFromTableInputs` (name-based FK, the F0-2 happy path) and `nodesFromHubdbTables` (id-based). Convention: `GraphNode.dependencies` = prereqs; `GraphEdge {from, to}` mirrors that ("from depends on to")
- Wrote `scripts/graph-check.ts` — 25 hand-verified assertions across linear DAG, spike scenario, self-loop, 2/3-node cycles with dangling dependents, `toposortOrThrow` cycle payload, `breakCycles` deferred-edge output, disjoint tie-break stability, both converters, and duplicate-name rejection. Two bugs shaken out during the check: (a) edge direction inverted (adj stored `n→dep` but Kahn's needed `prereq→dependent`); (b) `toposort` emitted cycle nodes when they had no non-cycle prereqs — semantic decision to filter them out and force callers to `breakCycles` if they want an order that covers cycle nodes
- **Caveat — not a test runner.** `scripts/graph-check.ts` is a one-shot assertion runner, not Vitest. No CI, no watch mode, no `describe/it`. Fine for shaking bugs out at write-time; needs to be replaced by a proper test runner before the module has real callers. Phase 1 task #7 tracks this
- **Wrote-time artifact worth watching.** First Write of `lib/graph.ts` landed with `U+0000` (null bytes) in two template literals where the source had spaces (`` `${e.from} ${e.to}` `` → `` `${e.from}\0${e.to}` ``). Detected via `cat -A` when an Edit failed to match. Recovered by rewriting the file. Unclear whether the corruption was in the Write tool, the terminal bridge, or my own emission. Did not recur in `lib/schema.ts` or `lib/hubdb/provision.ts` — one-off so far
- Installed **Vitest 5** and ported `scripts/graph-check.ts` to `lib/graph.test.ts` (12 `describe/it` blocks). Deleted the smoke script. `pnpm install` had to run first — pnpm store path drifted from snap `code/261` → `code/263` (VS Code snap version bump) and node_modules was linked to the old store. One-time relink, harmless. Peer warning: vitest wants `@types/node ^22 || >=24` but Next 16 pins `^20`; warning only, suite runs
- Wrote `lib/schema.ts` — zod schema for structural parse, then a manual cross-ref pass that surfaces *all* issues at once (duplicate table/column names, dangling FK targets, FK missing `foreignTable`, `foreignColumn` not on target, `naturalKey` referencing an undefined column). `resolveDefaults` fills FK `foreignColumn` from the target's single-column `naturalKey` when omitted; composite `naturalKey` leaves the caller to be explicit. `diffSchema(schema, portal)` returns per-table `create | match | update | conflict` with three conflict shapes (`column-type-mismatch`, `fk-target-mismatch`, `fk-column-mismatch`); never emits drop/retype (F3). Extra portal columns not in the schema are ignored — schema is additive, not authoritative-over-portal. 21 vitest cases covering parse failure modes + every diff verdict
- Wrote `lib/hubdb/provision.ts` — takes an injectable `ProvisionOps` adapter (not the raw client) so tests can plug in a fake with internal state instead of MSW. `opsFromClient(client)` builds the real adapter. Dep graph nodes are built only from `create` + `update` actions; a table depends on another *only if the target is also being created* (updates/matches already exist in the portal). Self-references are included so `breakCycles` catches them. Phase 2 groups deferred edges by source and PATCHes the missing FK columns per table
- **PATCH semantics conservatism.** The provisioner sends `portal.columns + new columns` on every PATCH (existing ids preserved). Safe assumption if HubDB PATCH is either full-replace *or* merge — but not yet validated against the sandbox. Added a Phase-1 task to spike this before broader wiring
- **Bug shaken out in provisioner:** initial version filtered `col.foreignTable !== name` when building deps, so self-loops were invisible to the graph and got sent to `createTable` with the self-referencing FK column intact. Caught by the self-loop test on first run; fix was one line
- **Next step:** validate PATCH semantics against the sandbox, then either `lib/resolve.ts` (F8) or start the F1 portal-connection API + Supabase scaffolding

- Ran the PATCH-semantics validation. **Three new findings** (F0-10 through F0-12, documented in the Phase-1 addendum of `phases/phase-0-spike.md`):
  - **F0-10** — `PATCH /tables/{id}` returns HTTP **401** with a misleading "service-to-service not engaged" body (no hint that the path is wrong). The correct endpoint is `PATCH /tables/{id}/draft` — schema mutations live under the draft namespace, extending F0-5's rows pattern to tables. `PUT` and dated-base PATCH cleanly 405; only v3 PATCH on the root gives the confusing 401
  - **F0-11** — `PATCH /tables/{id}/draft` is **full-replace** on the `columns` array. Sending only new columns drops the existing ones. Sending a subset drops the omitted ones. Push-live promotes whatever the draft says. **This confirms the provisioner's `portal.columns + new columns` approach was correct — sending only new columns would silently destroy schemas.** The PRD §F3 "never drop or retype" rule is enforced by our code, not the API
  - **F0-12** — `GET /tables/{id}` returns the LIVE view, not the draft. After PATCH /draft, GET stays on pre-PATCH state until push-live. Callers that need to see pending schema changes (diff, verify) must use `GET /tables/{id}/draft`
- **Live bug fixed in `lib/hubdb/tables.ts::patchTable`** — was hitting `/tables/{ref}` (guaranteed 401 in production). Now `/tables/{ref}/draft`. Added `getDraftTable(client, ref)` and exported it from the barrel. All 40 vitest cases still green (fake ops don't care about paths)
- **End-to-end integration test**: `scripts/spike/11-provision-update.ts` creates a throwaway table with 2 columns, parses a 3-column schema, diffs against the draft, runs `provision(opsFromClient(client), plan)`, verifies the draft ends up with the expected columns. Passed on first run after the path fix
- **Spike scripts 06–11 added** as the trace of the investigation. Kept in-tree for future reference; they're throwaway but tell the debugging story
- **Next step:** `lib/resolve.ts` (F8, key-map builder + FK resolver) or start F1 portal-connection API + Supabase scaffolding — no more open assumptions in the lib layer

- Wrote `lib/resolve.ts` — the low-level FK resolver. Kept scope tight: only normalization + key-map building + value resolution + a multi-value splitter. Everything execution-level (onMissing policies, stale-ID re-resolve on write error, row-cap enforcement at pass-1) lives at the caller. `resolveForeignValue` is all-or-nothing: any single missing value fails the whole cell and returns `{ok:false, missing, cell}` (partial cell + missing list) so the caller can apply its policy per-row. Empty input → `{ok:true, cell:[]}` per PRD's "empty FK cell → empty array (not null)"
- Design choice: `Delimiter` typed as `"," | "|" | ";" | "\n"` per PRD F5. Union rather than free-string keeps mapping-file validation cheap downstream
- 24 vitest cases (normalize defaults + opt-outs + coercion + null handling; split per delimiter; buildKeyMap happy/duplicate/empty-value-skip/coercion; resolve single/empty/whitespace/dedupe/multi-value-hit/multi-value-miss/every-distinct-missing + composition with `splitMultiValue`). Suite now 64 across 4 files
- **Next step:** `lib/hubdb/import.ts` composes wrapper + schema + graph + resolve into the F8 execution: pass-1 upsert foreign tables (batch) + build key maps (with dupe detection + 10k cap check), pass-2 main table with resolved FKs, retry on 429/5xx, cancel at batch boundary. Or pivot to F1 portal-connection API + Supabase scaffolding if we want to bring the app layer up first

- Wrote `lib/hubdb/import.ts` — the F8 execution layer. Topologically iterates schema tables via `toposortOrThrow(nodesFromTableInputs(...))`, and for each: fetches existing draft rows, builds a normalized keyMap on `naturalKey` (aborts with `ImportPreflightError` on dupes or if existing + source would exceed the 10k cap), splits source rows into insert vs update by naturalKey lookup, resolves FK columns via previously-populated tables' key maps, and batch-writes at 100/call (updates then inserts). Newly-inserted row ids fold back into the local key map so downstream tables that FK-reference this one resolve against the fresh ids
- **Scope-limited choices**: (a) `onMissing='fail'` per-row — the row is skipped and logged as a `RowError`, other rows continue; other policies (skip/null/stub) are follow-ups; (b) assumes `foreignColumn === target.naturalKey` (which `resolveDefaults` guarantees for single-column naturalKeys) — composite naturalKeys are unsupported in v1; (c) no stale-ID re-resolve on write error, no cancel signal (retries handled by wrapper); (d) doesn't call `push-live` — that's F9
- **Injectable `ImportOps`** (list + batchCreate + batchUpdate) mirrors the provisioner's testability pattern. Tests use a `FakeOps` with an internal Map<ref, HubdbRow[]> store and a call log. 11 vitest cases (all-new insert with cross-table FK resolution, PATCH-update by naturalKey, casefold matching, dupe abort, 10k-cap abort, per-row unresolved-fk skip, missing-naturalKey source, 100-row chunking on a 250-row batch, empty-source no-writes, unmapped-table error path, lifecycle event ordering). Suite now 75 across 5 files
- **Wrote-time artifact false-positive**: my usual `cat -A | grep '\^@'` check reported 1 match on this file, but `python3 -c "open(p).read().count(b'\\x00')"` returned 0 — the "hit" was `M-^@` inside the UTF-8 sequence for `…`. Retiring the cat-based check in favor of the python one
- **Ticked off in `phases/phase-1-mvp.md`**: F8's 5 code items + F8's "retry on 429/5xx" (wrapper-level), 5 of 7 items in "Foreign key resolution correctness" (Section 8), and 3 of 4 constraint-enforcement items (Section 10). Remaining F8 items are execution-environment concerns (Supabase persistence, throttle, cancel button, worker separation)
- **Next step:** end-to-end sandbox spike (`provision → importRows → push-live`) analogous to `11-provision-update.ts`, then either F1 portal-connection API + Supabase scaffolding or start on results/logging (F10) surface

- **Pivoted to phase-1-mvp order.** User asked "we still don't have UI?" and then "let's proceed to what has planned on phase 01" — so I switched from further lib work to the phase-1 checklist starting at "Project scaffolding" → F1. Picked "Cloud project" for the Supabase-mode question via AskUserQuestion
- Wrote `supabase/migrations/20260915000000_init.sql` — full DDL for `portals`, `mappings`, `jobs`, `job_batches`, `job_errors`, `key_maps`. Enables `pgcrypto` for `gen_random_uuid()`. Shared `set_updated_at()` trigger on `portals` + `mappings`. Job-side tables carry the cursor state (`job_batches`) and per-row failure log (`job_errors`) that F8/F10 will need. `key_maps.entries` is a JSONB `{normKey: rowId}` snapshot per (job, table) for resume without re-fetching. Filename follows the Supabase CLI's timestamp convention so we can `supabase link` this later without renaming
- Wrote `lib/crypto.ts` — AES-256-GCM. `encrypt(plaintext) → base64` returning a single string that packs `iv || tag || ciphertext`. Key read from `PORTAL_TOKEN_ENCRYPTION_KEY` (32 bytes, base64) with a clear error if missing or wrong length. `generateEncryptionKey()` helper. 10 tests (roundtrip, unicode, IV freshness, bit-flip → auth tag catches, truncation, wrong-key rejection, missing env, wrong length)
- Installed `@supabase/supabase-js` (2.116.0). Wrote `lib/db/supabase.ts` — server-only factory using the service-role key, `persistSession: false`, `autoRefreshToken: false`. Untyped `SupabaseClient` for now; typed repos will layer on top when F1 lands
- Rewrote `.env.example` — SUPABASE_* + PORTAL_TOKEN_ENCRYPTION_KEY blocks with generation instructions; the old HUBSPOT_* vars are re-scoped as "spike scripts only" since app code will read tokens from the `portals` table once F1 is wired
- **Blocked on user action for full validation:** creating the Supabase cloud project + applying the SQL migration + pasting env vars. Code side is unblocked — F1 API routes can be written and typechecked against this scaffolding; they'll fail with a clear message at runtime if Supabase isn't configured
- 85 cases across 6 suites still green. tsc clean
- **Next step:** F1 API routes (`POST /api/portals`, `GET /api/portals`) + `lib/db/portals.ts` typed repo + portal picker page

- **Supabase applied, F1 shipped end-to-end.** User created the cloud project + applied the migration via the SQL Editor. After a small speed bump (`25006 read-only transaction` — root cause was the SQL Editor's role selector being on a non-`postgres` role), all 6 tables came up green
- Wrote `scripts/spike/12-supabase-ping.ts` — counts rows in each of the 6 phase-1 tables via the server client. First run failed with `Node.js detected but native WebSocket not found` from supabase-js's Realtime constructor. Fix: installed `ws` (+`@types/ws`) and set `globalThis.WebSocket = WsWebSocket` at the top of `lib/db/supabase.ts` when the global is missing. Ping then reported all 6 tables reachable, 0 rows each. Deprecation warning for Node 20 surfaces on every Supabase call — not fatal; upgrade to Node 22 tracked as task #20
- Wrote `lib/hubdb/introspection.ts` — `validateHubdbToken(token)` hits `/tables?limit=1` with retry disabled. 200 → `{ok:true, scopes:["hubdb"]}`. 401 → `{ok:false, status:401, error:"…invalid or missing hubdb scope"}`. Other errors → generic status + body preview. Can't introspect private-app scopes via the HubSpot API, so the `["hubdb"]` scope is inferred from success. Fetch-injection tested (3 cases)
- Wrote `lib/db/portals.ts` — typed repo. Split `PortalRow` (server-only, includes `token_ciphertext`) from `PortalSummary` (client-safe, no token). Functions: `createPortal` (encrypts token via `lib/crypto`), `listPortals`, `getPortalById`, `getPortalToken` (server-only decrypt), `deletePortal`. All take a `SupabaseClient` as first arg
- Wrote `app/api/portals/route.ts` — `runtime = "nodejs"`. GET returns `{ portals }`. POST validates body with zod, calls `validateHubdbToken` (400 with the HubSpot status echoed on failure), inserts. Unique-index violation on `(label, env)` is caught and rewritten to a 409 with a friendly message
- Wrote `app/portals/page.tsx` (server component, `dynamic = "force-dynamic"`) + `app/portals/portal-form.tsx` (client). Env badge is red for production per F1. Form posts to `/api/portals`, then `router.refresh()` — no client-side state duplication. Replaced the create-next-app template on `app/page.tsx` with a real home page linking to `/portals` (ready) + disabled placeholders for Mappings / Runs / Results
- **End-to-end verification against real Supabase + real HubSpot:**
  - GET `/api/portals` → 200, empty list
  - POST `/api/portals` with the real `HUBSPOT_TOKEN` → **201**, portal created with `scopes: ["hubdb"]`, row landed in Supabase
  - GET `/api/portals` → 200, one portal (token not echoed)
  - POST with fake token → **400**, `"Token is invalid or missing the required hubdb scope"`
  - POST duplicate `(label, env)` → **409**, friendly message from unique-index catch
  - GET `/portals` (HTML) → 200, portal rendered with sandbox badge + Hub 51706903
- **Next step:** F2 (source ingestion — CSV / JSON upload + parse + preview + validation warnings), then either F3 (portal introspection UI) or the end-to-end importer spike

### 2026-09-14
- `.env.local` provisioned with `HUBSPOT_TOKEN` — spike unblocked
- Extended `00-ping.ts` to hit both `/cms/v3/hubdb/tables` and `/cms/hubdb/2026-03/tables` in parallel with timing
- Both paths returned identical results (1 existing table `trst`, ~730–760ms). Task #2 resolved — defaulting to `v3`
- Wrote `01-provision-foreign.ts` — idempotent create of `brands` + `categories` (both with `name` + `slug` TEXT columns), plus a deliberate bad-FK POST to capture the error surface
- Ran it: `brands` (`412407343`) and `categories` (`412407344`) created as draft. Bad FK returned HTTP 400 with the message *"Foreign table id or foreign table name must be defined"*
- **Finding — revisit PRD §6/§8:** HubSpot accepts `foreignTableName` as an alternative to `foreignTableId` on `FOREIGN_ID` columns. Schema files could reference foreign tables by name directly, skipping the schema-local-id → HubSpot-id translation step at provision time. Still need to confirm the same holds for `foreignColumnName` vs `foreignColumnId`
- Wrote and ran `02-provision-main.ts` — created `products` (id `412407345`, draft) with two `FOREIGN_ID` columns. The name-based path **succeeded on the first attempt** — no fallback needed. HubSpot resolved `foreignTableName: "brands"` + `foreignColumnName: "name"` server-side and returned `foreignTableId: 412407343`, `foreignColumnId: 1`

**Architectural impact — PRD §6 and §8 need revision:**
> "The provisioner translates to `foreignTableId` / `foreignColumnId` integers at write time so schema files are portable across portals."

That translation step is now **optional**. Schema files can be authored with `foreignTable: "brands"` + `foreignColumn: "name"` (names) and passed through to the API unchanged; HubSpot resolves in-portal at write time. Cross-portal portability comes for free. The provisioner still needs the id-translation code path as a fallback (e.g. for topology inspection, cycle-breaking PATCHes, or if we ever hit a scenario name-resolution can't handle), but it's no longer the primary path.

**Additional API observations to bake into the wrapper:**
- HubDB column ids are **per-table sequential integers** (both `brands.name` and `categories.name` are id `1`) — not globally unique. Any code comparing column ids across tables is a bug
- Response type inconsistency: `FOREIGN_ID` column echoes `foreignTableId`/`foreignColumnId` as **numbers**, while `/tables` list returns table `id` as a **string**. `lib/hubdb/` wrapper should normalize to strings on ingest

- Wrote `03-insert-foreign.ts` — generates deterministic rows (`brand-001`…`brand-200`, `category-001`…`category-200`), reads existing draft rows for idempotency, batches at 100 rows/call, builds `slug → row-id` maps
- First run 404'd on `POST /tables/{name}/rows/batch/create` with an HTML body (edge/gateway 404). **Correct endpoint is `/rows/draft/batch/create`** — batching is scoped under the draft namespace, not the top-level rows path. Fixed and re-ran successfully
- 200 rows inserted into each of `brands` + `categories` via two 100-row batches. Throughput ~800–1020ms per 100-row batch (~100–125 rows/sec, single-threaded). No dropped rows. Row IDs are 12-digit global HubSpot ids (`"221835620680"`), reinforcing the wrapper's id-normalization TODO
- **API surface additions for `lib/hubdb/`:**
  - Only `/rows/draft/batch/{create,update,purge}` is valid — hardcode the `draft` segment
  - Read draft rows from `/rows/draft` (with `limit` + `after` cursor for pagination — PRD §10 says 1000 default)
- Wrote and ran `04-insert-main.ts` — read `brands` + `categories` draft rows in parallel, built slug→id maps, generated 200 products with 1:1 FK assignment (`product-001` → `brand-001` + `category-001`), inserted via two 100-row batches to `/rows/draft/batch/create`. Timings ~1024ms + ~797ms
- **Finding — FK cells round-trip verbatim.** Sent `{id, type: "foreignid"}` and got back the same. No server-side normalization, no `foreignTableId` bleed onto the cell, no join expansion. Write and read shapes are symmetric — the resolver stays simple. The target table is pinned by the *column definition*, not the cell
- Row IDs across all three tables share one global integer namespace (`221835620680` brand → `221835620868` product, adjacent). Not per-table. Wrapper should never assume table-scoping on row ids
- Wrote `99-inspect.ts` to spot-check state — confirmed 200 draft / 0 live rows in each of `brands`, `categories`, `products`. `publishedAt: "1970-01-01T00:00:00Z"` is HubSpot's null-sentinel for "never published"
- **Finding — UI display of FK columns requires the foreign table to be published.** In the HubSpot HubDB editor, `products.brand` cells show blank even though the id is stored, because the display value is resolved from the foreign table's `name` column (`foreignColumnId: 1`) and only reads the *live* version. Publishing `brands` + `categories` first should make products' FK cells render — this is exactly what task #8 tests
- Wrote and ran `05-publish.ts` — sequential `POST /tables/{name}/draft/push-live` for `brands` → `categories` → `products`. All three returned `published: true` with fresh `publishedAt`. Timings: `brands` 959ms, `categories` 1736ms, `products` 923ms (~3.6s wall-clock). Re-ran `99-inspect.ts` — live counts now 200 in each table, row ids stable across draft/live (publish promotes rather than duplicates)
- **Finding — topological order is a UI/HubL requirement, not an API requirement.** Publishing `products` before `brands` would still succeed at the API level, but products' FK display columns would be blank in the HubSpot UI and HubL joins would return nothing until foreign tables catch up. The dependency order stands, but the rationale is "user-visible correctness" not "API validation"
- Categories publish was ~2× the others (1736ms vs ~940ms). Could be variance or FK-constraint re-check on the second foreign table publish. Worth watching in longer runs
- Rewrote `phases/phase-0-spike.md` — checked off all completed items with the artifact ids inline (`brands` `412407343`, etc.), documented 9 findings (F0-1 through F0-9), locked in 6 decisions for Phase 1, and rolled the two un-answered items (rate-limit ceiling, long-job runner strategy) forward as Phase-1 open questions
- **Phase 0 closed.** Remaining Phase-0 checkbox (HubL page render) is a manual editor test that does not gate Phase 1 code work
- **Next step:** kick off Phase 1 — start `lib/hubdb/` (typed wrapper hardening: id normalization to strings, `/rows/draft/batch/*` path enforcement, 429 backoff with `Retry-After`), then `lib/graph.ts` (topological sort shared by provisioner + importer). See `phases/phase-1.md` for the full task list

### 2026-09-12
- Wrote PRD `hubdb-importer-prd.md`
- Broke PRD §12 into per-phase task checklists under `phases/`
- Wrote `CLAUDE.md` guidance for future Claude Code sessions
- Scaffolded Next.js 16 app via `create-next-app`, added shadcn/ui, pushed to GitHub
- Started Phase 0: added `tsx` + `zod` deps, wrote `scripts/spike/client.ts` (typed HubSpot fetch wrapper with `HubdbError` surfacing rate-limit headers) and `00-ping.ts` (list tables). Added `pnpm spike` script
