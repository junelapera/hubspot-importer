# Project status

Running log of where the HubDB Importer project is, what's in flight, and what's next. Update as we go.

## Current state — 2026-09-26 (night)

**Wizard-state persistence shipped — sessionStorage-backed, per-portal.** Closes the state-loss round-trip pain point the smoke test surfaced: navigating to `/portals/[id]/schema` to provision tables and back to `/import` used to drop everything (parsed sources gone, mapping cards not rendered, needed a full re-upload + re-config). Now the mapping config survives across nav within the tab; only raw parsed rows need re-fetching (one click).

1. **`SourceUploader` sessionStorage integration** (`app/import/source-uploader.tsx`) — persists `mode` + `mappings` + `selectedProfileId` + `gsheetRows` to `sessionStorage` under `hubdb-importer:wizard:{portalId}` on every state change. Hydrates on mount client-side. `SessionState` type + `sessionKey(portalId)` helper co-located at the top of the file. Resume flow (`?resume=<jobId>` in URL) takes precedence — the profile-linked mapping load beats last session's state so users who deep-link into a resume don't get their previous work stomped
2. **Restored-mappings banner** — a compact primary-colored card that appears when `state.kind === "idle"` (no parsed sources yet) but `mappings` has content (rehydrated from the last session). Text: "Restored N mapping(s) from your last session on this portal · Re-fetch or re-upload your source files below — the mapping config is already filled in and will attach automatically." **Clear session** button on the right — empties the wizard state and removes the sessionStorage key
3. **Phase-2.md**: new "Wizard state persistence" block with the checkbox ticked and design rationale in the description

**Files touched:** `app/import/source-uploader.tsx` (~+60 lines: `SessionState` type + `sessionKey` helper + `hydratedRef` + hydration effect + persist effect + `clearSession` handler + banner JSX + `useRef` import), `phases/phase-2.md` (new block). 247 vitest cases / 18 suites still green, tsc + lint clean

**Design decisions worth remembering (captured in-code as comments too):**
- **`useRef` for the hydration gate, not `useState`.** Would have tripped ESLint's `react-hooks/set-state-in-effect` rule on the mount-signal setter. Hydration is one-shot; we don't need a re-render to signal completion because the banner condition (`Object.keys(mappings).length > 0`) is a natural derived signal — if mappings has content, hydration already ran
- **Persist effect skips empty payloads and `removeItem`s the key instead of writing defaults.** Protects against the effect-ordering edge case where React runs both effects in sequence within a single commit: hydration effect (declared first) reads sessionStorage and calls `setMappings(saved)`, but the persist effect (declared second) fires in the same commit with the CLOSURE still holding the render-time defaults (`{}`). If persist wrote unconditionally, it would briefly overwrite sessionStorage with `{}` before the state update commits and re-fires persist with hydrated values. Skipping empty writes turns that failure mode into a no-op. Also means "Clear session" (which resets state to defaults) naturally removes the key — one action does both
- **Raw parsed rows NOT persisted.** sessionStorage has a ~5 MB per-origin cap (browser-dependent, some go lower). A 10k-row CSV with wide columns can easily blow that out and cause a silent `setItem` throw. Persisting only lightweight config (mappings, mode, profile id, gsheet URLs — all short strings) keeps well under the cap regardless of dataset size. Re-parsing on return is cheap (single click, sub-second for typical files) and the config was the expensive-to-recreate thing anyway
- **`sessionStorage` over `localStorage`.** Auto-clears when the tab closes — a natural boundary that avoids "week-old parse lingers in weird ways" bugs. If someone wants durability across sessions they should save a mapping profile (that already exists)
- **Per-portal key prefix.** `hubdb-importer:wizard:{portalId}` — switching portals in the picker doesn't cross-contaminate mapping state that references portal-specific target table names. If a user works on portal A, then portal B, then back to A, each gets its own hydrated slice
- **Targeted `eslint-disable react-hooks/set-state-in-effect`** on the hydration effect's setter block, with a comment explaining why (legit external-store sync, `useSyncExternalStore` overkill here). Doesn't disable the rule anywhere else

**Still open on `phases/phase-2.md`:**
- OAuth flow for private Google Sheets (deferred)
- Refresh-from-sheet action for saved mappings
- Full cycle handling — two-phase write for cyclic FK graphs, self-reference end-to-end
- F11 tail — re-run saved mapping against a different portal
- Detect interrupted jobs on runner restart (Phase 3 with Inngest)
- (Provision-writes-back-tableId — Phase-1 F3 polish carried over)

**Next up (unblocked):**
- **Inngest step-function rewrite** — the last "MVP → production scale" unlock; unlocks tab-close-safe durability
- **Refresh-from-sheet** — small QoL win now that gsheets is proven; needs `mappings.state_json` to carry the URL
- **Persist last-parsed-source *metadata* (filenames / URLs, not rows)** — natural extension of the sessionStorage work; would let the banner auto-suggest what to re-fetch. ~15 min, if the current UX turns out to still leave users hunting for what they had loaded before

---

## Prior state — 2026-09-26 (late evening)

**Google Sheets sandbox smoke test passed — three real bugs surfaced and fixed, plus non-dev tutorials added on `/import` + schema-planner.** End-to-end validation of the gsheets shipment against the real HubSpot sandbox turned into a productive session: the smoke test itself worked, but the *content* of the test data (numeric price column + FK columns) exposed three actual bugs, all now shipped fixes for. Then rolled out inline tutorials so non-dev users can self-serve the wizard without hunting through `/docs`.

**Bugs found + fixed:**

1. **Type coercion at the write boundary** (`lib/hubdb/import.ts`) — every source cell is a string (CSV / XLSX / gsheets all normalize to `Record<string, string>`), but HubSpot's batch write API is strict: NUMBER / CURRENCY columns reject `"29.99"` and demand JSON number `29.99`; BOOLEAN wants a real JSON boolean; DATE / DATETIME want epoch milliseconds (integer). New `coerceCellForColumn(raw, columnType)` helper handles all four: strips currency formatting (`$`, `€`, `£`, `¥`, commas, whitespace) before `Number()`; accepts `true/false/1/0/yes/no` for booleans; `Date.parse()` for dates. Empty strings → `SKIP_CELL` sentinel (dropped from the row entirely — sending `null` or `""` on a non-string column also 400s). Unparseable values become `type-mismatch` row errors instead of blowing up the whole batch, so one bad row doesn't kill the run. New `RowErrorKind: "type-mismatch"` added to the union. Symptom before fix: `POST /rows/draft/batch/create → 400 "the value {type=string, value=29.99} for column price was of type STRING, but we're expecting NUMERIC"`
2. **Observability gap on HubDB errors** (`app/api/portals/[id]/execute/route.ts` + `app/import/execute-panel.tsx`) — `HubdbError.message` is just `HubDB POST /path → status` (URL + status only); the actual "which cell got rejected" response body lived on `err.responseBody` and was completely invisible to the client. Fix: `HubdbError` import + type-guard in the execute catch; when the failing error is a `HubdbError`, echo `{status, path, method, body: responseBody}` under a `hubspot` key in the 502 JSON. Client `ExecutePanel` now renders an expandable `<details>` block ("HubSpot response — POST … → 400") with the pretty-printed body. Also added `console.error("[execute] HubDB error …", body)` server-side so the dev console shows it too. **Before this fix, debugging any 4xx from HubSpot required either reading the exception in a Node debugger or adding console.logs — completely opaque from the UI.**
3. **Base UI Select controlled/uncontrolled bug on FK panel** (`app/import/mapping-editor.tsx::ForeignKeyPanel`) — the "Foreign source table" and "Match key column" Selects passed `value={cfg.sourceTable ?? undefined}` (no `items` prop), so on first render Base UI treated them as **uncontrolled** (value=undefined → internal state manages it). When the user picked a value, `onValueChange` fired and the trigger label updated via Base UI's internal state, but `cfg.sourceTable` in React state never got committed — the panel looked configured but wasn't. Silent breakage: FKs failed to resolve, cells landed empty in HubDB. `resolveForeignValue` returned `ok: true` with an empty array (no missing keys — the source table lookup itself returned nothing) so no error surfaced anywhere. Fix: `value={cfg.sourceTable ?? ""}` (always a string, always controlled) + `items` prop for stable trigger display. Same pattern CLAUDE.md already warned about in the shadcn/Base UI section — these two Selects had been missed in the earlier rollout. Every other Select in the codebase already had `items` set. **Insidious because the UI looked right and the run completed successfully with no error surfaced.** How it manifested for the user: "the brand column shows as unmapped in HubDB but categories are fine" (both configured identically in the UI). Diagnostic tell: `Resolvability preview` counter didn't render on the broken panel — that computes from `cfg.sourceTable`, which was still `null`

**Tutorials shipped (the user-facing improvement):**

4. **`components/ui/tutorial-panel.tsx` (new)** — reusable disclosure via native `<details>` + `<summary>` (zero JS, cheap, hot-reload safe). Three exports: `TutorialPanel` (the disclosure wrapper), `TutorialSteps` (numbered `<ol>`), `TutorialTip` (styled callout with "Tip:" prefix). Collapsed by default with a "❓ New to this? Show me how ▾" affordance so experienced users aren't scrolled past every time. Brand-butter background via `color-mix(in oklch, var(--color-brand-butter), var(--card) 50%)` so it reads as *help*, not error/warning. Rotating chevron via `group-open:rotate-180`
5. **Tutorials on all 4 source tabs** (`app/import/source-uploader.tsx`) — one per tab, tuned to what each format actually asks of the user:
   - **CSV**: how to export from Excel/Sheets/Numbers (`File → Save As` / `File → Download → CSV`), the "row 1 = headers" convention, filename → table name, and a delimiter tip for European locales where Excel emits `;`-delimited files
   - **XLSX**: the "sheet tab = HubDB table" mental model, how to rename tabs to intended table names, `File → Download → Microsoft Excel` from Google Sheets, sheet-allowlist syntax, single- vs multi-sheet naming (`filename` vs `filename__sheetname`)
   - **JSON**: titled "developer format — most users can skip" and points users at CSV/XLSX/Google Sheets instead in the opening paragraph. Both accepted shapes (`{table: [rows]}` and `[{table, rows}]`) shown as code blocks side-by-side. Notes the "cells must be plain scalars" constraint
   - **Google Sheets**: full 6-step publish-to-web recipe with the specific dropdown positions ("left dropdown", "right dropdown"), a sample URL shape, and a tip about Google's ~90s cache delay on published sheets
6. **Tutorial on schema-planner** (`app/portals/[id]/schema/schema-planner.tsx`) — the biggest win for non-devs. Explains provisioning at all ("creates the HubDB tables your import will land in"), the two-step safety pattern (Generate plan is preview / no writes, Provision is the actual write and only enables when no conflicts), what each verdict means (`create` / `match` / `update` / `conflict`), and — crucially — **points non-devs at the `Suggest a schema` panel on `/import` as the no-coding alternative** for cases when they don't have a hand-authored `schema.json`. Also calls out that everything lands draft-only until Execute publishes

**Files touched:** `lib/hubdb/import.ts` (coerceCellForColumn + SKIP_CELL + CoerceError + type-mismatch RowErrorKind), `app/api/portals/[id]/execute/route.ts` (HubdbError import + type-guard + response-body echo + console.error), `app/import/execute-panel.tsx` (hubspot field on Stage + expandable render), `app/import/mapping-editor.tsx` (ForeignKeyPanel Select fix), `components/ui/tutorial-panel.tsx` (new), `app/import/source-uploader.tsx` (4 tutorial slots), `app/portals/[id]/schema/schema-planner.tsx` (1 tutorial slot + Link import). 247 vitest cases / 18 suites still green, tsc + lint clean

**Design decisions worth remembering:**
- **Type coercion errors don't fail the run.** A row with an unparseable NUMBER cell becomes a `type-mismatch` `RowError` in `result.errors` (visible on the results card), the *other rows in the batch still write*. Alternative would have been to fail-fast on the first bad cell — chose per-row skip so a single dirty row in a 10k-row import doesn't kill the whole thing. `type-mismatch` is a new `RowErrorKind` variant; downstream error CSV export picks it up automatically since it renders every kind uniformly
- **Empty strings → `SKIP_CELL`, not `null`.** Sending `null` or `""` on a NUMBER column also 400s (`null`: sometimes) and on other columns just leaves the cell in an ambiguous state. Dropping the key entirely from the `values` object is the cleanest "no data for this cell" signal to HubSpot
- **Currency formatting strip is opinionated.** `coerceCellForColumn` for `CURRENCY` strips `$€£¥`, commas, and whitespace before `Number()`. Rationale: users routinely export from accounting software as `"$1,299.00"` and expect it to just work. Downside: `"1,299"` in a European locale (which means 1.299) gets parsed as 1299. Livable — locale-aware currency parsing would need a per-portal setting we haven't scoped
- **Observability fix pays back on every HubSpot 4xx forever.** The response body echo isn't gsheets-specific — every future HubSpot error surfaces with actionable context in the UI now. Worth the small surface-area increase in the JSON response contract
- **Tutorials are collapsed by default, not always-visible.** Considered rendering them always-open until the user dismisses. Rejected: (a) experienced users would either dismiss on every session or scroll past a wall of text, (b) `<details>` state doesn't need persistence (localStorage would be a step change in complexity for near-zero UX value), (c) the "Show me how" affordance is bright and clearly clickable — first-timers do find it
- **Schema-planner tutorial actively redirects to `/import` first.** For the non-dev target user, "paste hand-authored JSON here" is the wrong first move — `Suggest a schema` on the import wizard produces the JSON for them. Making the redirect prominent in the tutorial matches the intended flow

**Still open on `phases/phase-2.md`:**
- OAuth flow for private Google Sheets (deferred)
- Refresh-from-sheet action for saved mappings
- Full cycle handling — two-phase write for cyclic FK graphs, self-reference end-to-end
- F11 tail — re-run saved mapping against a different portal
- Detect interrupted jobs on runner restart (Phase 3 with Inngest)
- (Provision-writes-back-tableId — Phase-1 F3 polish carried over)
- New: **persist wizard state across nav** (currently sources are dropped when you leave `/import` and come back — pain point the smoke test surfaced when the user provisioned then returned to import). Not on the phase-2 checklist yet; add if it keeps biting

**Next up (unblocked):**
- **Inngest step-function rewrite** — the last "MVP → production scale" unlock
- **Refresh-from-sheet** — small QoL win now that gsheets is proven
- **`sessionStorage` for wizard state** — 15-minute fix, big UX payoff
- **Save the smoke-test recipe as a repeatable regression check** — could turn the Google Sheets end-to-end into a nightly cron via the `schedule` skill, but requires stable published test sheets we control

---

## Prior state — 2026-09-26 (evening)

**Phase 2 — Google Sheets source shipped (published-to-web CSV URL flavor).** Seventh Phase-2 epic; one of three checkboxes on the "Google Sheets source" block is ticked (rewrote the block to reflect what actually shipped vs. what stays deferred). Chose the published-CSV URL slice over the full OAuth flow so we ship without pulling in a Google Cloud project, consent screen, or per-user token storage — covers the common case where the sheet owner is fine publishing a public link. OAuth for private sheets stays deferred until someone hits the "can't publish this sheet" ceiling.

1. **`lib/source/gsheets.ts` (new)** — pure `normalizeGoogleSheetsUrl(url)` handles four Google Sheets URL shapes and rewrites where safe: canonical `/pub?output=csv` (pass-through), `/pubhtml` (rewrite path to `/pub`, set `output=csv`, preserve `gid`), `/edit#gid=…` and bare `/spreadsheets/d/{id}` (rewrite to `/export?format=csv`, lift `#gid=` from hash fragment to query, warn that the sheet must be publicly shared), and gviz (`/gviz/tq?tqx=out:csv`, pass-through). Non-`docs.google.com` hosts pass through with a warning ("not docs.google.com — fetching as a plain CSV endpoint"). Unrecognized `docs.google.com` paths reject with a message pointing at the *File → Share → Publish to web* flow. 11 vitest cases covering each URL shape, hash-gid lifting, non-numeric gid guard, empty/malformed input, non-`https` schemes, and unrecognized paths
2. **`POST /api/sources/gsheets` (new)** — mirrors the CSV/XLSX endpoint contract but sources bytes over the network rather than from a multipart upload. Accepts JSON body `{ sources: [{tableName, url}, ...] }`; validates that each entry has a non-empty name + URL and dedupes table names client-side of the parse. For each source: normalize the URL, `fetch()` server-side with a 30s `AbortController` timeout + 20 MB response cap + explicit `User-Agent` header (Google sometimes serves HTML to bot-like UAs), guard against `text/html` content-type (returns 502 with a "sheet may not be published or shared publicly" hint — papaparse would otherwise silently produce garbage rows), pipe through `parseCsv` + `validateSource`, return the same `{name, headers, preview, rows, totalRows, detected, parseWarnings, validationWarnings}` shape as the other source endpoints. Also emits a top-level `warnings[]` array carrying the per-source `normalizeGoogleSheetsUrl` warnings so the UI can surface "rewrote /edit URL" notices
3. **"Google Sheets" tab in `SourceUploader`** — fourth `ModeTab` alongside CSV / XLSX / JSON. Dynamic list of `{tableName, url}` pairs with Add another sheet / Remove buttons (Remove disabled when only one row remains). Text input for the table name, URL input with monospace font (URLs are long), submit button (`Fetch sheets`) POSTs to `/api/sources/gsheets`. Response drops into the same `SubmitState.success` variant so `SchemaInferPanel` / mapping / dry run / execute all light up unchanged
4. **`phases/phase-2.md` rewritten** — the "Google Sheets source" block used to list OAuth / sheet picker / refresh action. Rewrote to reflect the actual shipped scope (Published-CSV URL flow ✅) and what's deferred (OAuth for private sheets, refresh action which needs to store the URL alongside the mapping profile first)

**Files touched:** `lib/source/gsheets.ts` (new), `lib/source/gsheets.test.ts` (new, 11 cases), `lib/source/index.ts` (barrel), `app/api/sources/gsheets/route.ts` (new), `app/import/source-uploader.tsx` (fourth tab + `submitGsheets` + dynamic row state), `phases/phase-2.md` (block rewrite). 247 vitest cases / 18 suites (+11 net-new), tsc + lint clean, dev server serves 200 on `/` + `/import`

**Design decisions worth remembering:**
- **Server-side fetch, not client-side.** Considered fetching the CSV from the browser to skip the Vercel function invocation. Killed for two reasons: (a) CORS — Google's `/pub` endpoint doesn't send permissive CORS headers, so a browser `fetch` would fail on preflight, and (b) size cap + timeout — server-side we can enforce a 20 MB response cap and 30s AbortController timeout; browser-side both are subject to user network conditions. The extra function invocation is a small cost for a much simpler + more predictable failure mode
- **HTML content-type guard is a 502, not a silent parse.** When a sheet isn't published (or the user pasted an `/edit` URL for a private sheet), Google serves the sign-in HTML page. Papaparse would happily parse the HTML as one long CSV row and return garbage. Checking `Content-Type: text/html` before parse and 502-ing with a specific hint saves the user from "why did my import produce nonsense rows" debugging
- **`normalizeGoogleSheetsUrl` errs on the side of transforming rather than rejecting.** Users paste URLs from the browser address bar (`/edit#gid=…`), not always from *File → Publish to web*. Rewriting to `/export?format=csv` works for public sheets and fails loudly for private ones — better UX than rejecting the URL outright and forcing the user to hunt for the publish flow. The rewrite always includes a warning so the user sees what we did
- **One URL per HubDB table, not one URL per workbook.** Considered accepting the workbook URL and enumerating sheets via the gviz `sheet=` param. Would need us to know the sheet names ahead of time (which we don't for a bare workbook URL) or make an extra probe request. One URL per table is explicit, matches the XLSX per-sheet-is-a-table mental model, and users get to control the table name independently of the sheet tab name

**Still open on `phases/phase-2.md`:**
- OAuth flow for private Google Sheets (deferred — GCP project + consent screen; upgrade when someone hits the ceiling)
- Refresh-from-sheet action for saved mappings (needs to store the sheet URL alongside the mapping profile, then a re-fetch button on `/import`)
- Full cycle handling — two-phase write for cyclic FK graphs, self-reference end-to-end
- F11 tail — re-run saved mapping against a different portal
- Detect interrupted jobs on runner restart (Phase 3 with Inngest)
- (Provision-writes-back-tableId — Phase-1 F3 polish carried over)

**Next up (unblocked):**
- **Sandbox smoke test of gsheets** — publish a real sheet, paste the URL, confirm the fetch + parse + mapping flow end-to-end
- **Inngest step-function rewrite** — turns the persist-and-resume into full tab-close-safe durability; last "MVP → production scale" unlock
- **Refresh-from-sheet** — small F11-adjacent quality-of-life win now that we have the gsheets source; needs `mappings.state_json` to carry the URL

---

## Prior state — 2026-09-26 (afternoon)

**Phase 2 — resume from failure shipped (persist-and-resume flavor).** Sixth Phase-2 epic; three of four checkboxes on the "Resume from failure" block are ticked. Skipped "detect interrupted jobs on runner restart" — that belongs with the Phase-3 Inngest step-function rewrite. Chose the persist-and-resume slice over the full Inngest rewrite so we ship user-facing value without pulling in an external service; the persisted state (`job_batches`, `key_maps`) is exactly what Inngest would consume when we layer it on later.

1. **`lib/db/job-batches.ts` (new)** — thin repo. `recordBatchStart` upserts `{job_id, table_name, batch_index, status: "sent", sent_at}` before each HubSpot call (upsert-on-conflict so resume overwrites the same row). `recordBatchComplete` updates status to `succeeded` / `failed` + `finished_at`. `listCompletedBatches(jobId)` returns `{tableName → Set<batchIndex>}` for future skip-logic use
2. **`lib/db/key-maps.ts` (new)** — `upsertKeyMap(jobId, tableName, entries)` writes the natural-key → row-id map (Record<string, string>) at the end of each table's pass-1. `loadKeyMaps(jobId)` reads back all persisted maps for a job — wired for future Inngest step-function use; the current single-request executor doesn't yet preload from this since `listAllDraftRows` naturally sees the succeeded prior-run rows
3. **`lib/hubdb/import.ts` gains `ImportHooks`** — three optional callbacks on `ImportOptions.hooks`: `onBatchStart({table, batchIndex, kind, size})` fires before each HubSpot batch call; `onBatchComplete({table, batchIndex, kind, status})` fires after with `succeeded` / `failed`; `onKeyMapReady(table, entries)` fires when pass-1 finalizes the key map. All fired through a `fireHook(name, fn)` wrapper that catches + `console.warn`s errors — persistence is best-effort and never aborts the run. `batchIndex` is monotonic across the update-then-insert loops (update batch 1 → update batch 2 → insert batch 1 gets index 3) so the audit trail reads linearly
4. **`POST /api/portals/[id]/execute` route** — passes `persistHooks` bound to Supabase when `jobId` is available. New `resumeJobId` field in the request body: if set, skip `resolveMappingId` + `createJob` and reuse the existing job row (verified via `getJobById`, 404 if gone). Non-resume path unchanged. Hooks fire best-effort; hook-side Supabase errors never abort the HubSpot writes
5. **`app/jobs/[id]/page.tsx` — Resume button** — new primary-color card shown only for `failed` / `cancelled` jobs that still have a `portalId` + `mappingId`. Links to `/import?resume=<jobId>&portalId=<...>&mappingId=<...>`. Explains the semantics inline ("re-upload the same source files, then Execute — upserts by natural key skip already-imported rows")
6. **`SourceUploader` reads `?resume=` from `useSearchParams`** — auto-selects the portal (via `resumePortalId ?? portals[0]?.id`), fetches the linked mapping profile via `GET /api/mappings/[id]`, injects it into `mappings` state, shows a resume banner with three states (`loading` / `loaded` / `error`), and threads `resumeJobId` through `Results` → `ExecutePanel` → the POST body
7. **3 new vitest cases** on `importRows` — hooks called in the right order (start → complete → keymap per table), `onBatchComplete(status="failed")` fires when a batch throws and doesn't get swallowed, hook errors don't abort the run (simulated `onBatchStart` throw still lets the create succeed and the result come back with `ok: true`)

**Files touched:** `lib/hubdb/import.ts` (hooks + `fireHook`), `lib/hubdb/index.ts` (barrel export), `lib/hubdb/import.test.ts` (3 new cases), `lib/db/job-batches.ts` (new), `lib/db/key-maps.ts` (new), `app/api/portals/[id]/execute/route.ts` (hooks + resume path), `app/jobs/[id]/page.tsx` (Resume button), `app/import/source-uploader.tsx` (URL params + banner + prop thread), `app/import/execute-panel.tsx` (resumeJobId in body), `phases/phase-2.md` (three checkboxes ticked). 236 vitest cases / 17 suites (+3 net-new), tsc + lint clean

**Design decisions worth remembering:**
- **Persist-and-resume without Inngest.** Full Inngest rewrite would give tab-close survival + `maxDuration` bypass but requires an Inngest account, INNGEST_EVENT_KEY / SIGNING_KEY env vars, and Vercel routing config. The persist-and-resume slice ships in one session, unlocks the "click Resume, don't lose your wizard state" UX, and writes the exact `job_batches` / `key_maps` rows Inngest will read once we layer it on. Deferred the "detect interrupted jobs on runner restart" checkbox — no daemon in this architecture yet
- **Hooks are best-effort inside `importRows`.** `fireHook(name, fn)` catches + `console.warn`s any thrown error. Rationale: durability is a secondary concern to actually landing rows in HubDB. If Supabase is transiently unreachable during a run, the HubSpot writes still succeed and the response still comes back; the audit trail in `job_batches` may be incomplete but the primary work is done
- **`batchIndex` monotonic across update-then-insert.** Update batches get indices 1..M; insert batches get indices M+1..M+N. Alternative was per-kind indexing (updates 1..M, inserts 1..N) which would need a composite key on `(job_id, table_name, batch_index, kind)`. Monotonic keeps the schema simple and the audit trail linear
- **Upsert idempotency IS the resume mechanism.** Because `importRows` splits source rows into insert/update by looking up their natural key in `existingKeyMap`, a resume run naturally sees prior successes as pre-existing → re-classifies them as no-op updates. No explicit "skip completed batches" logic needed for correctness — the semantics fall out of the executor's upsert design. The persistence layer is for audit + future Inngest wiring, not correctness

**Still open on `phases/phase-2.md`:**
- Google Sheets source (OAuth + sheet picker + refresh-from-sheet on saved mappings)
- Full cycle handling — two-phase write for cyclic FK graphs, self-reference end-to-end
- F11 tail — re-run saved mapping against a different portal
- Detect interrupted jobs on runner restart (Phase 3 with Inngest)
- (Provision-writes-back-tableId — Phase-1 F3 polish carried over)

**Next up (unblocked):**
- **Sandbox smoke test of resume** — run an import to failure, verify the audit trail in `job_batches` + `key_maps`, click Resume from `/jobs/[id]`, confirm the wizard pre-loads and the second run completes
- **Google Sheets source** — the last "new intake" epic
- **Inngest step-function rewrite** — turns the persist-and-resume into full tab-close-safe durability

---

## Prior state — 2026-09-26 (morning)

**Phase 2 — XLSX source shipped.** Fifth Phase-2 epic ticked (all three checkboxes on the "XLSX source" block in `phases/phase-2.md`). Users can now upload `.xlsx` files alongside CSV / JSON; each sheet becomes an independent table that flows through the same mapping → dry run → execute pipeline unchanged.

1. **`lib/source/xlsx.ts` (new)** — pure `parseXlsx(bytes, opts)` returns `{sheets: [{name, headers, rows, warnings}], warnings}`. Uses SheetJS (`xlsx` npm, 0.18.5, Apache 2.0) via `XLSX.read(bytes, {type: 'array'})` + `sheet_to_json(sheet, {header: 1, defval: '', blankrows: false, raw: false})` to get row-major matrices with all cells stringified (matches CSV cell semantics — no ghost numbers/booleans getting into the pipeline). Sheet names normalized to safe identifiers (`[^\w.-]+ → _`) with clash-suffix dedup; headers deduped via the same `column_N` + `name_2` pattern as CSV. `headerRow` + `sheetNames` (allowlist, warns on unknown names) options. 9 vitest cases covering single-sheet, multi-sheet, cell coercion, header-row override, dedup, whitelist filtering, sheet-name normalization/clash, empty-sheet path, and empty-input rejection
2. **`POST /api/sources/xlsx` (new)** — mirrors CSV endpoint's contract. 25 MB per-file cap (a touch higher than CSV's 20 MB since XLSX files carry formatting/style overhead for the same row count). Emits the same `{tables[], warnings[]}` shape as `/csv` and `/json` so `SourceUploader`'s parse response type didn't need to change. Table names combine as `filename__sheetname` for multi-sheet workbooks and drop to just `filename` when the workbook has a single sheet (matches "filename == tablename" convention). Coerces `headerRow` and `sheetNames` from form fields with the same defensive parsing as CSV
3. **XLSX tab in `SourceUploader`** — third `ModeTab` alongside CSV / JSON. Form accepts multiple `.xlsx` uploads, a text input for the sheet allowlist ("brands, categories, products" — blank = all), and the same "Header row" number input helper the CSV form uses. No delimiter / encoding controls (not applicable). `submitXlsx` handler mirrors `submitCsv` — POST to `/api/sources/xlsx`, drop into the same `SubmitState.success` variant so downstream panels light up identically
4. **`lib/source/index.ts` barrel** — re-exports `parseXlsx`, `XlsxParseError`, plus the option/result types

**Files touched:** `lib/source/xlsx.ts` (new), `lib/source/xlsx.test.ts` (new), `lib/source/index.ts` (barrel), `app/api/sources/xlsx/route.ts` (new), `app/import/source-uploader.tsx` (tab + handler + form), `phases/phase-2.md` (three checkboxes). `package.json` gains `xlsx@^0.18.5`. 233 vitest cases / 17 suites (+9 net-new), tsc + lint clean

**Design decisions worth remembering:**
- **`raw: false` on `sheet_to_json`.** Forces SheetJS to format numbers, dates, and booleans as their display strings before we see them. Without this, a numeric SKU column would come through as JS `number` values, which the downstream validator (`validate.ts`) doesn't expect (it assumes cell values are always strings). Cell semantics now match CSV exactly: every cell is a string
- **Single-sheet workbooks drop the sheet name from the table name.** Users who export a single-sheet workbook expect `products.xlsx` to become a table named `products`, not `products__Sheet1`. Multi-sheet workbooks need disambiguation, so those combine as `filename__sheetname` — the double underscore signals "this came from a specific sheet"
- **Sheet allowlist is comma-separated string, not multi-select.** Considered a chip-based multi-select but that requires reading the workbook client-side first to know the sheet names, which doubles parse work. A text input scales to any workbook and unknown names emit a warning listing what's available in the response — the UX loop is "type names → parse → check warnings → adjust names" and terminates in one round in the common case
- **25 MB cap vs CSV's 20 MB.** XLSX files carry formatting, styles, and sometimes embedded thumbnails/images that make them denser than a CSV of the same row count. Bumped by 25% to accommodate real-world exports without being generous enough to invite abuse

**Still open on `phases/phase-2.md`:**
- Google Sheets source (OAuth + sheet picker + refresh-from-sheet)
- Resume from failure (Inngest per `docs/long-job-runner.md`)
- Full cycle handling — two-phase write for cyclic FK graphs, self-reference end-to-end
- F11 tail — re-run saved mapping against a new file / different portal
- (Provision-writes-back-tableId — Phase-1 F3 polish carried over)

**Next up (unblocked):**
- **Google Sheets source** — OAuth setup + `/api/sources/gsheets` + refresh action on saved mappings. Larger; needs Google Cloud project setup
- **Resume from failure** — Inngest wiring per `docs/long-job-runner.md`; the last "MVP → production scale" unlock
- **Sandbox smoke test of XLSX** — round-trip a multi-sheet workbook through `/import`

---

## Prior state — 2026-09-25 (late evening)

**Second-try fix on Base UI Select trigger.** The `deriveItemLabel` + `label={derivedLabel}` change from the earlier evening entry (commit `cbf08c1`) didn't actually solve the trigger-display issue — the `Select.Item.label` prop in Base UI is for **keyboard text-navigation matching** ("type 's' to jump to 'sandbox'"), NOT the trigger display. Trigger display comes from `Select.Root`'s `items` prop, or from a `children` render function on `Select.Value`. Without either, `Select.Value` stringifies the raw `value` (uuid, sentinel, whatever). Confirmed by reading `@base-ui/react/select`'s type definitions after the user reported the uuid still showing.

**Real fix — pass `items` prop to `Select.Root` at each call site with a value→label mismatch:**
- `source-uploader.tsx` Target portal picker — `items` maps `p.id → "{p.label} · {p.env} · Hub {p.hubId}"`
- `source-uploader.tsx` `SelectField` helper (delimiter/encoding) — `items` derived from `options`, with `AUTO_SENTINEL` remap for the empty-value case
- `profile-panel.tsx` profile picker — `items` maps `p.id → p.name`
- `mapping-editor.tsx` target-table picker — `items` maps `t.name → "{t.label} ({t.name})"`
- `mapping-editor.tsx` `AssignmentSelect` (three-way per-column mapping) — `items` includes unmapped/ignored sentinels + `col:{name}` entries with "(in use)" suffix on claimed targets
- `mapping-editor.tsx` `SelectField` (hs_name/hs_path picker) — `items` prepends the NONE_SENTINEL entry to caller-supplied options
- `mapping-editor.tsx` ForeignKeyPanel — multi/delimiter/onMissing/matching all get `items` arrays with the friendly labels ("single/multiple", ", (comma)", "null the cell", "default (trim + casefold)", etc.)
- `schema-infer-panel.tsx` FK target picker — `items` maps `t.name → "{t.name} · {t.naturalKey}"`
- `execute-panel.tsx` publish-mode selector — `items` for "none (draft only) / foreign tables only / all"

Kept the `deriveItemLabel` + `label={...}` change on the shadcn `SelectItem` wrapper (commit `cbf08c1`) — that's still useful for keyboard nav. Reverted a short-lived context-registry attempt (would have auto-wired trigger display from `SelectItem` children) after discovering it can't work: Base UI renders `SelectItem`s inside a lazy Portal that only mounts when the popover opens, so on first render the registry is empty and the trigger has nothing to display

**Files touched:** `app/import/source-uploader.tsx`, `app/import/profile-panel.tsx`, `app/import/mapping-editor.tsx`, `app/import/schema-infer-panel.tsx`, `app/import/execute-panel.tsx`. 224 vitest cases still green, tsc + lint clean

**Design decisions worth remembering:**
- **`items` prop is Base UI's intended API for value→label mapping.** It pre-declares the mapping at Root level, so `Select.Value` can render the trigger before any Item mounts. Popup children (`<SelectItem>`) remain the source of truth for the popover list; `items` is a parallel declaration purely for trigger display. Yes it's a duplication, but the two shapes differ enough (popup wants rich JSX for the "(in use)" badge, trigger wants a plain string) that a single source doesn't map cleanly
- **Portal-mount-lazy is the trap.** Any "auto-wire from SelectItem children" solution — via Context, Refs, or a global registry — fails because Portal doesn't render children until open. This is documented in Base UI but not obvious from the API surface. Rule of thumb for future Base UI work: assume Portal-rendered subtrees don't exist until opened
- **`SelectItem.label` prop kept on the wrapper.** Even though it doesn't fix the trigger, it's still the correct place for the keyboard-text-nav label. Wrapper's `deriveItemLabel` covers most cases automatically

---

## Prior state — 2026-09-25 (evening — superseded by the late-evening fix above)

**Fix — Base UI Select trigger showed raw value instead of the item's label.** ~~Fixed in the shadcn wrapper by walking children~~ Fix was incomplete — see the late-evening entry above for the actual working fix via `items` prop. Keeping this entry for the historical trail on why the wrapper-based approach seemed right but wasn't

---

## Prior state — 2026-09-25 (afternoon)

**Form-control height alignment pass.** After the shadcn Select rollout every form control had slightly different metrics — buttons + selects were `h-7` (28px, from `size="sm"`), text inputs sat around 40px (ad-hoc `py-2 text-sm`), and `<input type="number">` was taller still because WebKit's spinner controls add invisible padding. All standardized on **h-9 (36px)** — a common form-control height that reads comfortably next to Hanken Grotesk body copy without feeling bulky.

1. **`components/ui/button.tsx`** — bumped `default` + `sm` variants to `h-9 px-3` (were `h-8 px-2.5` / `h-7 px-2.5`), `lg` to `h-10 px-4`, icon-sm to `size-9`, icon-lg to `size-10`. `xs` / `icon-xs` stay at `h-6` for genuinely-tiny inline actions. Padding scaled proportionally so text doesn't crowd the border. Added a comment explaining the canonical form-control height so future edits don't drift back
2. **`components/ui/select.tsx`** — both `default` and `sm` size variants set to `h-9`; left padding standardized to `pl-3` (was `pl-2.5`) to match Button. Trigger and Content share the same visual weight now, so open/closed states don't shift the popover origin
3. **Text inputs (5 sites)** — swept the ad-hoc `py-2 text-sm` / `py-1.5` / `py-1` patterns and replaced with explicit `h-9 px-3` in `portal-form.tsx` (3 inputs incl. font-mono token), `source-uploader.tsx` (InputField helper), `profile-panel.tsx` (save-name), `schema-infer-panel.tsx` (label edit). Textareas (2 sites, `rows={8}` on the schema paste, `rows={10}` on the JSON paste) intentionally left alone — multi-row inputs shouldn't force a single-row height
4. **`app/globals.css`** — new `@layer base` rule strips WebKit `::-webkit-inner/outer-spin-button` and sets Firefox `appearance: textfield` on `<input type="number">`. Without this, WebKit/Firefox render number inputs slightly taller and offset from regular text inputs even when both share the same `h-9 + px-3`. Global so any future number input inherits the fix — the "Header row" spinner in the CSV upload form was the visible tell

**Files touched:** `components/ui/button.tsx`, `components/ui/select.tsx`, `app/globals.css`, `app/portals/portal-form.tsx`, `app/import/source-uploader.tsx`, `app/import/profile-panel.tsx`, `app/import/schema-infer-panel.tsx`. 224 vitest cases still green (UI-only), tsc + lint clean

**Design decisions worth remembering:**
- **h-9 (36px) is the canonical form-control height.** Chose this over h-8 (32px, feels a touch cramped with the 14px body) and h-10 (40px, starts to look chunky in dense wizard views). Buttons that want to be smaller use `xs` (h-6); everything else defaults to `sm`/`default` which are now both h-9. Buttons that want to be larger use `lg` (h-10)
- **Padding hard-coded per-variant, not tokenized.** Considered introducing `--h-form` / `--px-form` CSS variables so the token lives in one place, but Tailwind arbitrary values (`h-[var(--h-form)]`) hurt tree-shaking and readability. Explicit `h-9 px-3` in the two shadcn primitives + comment covers the future-drift risk without the indirection
- **Number-input spinner reset lives in globals.css, not per-usage.** WebKit's default spinner is invisible-padding-then-arrows, which throws every layout that mixes number + text inputs. Once-per-project reset in the base layer beats remembering to add `[&::-webkit-inner-spin-button]:appearance-none` to every `type="number"` site

---

## Prior state — 2026-09-25 (morning)

**Two follow-on passes: shadcn Select rollout + docs cleanup on the F3 two-button flow.**

**A. shadcn Select swap — all 16 native `<select>` sites replaced.** Ran `pnpm dlx shadcn@latest add select` (auto-picks the Base UI variant per `components.json`'s `base-nova` style). New `components/ui/select.tsx` exports `Select / SelectTrigger / SelectValue / SelectContent / SelectItem / SelectGroup / SelectLabel / SelectSeparator / SelectScrollUp/DownButton`. Then swapped every native `<select>` in the app:
- `app/portals/portal-form.tsx` — env picker (FormData-based; added `useState` + hidden `<input type="hidden" name="env">` so submit still picks up the value)
- `app/import/source-uploader.tsx` — portal picker + the reusable `SelectField` helper (delimiter/encoding overrides in the CSV upload form)
- `app/import/profile-panel.tsx` — profile load dropdown
- `app/import/execute-panel.tsx` — publish mode selector
- `app/import/schema-infer-panel.tsx` — type + FK target dropdowns
- `app/import/mapping-editor.tsx` — target picker, `AssignmentSelect` (three-way unmapped/ignored/target with disabled options), `SelectField` (hs_name/hs_path), and all six FK-panel selects (source / matchKey / multi / delimiter / onMissing / matching)

224 vitest cases still green (UI-only), tsc + lint clean; every route (`/`, `/portals`, `/import`, `/docs`, `/jobs`) still serves 200

**B. `/docs` walkthrough step 3 rewritten** to spell out the two-button flow on the schema planner: **Generate plan** = preview (POST `/api/portals/[id]/diff` → four per-table verdicts `create / match / update / conflict`, no writes), then **Provision** = actual write (POST `/api/portals/[id]/provision`, only enabled when the plan has zero conflicts, topological order, draft-only). The old copy conflated the two into a single "click Provision" instruction which confused first-time users — now the diff-then-commit safety pattern is explicit

**Files touched:** `components/ui/select.tsx` (new, via shadcn CLI + auto-picked Base UI deps `@base-ui/react`), `app/portals/portal-form.tsx`, `app/import/source-uploader.tsx`, `app/import/profile-panel.tsx`, `app/import/execute-panel.tsx`, `app/import/schema-infer-panel.tsx`, `app/import/mapping-editor.tsx`, `app/docs/page.tsx`. 224 vitest cases still green, tsc + lint clean

**Design decisions worth remembering (Base UI Select quirks — these will bite next time):**
- **`onValueChange` fires with `string | null`** (null on deselect / clear), not just `string`. Every handler needs a `(v) => v && ...` or `(v) => setX(v ?? "")` guard. Base UI's Select isn't a drop-in Radix substitute even though the surface looks identical
- **Empty-string values are reserved** for the "no selection" state. Items that need to represent "unset / none / auto" have to use a sentinel constant that maps back to `""` at the FormData / caller boundary. Codebase now has three: `AUTO_SENTINEL = "__auto__"` (source-uploader), `NONE_SENTINEL = "__none__"` (mapping-editor SelectField), `UNMAPPED_SENTINEL = "__unmapped__"` (mapping-editor AssignmentSelect). Kept file-local for now — if we grow more, pull into a shared `lib/ui-sentinels.ts`
- **FormData-participating selects use a hidden `<input>`, not the Select's `name` prop.** Base UI Select does render a hidden native select when `name` is set, but the behavior around empty-string values makes it awkward for our "auto" placeholder pattern. Explicit `<input type="hidden">` is easier to reason about
- **shadcn's default `select.tsx` imports match the repo's non-standard setup** (`from "cn"` for the util, `from "@base-ui/react/select"` for the primitive). The generated file works as-is; don't rewrite the imports to match the "canonical" `@/lib/utils` pattern — the repo's `lib/utils.ts` is literally a one-liner re-export from `"cn"`

---

## Prior state — 2026-09-24 (late evening)

**Phase 2 — schema inference from CSV shipped.** Fourth Phase-2 epic ticked (all four checkboxes on the "Schema inference" block in `phases/phase-2.md`). Users can now upload CSVs and get a working schema.json without hand-authoring the JSON.

1. **`lib/schema-infer.ts` (new)** — pure `inferSchema(sources)` returns `InferredTable[]` with per-column type inferred from cell samples. Rules ordered most-specific first: DATE (`YYYY-MM-DD`) → DATETIME (parses + has time-of-day) → BOOLEAN → URL (http/https) → IMAGE (URL + image extension `.png/.jpe?g/.gif/.webp/.svg/.avif/.bmp/.ico`) → CURRENCY (numeric + column-name hint like `price/cost/amount/total/revenue/salary/fee/rate`) → NUMBER → RICHTEXT (any cell > 500 chars) → TEXT. Natural-key picker: fully-unique columns (case-insensitive dedupe) with ≥50% fill ratio; preference tier by name (`id`, `uuid`, `sku`, `slug`, `handle`, `code`, `key`, `identifier`, then `*_id`, then others). FK detection is a **value-membership check** (not a name heuristic): all non-empty values of column A must appear in table B's naturalKey column, case-insensitive; all-or-nothing per column. Second helper `toSchema(tables)` converts the inferred shape to the v1 schema the F3 provisioner accepts, stripping UI-only annotations (`isNaturalKey`, `reason`). 21 vitest cases cover every type branch, NK priority ordering (id > sku > handle), FK false-positive guards (case-insensitive match, subset check, NK never flagged as FK), and `toSchema` field-stripping
2. **`SchemaInferPanel` (new)** — client component rendered above the mapping cards on `/import` once any source is parsed. Collapsed by default with a "Review + download" primary button and a header summary line ("N tables · M natural keys · K foreign-key links"). Expanded view: per-table card with an editable label input, live counts, and a column table with per-row **type dropdown** (10 options incl. FOREIGN_ID), **natural-key checkbox** (single-column NK for now — composite still hand-editable in the JSON), **FK target picker** (only enabled when type = FOREIGN_ID; lists sibling tables that have a naturalKey; auto-defaults `foreignColumn` to the target's NK), and a **reason column** ("all values match YYYY-MM-DD", "every value found in brands.slug", etc.). Reset button restores the inferred baseline; Download emits a pretty-printed `schema.json`
3. **Wired into `source-uploader.tsx`** — panel slots in between the top-of-Results header and the ImportOrderPanel. Renders even before a target portal is picked (schema authoring is independent of the F3 diff), so a user can arrive with just CSVs, download `schema.json`, then paste into the schema planner
4. **State-reset pattern chosen deliberately** — panel's editable state re-derives when the upload fingerprint (`table names + row counts`) changes. Used the React sanctioned "setState during render, guarded by a committed-value check" pattern instead of a `useEffect` (which trips ESLint's `react-hooks/set-state-in-effect`). One-shot re-derive, no double-render

**Files touched:** `lib/schema-infer.ts` (new), `lib/schema-infer.test.ts` (new), `app/import/schema-infer-panel.tsx` (new), `app/import/source-uploader.tsx` (import + slot), `phases/phase-2.md` (four checkboxes ticked). 224 vitest cases green (+21), tsc + lint clean

**Design decisions worth remembering:**
- **FK detection uses value membership, not name heuristics.** A column named `brand` doesn't imply an FK — it might be a free-text field. Instead, we check if every non-empty value of `products.brand` appears in `brands.slug` (case-insensitive). Zero false positives at the cost of missing FKs where the source data has typos or unresolvable rows — a fair trade for a UI that's optional and pre-execution
- **Type inference is conservative — TEXT is the fallback for anything ambiguous.** Every stricter type requires *all* non-empty samples to match. One bad row rules out DATE/NUMBER/etc. Users can bump a mixed column up to a stricter type by hand in the panel; over-inferring would just create HubSpot validation errors later
- **Panel renders before the portal picker resolves.** Schema authoring doesn't need the portal — it's data-source-only. So the "get a schema.json" flow doesn't wait on `fetchPortalSchema`, meaning a user can grab their schema from CSVs alone and use it in a different context (paste into a JSON editor, commit to a repo, etc.) if they want to
- **`setState-during-render` reset instead of `useEffect`.** ESLint's `react-hooks/set-state-in-effect` rule flags the naive "reset state when fingerprint changes" effect pattern. The alternative (comparing committed-value in render and calling setState conditionally) is documented in the React docs as the correct pattern for prop-driven resets — one re-render, no cascade

**Still open on `phases/phase-2.md`:**
- Resume from failure (Inngest per `docs/long-job-runner.md`)
- XLSX + Google Sheets sources
- F11 tail — re-run saved mapping against a new file / different portal
- Full cycle handling — two-phase write for cyclic FK graphs, self-reference end-to-end
- (Provision-writes-back-tableId — Phase-1 F3 polish carried over)

**Next up (unblocked):**
- **Deploy to Vercel** — everything ready (`DEPLOYMENT.md` + `vercel.json`); manual step is the only remaining lift
- **XLSX source** — new source type, sheet-per-table detection; drop-in extension to `/api/sources/*`
- **Resume from failure** — bigger; needs Inngest wiring per `docs/long-job-runner.md`

---

## Prior state — 2026-09-24 (evening)

**`/docs` guide + downloadable example dataset shipped.** New end-user-facing walkthrough page with a scroll-spy TOC, a fifth sidebar nav item, and a small relational dataset served from `public/examples/` so a new user can go from zero to a full sandbox run without touching any code.

1. **`/docs` route** — `app/docs/page.tsx` (server component). Structured content across six sections: overview / example dataset / prerequisites / 8-step walkthrough / reference / troubleshooting. Every step calls out the route it targets (`/portals`, `/import`, `/jobs`) and inline-links back to the running app via `NavLink`. Reusable `Section` / `Subsection` / `Step` / `Code` / `NavLink` / `ExternalLink` helpers styled with the S2 palette
2. **Example dataset** — `public/examples/{brands,categories,products}.csv` + `public/examples/schema.json`. Small product catalog (4 brands, 4 categories, 8 products) exercising both FK columns via slug natural keys and every column type in play (TEXT / URL / NUMBER / RICHTEXT / FOREIGN_ID). schema.json is a valid v1 schema definition ready to paste into F3 provisioning. All four files served statically; download links use `<a download>` for the correct filename
3. **Inner navigation for the guide** — `app/docs/sections.ts` exports the single source of truth for section id + title; `app/docs/docs-toc.tsx` (client component) renders two variants of the TOC — a **sticky sidebar** on `lg` screens (right column of a two-col grid, left-border active highlight in primary color) and a **compact inline card** on smaller screens (pill-style flex-wrap links). Both share one `IntersectionObserver` under the hood with `rootMargin: "-20% 0px -70% 0px"` so the topmost section crossing ~20% down from the viewport becomes active
4. **Sidebar nav item** — new "Docs" entry using `public/icons/team-building.svg` (Saltedstone S2 set; forest fill so the mask trick still works). Sidebar icons bumped from `size-4` (16px) to `size-5` (20px) at user request; mobile nav kept at `size-3.5` for the compressed strip. Home-page tile added for `/docs` so it's discoverable from `/` too
5. **Global smooth-scroll** — `scroll-behavior: smooth` on `html` in `globals.css`; each `<Section>` gets `scroll-mt-20` so anchor jumps animate and land under the mobile sticky nav bar

**Files touched / added:** `app/docs/page.tsx` (new), `app/docs/docs-toc.tsx` (new), `app/docs/sections.ts` (new), `app/nav.tsx` (Docs item + size bump), `app/page.tsx` (Docs tile), `app/globals.css` (smooth scroll), plus assets: `public/examples/{brands,categories,products}.csv`, `public/examples/schema.json`, `public/icons/team-building.svg`. 203 vitest cases still green (UI-only + static assets), tsc + lint clean

**Design decisions worth remembering:**
- **Single sections module, two TOC variants.** The `SECTIONS` const in `sections.ts` is the only place section ids + titles live — both the page (`Section id={...}`) and the TOC iterate over it. Adding a new section = one edit. Both TOC variants call the same `useActiveSection()` hook so the sticky sidebar + inline card stay in sync during scroll
- **IntersectionObserver, not scroll listener.** Cheaper (browser-scheduled), zero throttle logic needed. Only downside is the "which one wins when several are simultaneously visible" tie-break — resolved by picking the intersecting section with the highest `top` (closest to viewport top), which matches the "you are here" intuition
- **Grid layout instead of `float` or a `fixed` panel.** Two-column CSS grid with `minmax(0,1fr)_200px` keeps the sticky TOC in normal flow — no viewport-height math, no z-index games, and the TOC scrolls with the page footer if the content is shorter than the sidebar
- **Example dataset chosen to be the smallest thing that exercises the full loop.** Two foreign tables + one main table with two FK cols means the toposort runs (not a single-node graph) and both FK columns need independent resolution. Slug natural keys chosen because they're the most common real-world identifier in CMS data — matches how a user is actually going to model their content

---

## Prior state — 2026-09-24 (afternoon)

**Saltedstone brand pass — palette + typography + wordmark + icons.** No functional changes; UI-only reskin so the app looks like it belongs to S2 rather than default shadcn.

1. **Brand palette in `app/globals.css`** — full 24-token Saltedstone palette exposed as `--color-brand-*` CSS variables at `:root` (ochre / forest / coffee / purple / olive / burgundy / navy / chestnut / lilac / yellow / grass / red / blue / thistle / butter / mint / salmon / sky / sand / cream / offwhite / slate). Every existing shadcn token (`--primary`, `--background`, `--foreground`, `--muted`, `--accent`, `--destructive`, `--border`, `--ring`, `--sidebar-*`, `--chart-1..5`) remapped onto them so every existing component reskins for free — light mode: offwhite bg + forest text + ochre primary + yellow accent; dark mode: forest bg + offwhite text (same ochre primary + yellow accent). Cards get a lighter offwhite shade (`#f8f4ea`) for surface contrast; sidebar drops one step (`#eae4d5`) so chrome reads distinct
2. **Hanken Grotesk swapped in for Geist** as the app font — `next/font/google` loads `Hanken_Grotesk` with `variable: "--font-hanken-sans"`; `globals.css`'s `--font-sans` + `--font-heading` both point at it; Geist Mono kept for `<code>` blocks. SIL OFL 1.1 license — free for any use
3. **Saltedstone favicon + wordmark** — `favicon-32x32.png` from `saltedstone.com/hubfs/favicomatic/` fetched to `app/icon.png` (Next 16 auto-detects and injects `<link rel="icon">`); default Next scaffold `favicon.ico` removed so it doesn't compete. Full `saltedstone-2025.svg` wordmark fetched to `public/saltedstone-logo.svg` and rendered in the sidebar header at 24px tall with `dark:invert` so it flips forest → offwhite on the dark forest bg
4. **Nav icons swapped to Saltedstone S2 SVG set** — four icons pulled to `public/icons/`: `laptop.svg` (Home), `compensation.svg` (Portals), `hubspot-expertise.svg` (Import), `custom-solutions.svg` (Jobs). Rendered as `<span>` with CSS `mask-image` + `bg-current` in both `Sidebar` and `MobileNav` so the flat-`#1D2923` glyphs inherit the nav item's current text color — no per-mode filter juggling; icon flips forest ↔ offwhite between inactive/active states and between light/dark modes automatically. `lucide-react` imports dropped from `nav.tsx` (dep still installed for anywhere else)
5. **App title tweaked** — sidebar header + home hero h1 + `<title>` metadata all say "S2 HubDB Importer" (rebrand from bare "HubDB Importer"). Phase-1-MVP tag still sits above the title in both spots

**Files touched:** `app/globals.css` (palette + font var swap), `app/layout.tsx` (Hanken import + wordmark), `app/nav.tsx` (logo, icons, S2 title), `app/page.tsx` (S2 title), plus new assets: `app/icon.png`, `public/saltedstone-logo.svg`, `public/icons/{laptop,compensation,hubspot-expertise,custom-solutions}.svg`. Removed: `app/favicon.ico`. 203 vitest cases still green (UI-only change), tsc + lint clean

**Design decisions worth remembering:**
- **`--color-brand-*` at `:root`, shadcn tokens as aliases.** Split so the palette stays browsable / documentable in one block and shadcn-consumers keep working via existing `bg-primary` / `text-muted-foreground` / etc. New code that wants a specific brand shade reaches directly for `bg-[var(--color-brand-lilac)]` — no Tailwind config edit needed
- **CSS mask-image + `bg-current` for the icon set, not `<img>` + `dark:invert`.** The invert trick works for the wordmark but breaks on nav items where the active state also flips the text color (forest → offwhite on active). Mask trick makes the icon inherit whatever color the surrounding text has, so it's correct in all four combinations (light/dark × active/inactive) without any state-specific CSS
- **Card + sidebar shades hand-picked, not palette-drawn.** `#f8f4ea` (card) and `#eae4d5` (sidebar) are one step off offwhite in either direction — the palette doesn't include either. Keeps card/sidebar readably distinct from the main background without introducing a whole new hue

---

## Prior state — 2026-09-24 (morning)

**Phase 2 kickoff — mapping profile duplicate / export / import shipped.** Three F11 checkboxes ticked in one pass; 203 vitest cases (+11 net-new) still green, tsc + lint clean.

1. **`lib/mapping-profile.ts` (new)** — pure module. Exports a versioned envelope (`kind: "hubdb-importer-mapping-profile"`, `version: 1`, `name`, optional `exportedAt`, `state`) plus `serializeProfileExport`, `parseProfileExport` (zod-validated; envelope only — state is `passthrough()` so we don't re-validate deeply here, wizard/executor already do that at load time), and `nextCopyName(base, existing)` (`Copy of X`, then `Copy of X (2)`, `(3)`… — predictable, no fancy re-parsing when the base is itself a copy). 11 vitest cases covering serialize round-trip, envelope rejects (wrong kind / wrong version / empty name / empty state / garbage null / string / `{}`), backward-compat when `exportedAt` is omitted, and `nextCopyName` collision walks
2. **`duplicateMapping(client, id)` in `lib/db/mappings.ts`** — server-side helper: fetches the source profile (throws new `MappingNotFoundError` if gone), lists the portal's siblings, feeds names into `nextCopyName`, inserts. State JSONB is cloned verbatim so the copy is an exact snapshot. `MappingNotFoundError` exported from the repo module
3. **`POST /api/mappings/[id]/duplicate` (new route)** — thin wrapper: 404 on `MappingNotFoundError`, 409 on `DuplicateMappingNameError` (shouldn't happen post-`nextCopyName` but safe fallback), 201 with new profile on success
4. **`ProfilePanel` UI** — new actions row under the existing 2-col load/save grid: **Duplicate** (needs selection, hits the new endpoint, auto-selects the copy), **Export JSON** (client-side blob download named `{slug}.hubdb-profile.json`), **Import JSON…** (hidden `<input type="file">`, reads text → `parseProfileExport` → POST to `/api/portals/[id]/mappings`; if the profile name collides on the target portal, auto-renames via `nextCopyName` and surfaces the rename in the status message so the user knows it happened). Single `busy` state serializes the three actions so no double-click can fire two mutations in flight

**Files touched:** `lib/mapping-profile.ts` (new), `lib/mapping-profile.test.ts` (new), `lib/db/mappings.ts`, `app/api/mappings/[id]/duplicate/route.ts` (new), `app/import/profile-panel.tsx`. Phase-2 checklist updated

**Design decisions worth remembering:**
- **Envelope, not raw state.** Exports carry `kind` + `version` so we can (a) detect a paste of raw wizard state vs. a real export, and (b) grow the wire shape later with a migration step in `parseProfileExport`. Cost: two extra keys on disk. Value: unambiguous on ingest
- **State shape validated shallowly on import.** `parseProfileExport` uses `passthrough()` for each source-table entry so we don't re-encode `MappingState`'s deep zod schema in two places. If someone hand-crafts a broken state, they'll see the failure the moment the wizard or executor touches it — same failure mode as any other in-memory edit
- **Duplicate + Import both use `nextCopyName`.** Server-side helper for duplicate (needs a DB read anyway to fetch the source), client-side for import (already have the full profile list in state). Same collision walk, same predictable output
- **Rename on import is silent-with-notice, not a modal.** Prompting the user for a new name mid-upload would gate an already-committed action. Auto-suffix + a clear status message ("Imported as X (renamed from Y — a profile by that name already exists)") is friendlier and one less click

**Still open on `phases/phase-2.md`** (F11 tail):
- Re-run saved mapping against a new file (mostly a UX flow — profile is already loadable; needs the wizard to accept "load profile first, then upload matching sources")
- Re-run saved mapping against a different portal (needs a portal picker in the load step + name→id resolution when the target portal has different table ids)

**Next up (unblocked):**
- Rest of Phase 2 — resume from failure (Inngest per `docs/long-job-runner.md`), schema inference from CSV, XLSX + Google Sheets sources, the two F11 re-run bullets above
- Sandbox smoke test — export a real profile, edit the JSON, re-import into the same portal to see the auto-rename land
- Deploy — manual Vercel step still pending

---

## Prior state — 2026-09-23 (afternoon)

**Five follow-on tasks shipped in one pass**, after the morning's server-side hardening:

1. **Sandbox smoke test** — all three new 400s confirmed live against the Dev sandbox via curl: `dry-run signature mismatch` (400), `cell-too-long` (400, echoes count + sampleRowIndex + sampleLength), `page-path-invalid` (400, echoes full `PathValidationIssue[]` including `not-lowercase` / `invalid-char` / `duplicate`). Closes the "not sandbox-verified" caveat from the morning entry
2. **Stale-ID retry on FK write error** — `lib/hubdb/import.ts` gains `isPossiblyStaleFkError(err)` heuristic (HubdbError with 4xx status + body hinting at "foreign" / "no such row" / "does not exist" / "not found"), a `refreshForeignKeyMaps()` closure inside `importOneTable` that re-lists every foreign table + rebuilds its key map, and a retry-once wrapper around both `batchCreateDraftRows` and `batchUpdateDraftRows`. On the retry, batches are re-planned via `planRow` (so freshly-resolved FK ids replace stale ones); any rows that still error drop cleanly into `result.errors`/`skipped`. Emits a new `stale-fk-retry` event with `{table, batchKind, refreshed}`. If no foreign tables are refreshable (schema has none, or all lack naturalKey), original error propagates. Threads `schemaByName` from `importRows` into `importOneTable` for the foreign lookup. 2 new tests: `create` fails once with a foreign-key-shaped 400 → refresh brings a fresh id (999 vs stale 500) → retry succeeds with the fresh id in the FK cell; `create` fails but no naturalKey-having foreign tables exist → HubdbError propagates
3. **Cache provisioned table IDs in mapping profile** — `MappingState` gains optional `targetTableId?: string | null`; `initialMappingState()` seeds it as `null`; `MappingEditor::chooseTarget` populates it from the picked `HubdbTable.id`. Round-trips through the profile's `state_json` JSONB blob for free (no new migration). Displayed inline under the target picker as `id <n> — cached in profile`. Server routes still resolve by name (id is a stability/provenance hint, not a lookup key)
4. **Cancel button** — new `ImportCancelledError` (exported from `lib/hubdb` barrel) carries a `partial: ImportResult`; new `_CancelSignal` module-private sentinel mirrors `_FailFastSignal`. `ImportOptions.signal?: AbortSignal` added; a `checkCancel()` closure inside `importOneTable` runs before each `table-start`, each update-batch, and each insert-batch (batch boundary — the in-flight HubSpot call always completes). `importRows` catches `_CancelSignal` and rethrows `ImportCancelledError` with the aborted table + placeholders for the untouched remainder. Execute route passes `req.signal` in, catches `ImportCancelledError` → HTTP 499 with `cancelled: true`, marks the job row `cancelled` in `completeJob`. `ExecutePanel` gets an `AbortController` per run, shows a red "Cancel" button while running, adds a `cancelled` stage that renders a yellow banner + partial `ResultView`. If the browser aborts before the server flushes the 499, the UI still shows a "Cancelled — server may still complete current batch" note. 1 new test: 250-row insert cancels between batches → 100 created, 150 unmarked, downstream table listed as 0/0/0
5. **Middleware → proxy migration** (Next 16) — codemod refused (dirty tree), so did it by hand: renamed `middleware.ts` → `proxy.ts`, renamed export `function middleware` → `function proxy`. Matcher config unchanged. Verified live via curl: no creds → 401, correct creds → 200, wrong creds → 401. Dev server no longer prints the deprecation warning; startup log shows `proxy.ts: 111ms` confirming Next picked up the new convention

**Files touched:** `lib/hubdb/import.ts`, `lib/hubdb/index.ts`, `lib/hubdb/import.test.ts`, `lib/mapping.ts`, `lib/mapping.test.ts`, `lib/execution.test.ts`, `app/api/portals/[id]/execute/route.ts`, `app/import/execute-panel.tsx`, `app/import/mapping-editor.tsx`, `middleware.ts` → `proxy.ts`. **192 vitest cases / 14 suites** (+4 net-new this pass). tsc clean, lint clean

**Design decisions worth remembering:**
- **Stale-FK detector is deliberately broad.** No documented HubSpot error code for stale FK id; matched on 4xx + string hints. False positives cost one extra list-per-foreign-table read — acceptable versus the risk of missing real cases
- **Cancel = batch-boundary, not mid-batch.** Mid-batch cancel would need cancelling the fetch to HubSpot, and HubSpot may still process the write server-side, so mid-batch cancel would leave the job in a weird partial state. Batch-boundary keeps the invariant that every issued batch either fully succeeds or fully fails
- **Cancel returns 499 (client-closed-request).** Non-standard but widely understood (nginx conv). Client that aborted the fetch won't see this response — the value is that the job row is marked `cancelled` so `/jobs` shows the story after the fact
- **`targetTableId` is a hint, not a lookup key.** Server routes still resolve `mapping.targetTableName` → portal `HubdbTable.id` via `fetchPortalSchema`. Caching the id doesn't skip the fetch (we also need column types for synthesis), but it survives a target rename and gives future features a stable pointer

**Still open on `phases/phase-1-mvp.md`** (polish + robustness intentionally deferred):
- F3: write provisioned table IDs back into mapping profile (now easier — `MappingState.targetTableId` exists; provision UI just needs to write it back for the row it just created)
- F8: per-batch cursor persistence, worker-runner separation, bounded slice + re-enqueue (Phase-2 runner rework in `docs/long-job-runner.md`)
- SSE progress: `GET /api/jobs/[id]/stream` + UI subscriber

**Next up (unblocked):**
- Phase 2 — mapping profile export/import JSON, resume from failure (Inngest), schema inference, XLSX + Google Sheets sources
- Manual Vercel deploy — `DEPLOYMENT.md`
- Provision-writes-back-tableId — the small remaining F3 polish

---

## Prior state — 2026-09-23 (morning)

**Server-side hardening pass landed.** Three synth/plan-time gates now stop doomed batches before they hit HubSpot:
1. **Cell-length caps** — `synthesizeExecution` rejects mapped rows whose source cell exceeds the target's ceiling (RICHTEXT: 65k, everything else: 10k). New `cell-too-long` issue kind carries `{source, sourceColumn, targetColumn, targetType, maxLength, count, sampleRowIndex, sampleLength}`
2. **hs_path validation** — when a mapping sets `hsPath`, synth calls `validatePathColumn` server-side. New `page-path-invalid` issue kind echoes the full `PathValidationIssue[]` list (`not-lowercase` / `invalid-char` / `duplicate` / `empty`)
3. **Dry-run signature gate** — new `computeExecutionSignature(sources, mappings)` (canonical-JSON SHA-256) returned by the dry-run endpoint. Execute now requires `dryRunSignature` in the body and 400s if the client-supplied hash doesn't match the server-recomputed one. Any edit to sources or mappings clears the client-side sig; `ExecutePanel`'s button is disabled without one

**Wiring:** `lib/execution.ts` gains both preflight blocks + the signature helper; `POST /api/portals/[id]/dry-run` returns `signature` (headers stripped before hashing since execute doesn't send them); `POST /api/portals/[id]/execute` requires `dryRunSignature` in the body zod schema; `DryRunPanel` fires an `onComplete(signature)` callback; `source-uploader` holds sig state and invalidates it on every mapping edit / profile load / new source parse; `ExecutePanel` passes sig in body and disables Execute when null. 188 vitest cases / 14 suites (+6 new: 2 cell-length, 2 hs_path, 2 signature). tsc + lint clean

**Still open on `phases/phase-1-mvp.md`** (polish + robustness intentionally deferred):
- F3: write provisioned table IDs back into mapping profile
- F8: cancel button, per-batch cursor persistence, worker-runner separation, bounded slice + re-enqueue (Phase-2 runner rework in `docs/long-job-runner.md`)
- FK correctness: stale-ID re-resolve on write error
- SSE progress: `GET /api/jobs/[id]/stream` + UI subscriber

**Next up (unblocked):**
- Sandbox smoke test — one turn against `/import` with an oversized cell + a bad hs_path to see the 400 responses render in `ExecutePanel`
- Mid-size polish — stale-ID retry (re-resolve FK once on write error), cancel button (UI abort at batch boundary), cache resolved table IDs in mapping profile
- Phase 2 — `phases/phase-2.md`: mapping profile export/import JSON, resume from failure (Inngest), schema inference, XLSX + Google Sheets sources
- Deploy — `DEPLOYMENT.md`; manual Vercel step still pending

---

## Prior state — 2026-09-18

**Phase:** 1 — MVP **usable end-to-end**. Core happy path shipped: F1 (portal connection) → F2 (source ingestion) → F3 (introspection + provisioning) → F4 (column mapping) → F5 (foreign-relationship config) → F6 (dependency ordering) → F7 (dry run) → F8 (execute + publish) → F10 (results & logging + downloads) → F11 (mapping profiles + job history). All four `onMissing` FK policies (skip-row / null / fail / create-stub) wired and sandbox-verified via `spike/14`. Composite NK + multi-value FK working. Node 22 upgrade complete. Delete-table endpoint shipped. Both Phase-0 open questions closed (rate-limit stress: no 429s at 100 concurrent — reactive retry sufficient; long-job runner: `docs/long-job-runner.md` recommends Inngest for Phase 2). Deployment scaffolding ready (`vercel.json` + `DEPLOYMENT.md`).

**Still open on the Phase 1 checklist** (`phases/phase-1-mvp.md`) — polish + robustness items intentionally deferred:
- **F3:** write provisioned table IDs back into mapping profile (state_json holds targets by name; ids aren't cached)
- **F7:** gate execute on a completed dry run (currently offered, not required)
- **F8:** cancel button, per-batch cursor persistence, worker-runner separation, bounded slice + re-enqueue — all cluster under the Phase-2 runner rework
- **FK correctness:** stale-ID re-resolve on write error
- **Constraint enforcement:** hard cell-length caps on execute (10k text / 65k rich text), lowercase page-path check on execute
- **SSE progress:** `GET /api/jobs/[id]/stream` + UI subscriber

**Phase-1 addendum finding (F0-13):** HubDB's `POST /tables` uniqueness applies to **label**, not just `name`. Trying to create a new table with a label that matches any existing table returns 409 `TableValidationError.DUPLICATE_NAME_AND_LABEL`. Namespacing the label alongside the name is the safe pattern; provisioning UI should probably surface this if a user picks a label that collides.

**Blocked on:** nothing. Migration `supabase/migrations/20260918000000_mapping_profiles.sql` was applied by the user this session; F11 chunks 1 + 2 verified end-to-end against live Supabase + sandbox. F10 (results & logging polish + downloads) shipped earlier this session — `ExecutePanel` now surfaces a totals dashboard, error grouping by `kind × column`, an uncapped scrollable row-error list, and two download buttons (`row-errors.csv` and `import-log.json`). `RowError` gained an optional first-class `column` field populated at the three construction sites (`missing-natural-key`, `missing-target-key-map`, `unresolved-fk`). F8 (execute + optional publish) shipped 2026-09-17 — the full loop runs at `/import`: pick portal → upload CSV/JSON → map columns → configure FK relationships → see toposort order → dry run → execute + push-live. Underlying pieces: `lib/execution.ts::synthesizeExecution` (translates wizard state into `importRows`'s `{schema, tableIds, source}` triple, re-keying into "target space"), `POST /api/portals/[id]/execute` (calls `importRows` + optional `pushLive`), and `ExecutePanel` client component. Verified against the sandbox via `/execute` on fresh namespaced tables: 5 rows created, 0 errors, foreign-only publish worked.

**Next up (unblocked):**
- **Server-side hardening pass** ← _picked as tomorrow's starting point._ Three quick wins that share a pattern (validate at synth/plan time so `importRows` never issues a doomed batch):
  1. Cell-length caps on execute — reject text > 10k / rich-text > 65k chars at plan time (currently only warned in F2 validation)
  2. Lowercase page-path check on execute — currently validated in the UI (`validatePathColumn`) but not server-side
  3. Gate execute on completed dry run — PRD F7 requires it; wizard offers it today but doesn't enforce
- **Mid-size polish** — stale-ID retry (re-resolve FK once on write error), cancel button (UI abort at batch boundary), cache resolved table IDs in mapping profile
- **Phase 2** — `phases/phase-2.md`: mapping profile export/import JSON, resume from failure (Inngest), schema inference, XLSX + Google Sheets sources
- **Deploy** — `DEPLOYMENT.md` documents the manual steps; user pushed `c97b44c` today which triggers Vercel auto-deploy if the project is linked
- **Delete-table endpoint** — sandbox is accumulating scratch tables (`f3demo_*`, `exec_*`) from verification runs; no way to clean them up yet. Also useful for `/portals/[id]/schema`.
- **Job cursor persistence + rate-limit throttle** — the two F8 items still open (deferred; the current single-request execute path works fine at MVP scale).
- Rate-limit stress test (still-open Phase-0 question).
- Node 22 upgrade to retire the `ws` polyfill and silence the supabase-js deprecation warning.

**Scratch state left behind in the sandbox:** F3 verification left `f3demo_1789646817` (draft, 0 rows). F8 verification left `exec_1789651861_{brands,categories,products}` (some published). No delete endpoint yet; harmless.

### What exists
- Next.js 16 App Router scaffold (TypeScript, Tailwind v4, ESLint 9, pnpm)
- shadcn/ui initialized (base-nova / Base UI) — `Button` under `components/ui/`
- PRD (`hubdb-importer-prd.md`) and phase plans (`phases/phase-0…phase-3.md`)
- Phase-0 spike harness — `scripts/spike/*.ts`, runs via `pnpm spike <path>` with `.env.local` loading; all migrated onto `lib/hubdb/`
- Phase-1 foundations:
  - `lib/hubdb/` — typed HubDB wrapper: `createHubdbClient()` factory with 429/5xx retry + `Retry-After` handling; id normalization (all ids returned as strings); typed operations for tables (`list/get/create/patch/pushLive`) and rows (`listAll{Draft,Live}Rows`, `batch{Create,Update,Purge}DraftRows`); `/rows/draft/batch/*` path shape hard-enforced (F0-5), 100-row cap asserted (F0-6)
  - `lib/graph.ts` — directed dependency graph shared by provisioner/importer/publisher: `toposort()`, `toposortOrThrow()` (F6 "reject with clear message"), `breakCycles()` (F6 "offer two-phase write") with deferred-edge output for column-less create + PATCH-in strategy; converters from `HubdbTableInput` and `HubdbTable`; iterative Tarjan's SCC for cycle detection
  - `lib/schema.ts` — schema-file parser (zod) + cross-ref validator (surfaces all issues at once, not fail-first) + `resolveDefaults` (FK `foreignColumn` defaults to target's single-column `naturalKey`) + `diffSchema` returning per-table `create | match | update | conflict` (never emits drop or retype — F3)
  - `lib/hubdb/provision.ts` — topological two-phase provisioner. Takes an injectable `ProvisionOps` adapter (not the raw client) for testability; `opsFromClient(client)` builds the real one. Self-references included in the graph so `breakCycles` catches them. Phase 2 groups deferred edges by source and PATCHes the missing FK columns in per table. **Sends `portal.columns + new columns` on every PATCH** (existing ids preserved) — validated as REQUIRED by F0-11: HubDB PATCH is full-replace; sending only new columns silently destroys the schema
  - `lib/resolve.ts` — low-level FK resolver (F8): `normalizeKey` (trim + collapse ws + casefold, each toggleable), `buildKeyMap` (natural-key → row-id with duplicate detection; returns `{ok:false, duplicates, map}` on collision), `resolveForeignValue` (all-or-nothing; dedupes repeated values by default; empty → `[]` per PRD), `splitMultiValue` helper (`,` `|` `;` `\n`), `HUBDB_MAX_ROWS_PER_TABLE = 10_000` constant. onMissing policies (fail/skip/null/stub) live at the caller — resolver just reports missing
  - `lib/hubdb/import.ts` — F8 execution layer. `importRows(ops, {schema, tableIds, source})` runs in topological order (`toposortOrThrow` on the schema graph): per table, fetch existing draft rows → build keyMap on naturalKey (dupe + 10k-cap pre-flight via `ImportPreflightError`) → for each source row, resolve FK columns using previously-populated tables' key maps → split into insert/update by naturalKey lookup → batch update, then batch insert at 100/call. Newly-inserted row ids fold back into the key map so downstream tables resolve correctly. Injectable `ImportOps`; `opsFromClientForImport(client)` builds the real one. `onMissing='fail'` per-row (skip + log, other rows continue); other policies + stale-ID retry + cancel signal noted as follow-ups. Assumes `foreignColumn === target.naturalKey` (the `resolveDefaults` happy path)
  - `lib/crypto.ts` — AES-256-GCM `encrypt(plaintext) → base64` / `decrypt(base64) → plaintext`. Single output string encodes `iv || tag || ciphertext`. Reads `PORTAL_TOKEN_ENCRYPTION_KEY` (base64, 32 bytes) from env; throws with a clear message on missing / wrong length. `generateEncryptionKey()` helper for one-shot key generation. 10 vitest cases (roundtrip, unicode, IV freshness, bit-flip rejection via GCM auth tag, truncation, wrong-key rejection, missing-env, wrong-length-key)
  - `lib/db/supabase.ts` — `createSupabaseServerClient()` factory using `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`. Server-only (bypasses RLS); `auth: { persistSession: false, autoRefreshToken: false }` for request-scoped use. Untyped `SupabaseClient` for now — typed repos will layer on top in F1
  - `supabase/migrations/20260915000000_init.sql` — full DDL for the 6 Phase-1 tables (`portals`, `mappings`, `jobs`, `job_batches`, `job_errors`, `key_maps`) plus a shared `set_updated_at()` trigger. Applied against the cloud project on 2026-09-15. Paste-into-SQL-Editor workflow; needs the role selector on `postgres` (F1-scaffold finding). Filename follows the Supabase CLI's timestamp convention for later adoption
  - `.env.example` — SUPABASE_* + PORTAL_TOKEN_ENCRYPTION_KEY blocks with generation instructions; the old HUBSPOT_* vars are scoped as "spike scripts only" (app code reads tokens from the `portals` table via `getPortalToken`)
  - `lib/hubdb/introspection.ts` — `validateHubdbToken(token)` probes `/tables?limit=1` with retry disabled. Returns `{ok:true, scopes:["hubdb"]}` on 200; `{ok:false, error, status:401}` on 401 with a specific "invalid or missing hubdb scope" message; generic error with status + body preview for other 4xx/5xx. 3 vitest cases via fetch injection
  - `lib/db/portals.ts` — typed portal repo. `PortalRow` (server-only, includes ciphertext) vs `PortalSummary` (client-safe, no token). Functions: `createPortal(client, input)` (encrypts token via `lib/crypto`), `listPortals(client)`, `getPortalById(client, id)`, `getPortalToken(client, id)` (server-only decrypt), `deletePortal(client, id)`. All take `SupabaseClient` as first arg
  - `lib/source/csv.ts` — CSV parser. Byte input, BOM sniff (UTF-8 / UTF-16 LE/BE) with manual `encoding` override; papaparse for tokenization with auto delimiter detection (`,` `;` `\t` `|`) + manual override; header dedup (`col_2`, `col_3`) + empty-header filler (`column_N`) with warnings; `headerRow` override for preamble rows. Throws `CsvParseError` on empty input or header row past EOF
  - `lib/source/json.ts` — JSON parser. Accepts either `{ tableName: rows[] }` or `[{ table, rows }]`; scalars coerced to strings (booleans, numbers, bigints, null → ""), nested objects **or arrays** rejected with `{ kind: "nested-value", path: "table[i].col" }` per PRD ("cells are scalars or delimited strings"); backfills missing columns to keep row shapes uniform
  - `lib/source/validate.ts` — warning collector (`Warning` union with `kind + table + message + detail`). Fires: `row-count-cap` (> 10k via `HUBDB_MAX_ROWS_PER_TABLE`), `cell-too-long-text` (> 10k), `cell-too-long-richtext` (> 65k for columns in `richTextColumns`), `empty-required-column` (per requested col), `duplicate-natural-key` (single or composite key; uses `normalizeKey` so `Acme` and `acme` collide; empty key parts are skipped instead of colliding). natural-key + required-col checks stay quiet at F2 stage; they fire once F4 supplies the args
  - `lib/source/index.ts` — barrel + `SOURCE_PREVIEW_ROWS = 20` constant
  - `lib/hubdb/portal-schema.ts` — `fetchPortalSchema(client, now?)` for F3. Runs `listTables` then `getDraftTable` per table in parallel; overwrites `columns` with the draft view and captures per-table failures in `draftFetchError` (one 403 doesn't nuke the whole snapshot). Returns `{ tables, fetchedAt }`
  - `lib/execution.ts` — F8 synthesizer. `synthesizeExecution({sources, mappings, portalTables})` returns `{schema, tableIds, source, issues}` — translates the wizard state into the triple `importRows` expects, renaming source columns to target column names in both the schema and the row payloads. FK column translation: `foreignTable` becomes the sibling source name, `foreignColumn` is the sibling's target-column name for the picked matchKey. Returns issues (rather than throwing) for missing-target, missing/composite/unmapped natural key, and fk-target-column-missing. 6 vitest cases
  - `lib/dry-run.ts` — F7 pure. `computeDryRun({sources, mappings, portalTables, existingRowsByTarget})` returns `{order, tables, projectedApiCalls, ok}`. Per source table: existing draft-row count, source-row count, planned create/update/skipped (natural-key intersection with existing rows, `normalizeKey`-folded), unresolved-FK list with `{sourceTable, sourceColumn, foreignSource, matchKey, rowIndex, value}`, coercion warnings via `detectTypeMismatch`, API-call estimate (`ceil(existing/1000)` list-page GETs + `ceil(create/100)` POSTs + `ceil(update/100)` PATCHes). Reuses `deriveImportOrder` for the traversal order. No writes; caller fetches portal rows. 7 vitest cases
  - `lib/mapping.ts` — F4 + F5 + F6 pure logic. F4: `normalizeIdent` (identifier-fold: lowercase + strip separators, distinct from `normalizeKey` which is for cell values), `autoMap(sourceHeaders, targetColumns)` returning per-source `mapped/ignored/unmapped` with claim-once (a target can only be assigned once), `detectTypeMismatch(sample, targetType)` (NUMBER/CURRENCY require numeric, BOOLEAN accepts true/false/yes/no/1/0 case-insensitively, DATE requires `YYYY-MM-DD`, DATETIME requires `Date.parse`-able, TEXT/RICHTEXT/URL permissive), `validatePathColumn(rows, column)` for `hs_path` (lowercase + URL-safe chars + duplicate + empty). F5: `ForeignKeyConfig` shape + `initialForeignKeyConfig()` + `normalizeOptionsFor(matching)` (bridges the UI's `default/strict` to `lib/resolve`'s NormalizeOptions), and `countResolvable(mainRows, mainColumn, foreignRows, matchKey, cfg)` returning `{matched, unmatched, empty, totalValues, missingValues}` for the live-resolvability preview. F6: `MappingState` type (moved out of `mapping-editor.tsx` so lib code can consume it), `initialMappingState()`, and `deriveImportOrder(sourceNames, mappings)` → `{nodes, order, cycles, deferred, cycleBreakOrder, ok}` powered by `lib/graph.ts::toposort`+`breakCycles`. Self-refs and out-of-set FK targets are ignored. 26 vitest cases
  - `app/api/portals/route.ts` — `GET` returns `{ portals: PortalSummary[] }`; `POST` validates body with zod, calls `validateHubdbToken` (400 on failure), inserts, returns `201 { portal }`. `runtime = "nodejs"` (needs `node:crypto` and the `ws` polyfill). Unique-index violation on `(label, env)` is rewritten to a 409 with a friendly message
  - `app/api/sources/csv/route.ts` — F2 CSV endpoint. `runtime = "nodejs"`. Accepts `multipart/form-data`, one or more `file` fields (max 20 MB each); global `delimiter` / `encoding` / `headerRow` overrides via form fields. Table name derived from filename (extension stripped, non-word chars → `_`). Response per file: `{ name, filename, headers, preview (first 20), totalRows, detected, parseWarnings, validationWarnings }`. `CsvParseError` → 400 with file context
  - `app/api/sources/json/route.ts` — F2 JSON endpoint. `runtime = "nodejs"`. `{ payload: string }` body (zod-validated). Dispatches on `parseJson` outcome: `nested-value` → 400 with `path`; other error kinds → 400 with `kind`. Success → same table-summary shape as CSV endpoint plus top-level `warnings` (e.g. duplicate table names in the payload)
  - `app/portals/page.tsx` + `app/portals/portal-form.tsx` — server component lists portals directly via `listPortals`; client `PortalForm` posts to `/api/portals`, calls `router.refresh()` on success. Env badge is red for production, muted for sandbox. `runtime = "nodejs"` + `dynamic = "force-dynamic"` on the page
  - `app/import/page.tsx` + `app/import/source-uploader.tsx` — F2 page. Server component pre-fetches portals (target-portal picker) + renders empty-state prompt when none exist. Client `SourceUploader` toggles between CSV upload and JSON paste forms, posts to the two endpoints, renders a `TableCard` per parsed table with detected encoding/delimiter, parse + validation warnings, and a preview grid (first 20 rows)
  - `app/portals/[id]/schema/page.tsx` + `refresh-button.tsx` — F3 portal introspection page. Server component fetches the portal, decrypts the token, builds a HubDB client, and runs `fetchPortalSchema`. Renders per-table cards with published/draft badge, row count, and column table showing name/type/FK target ids. Client `RefreshButton` uses `useTransition` + `router.refresh()`
  - `app/portals/[id]/schema/schema-planner.tsx` — F3 diff/provision client component. Textarea + `.json` upload, per-table verdict cards (create/update/match/conflict), Provision button, expandable result panel with toposort order + new table ids + event log
  - `app/api/portals/[id]/schema/route.ts` — F4 dependency: `GET` returns `{ portal, fetchedAt, tables }` for client consumption (MappingEditor calls this when a portal is picked)
  - `app/api/portals/[id]/diff/route.ts` + `provision/route.ts` — F3 write side (diff and provision endpoints, `SchemaValidationError` → 400 with issue list, `ProvisionConflictError` → 409)
  - `app/api/portals/[id]/execute/route.ts` — F8 endpoint. Body `{sources, mappings, publish}`. Fetches portal snapshot, calls `synthesizeExecution` (400 if issues), runs `importRows` (409 on `ImportPreflightError`), optionally `pushLive`s in dep order per the publish mode (`none` / `foreign-only` / `all`). Returns `{result, events, published}`
  - `app/import/execute-panel.tsx` — F8 client component. Publish selector + Execute button, per-table result cards (created/updated/skipped/errors with a collapsible errors list), publish result list, collapsible event log
  - `app/api/portals/[id]/dry-run/route.ts` — F7 endpoint. Body: `{sources, mappings}`. Fetches existing draft rows via `listAllDraftRows` for every target a mapping references (parallel), calls `computeDryRun`, echoes back the report plus any per-table read errors
  - `app/import/dry-run-panel.tsx` — F7 client component. "Run dry run" button; per-table cards with source/existing/create/update metrics + coercion warnings + collapsible unresolved-FK list (first 100 inline, with a "…and N more" line); "Download CSV" button assembles a blob of unresolved refs client-side
  - `app/import/import-order-panel.tsx` — F6 client component. Numbered vertical list of the toposort order (or the cycle-break order when cycles exist), with each row showing its dependencies + any deferred edges. Header shows a green "no cycles" or yellow "two-phase write" badge
  - `app/import/mapping-editor.tsx` — F4 + F5 client component. F4 pieces as above. F5: `ForeignKeyPanel` inserted per FK column with (source picker, match-key picker, multi + delimiter, onMissing, matching mode) and a live "matched / unmatched / empty" resolvability preview computed via `countResolvable`. State is a `foreignKeys: Record<sourceCol, ForeignKeyConfig>` map on `MappingState`; the editor keeps it in sync on target change and per-row assignment change (drops orphaned configs, seeds defaults when a FOREIGN_ID target gets a new mapping)
  - `app/portals/page.tsx` — now also renders a "schema →" link per portal row that navigates to the introspection page
  - `app/layout.tsx` + `app/globals.css` — font bug fix: `--font-sans` in `@theme` was `var(--font-sans)` (self-reference → undefined → serif fallback), now correctly points at `--font-geist-sans`. Metadata title updated from "Create Next App" → "HubDB Importer"
  - `app/page.tsx` — home page linking to `/portals` and `/import` (both ready) and disabled placeholders for Runs / Results
  - `lib/db/supabase.ts` — Node 20 polyfill for the WebSocket global: `import { WebSocket } from "ws"` set as `globalThis.WebSocket` when missing. Required by Supabase-js 2.116+'s RealtimeClient constructor. Retire when we upgrade to Node 22
  - `ws` (+ `@types/ws`) added as dependencies for the polyfill above
  - `vitest` — 5.0.1; suites colocated with source (`lib/graph.test.ts`, `lib/schema.test.ts`, `lib/resolve.test.ts`, `lib/crypto.test.ts`, `lib/mapping.test.ts`, `lib/dry-run.test.ts`, `lib/execution.test.ts`, `lib/hubdb/introspection.test.ts`, `lib/hubdb/provision.test.ts`, `lib/hubdb/portal-schema.test.ts`, `lib/hubdb/import.test.ts`, `lib/source/csv.test.ts`, `lib/source/json.test.ts`, `lib/source/validate.test.ts`). `pnpm test` / `pnpm test:run`. **169 cases across 14 suites, all green**. tsc clean
- `papaparse` (+ `@types/papaparse`) — CSV parser used by `lib/source/csv.ts`. Small, delimiter-detection built in
- Git: `main` tracking `origin/main` at https://github.com/junelapera/hubspot-importer

### In flight — Phase 1 foundations
| # | Task | Status |
|---|---|---|
| 1 | `lib/hubdb/client.ts` — factory, retry/backoff, `HubdbError` with rate-limit surface | done |
| 2 | `lib/hubdb/types.ts` — normalized shapes (string ids), `isPublished()` treating epoch as unpublished (F0-9) | done |
| 3 | `lib/hubdb/tables.ts` — list/get/create/patch/pushLive | done |
| 4 | `lib/hubdb/rows.ts` — draft/live pagination + `batch/{create,update,purge}` under `/rows/draft/` (F0-5), 100-row cap asserted | done |
| 5 | Migrate spike scripts onto `lib/hubdb/` (regression check) | done — 00-ping re-passes against sandbox |
| 6 | `lib/graph.ts` — toposort + cycle detection + two-phase decomposition | done — 25 hand-verified assertions |
| 7 | Test runner — Vitest installed; suites colocated with source | done — `pnpm test:run`, 40 cases green |
| 8 | `lib/schema.ts` — parse + validate schema file, diff vs portal | done — 21 cases (parse failure modes + every diff verdict incl. FK conflicts) |
| 9 | `lib/hubdb/provision.ts` — compose wrapper + graph into topological provisioner (name-based FK happy path from F0-2) | done — self-loop + 2-cycle break paths tested via fake ops |
| 10 | Validate PATCH-as-full-replace assumption in `provision.ts` against the sandbox | done — F0-10/11/12; `patchTable` path bug fixed, `getDraftTable` added, end-to-end integration spike passes |
| 11 | `lib/resolve.ts` — key map builder + FK resolver (F8) | done — 24 cases (normalize + split + build map w/ duplicates + resolve all-or-nothing + dedupe + multi-value) |
| 12 | `lib/hubdb/import.ts` — F8 execution (pass-1 upsert foreign, pass-2 main w/ resolved FKs, batching, retry) | done — 11 cases; ImportPreflightError for dupes + 10k cap; RowError for per-row failures |
| 13 | End-to-end sandbox spike for the importer (`provision → importRows → push-live`) | done — `scripts/spike/13-import-end-to-end.ts`; 3 tables provisioned, 30 rows imported, FK resolved (product.brand[0].id === brand-1.id), pushed live, live row ids stable, re-import fully idempotent, cleanup deleted the namespaced tables |
| 14 | Supabase project + 6 phase-1 tables applied | done — verified via `12-supabase-ping.ts`, all 6 tables reachable |
| 15 | `lib/crypto.ts` — AES-256-GCM encryption for portal tokens | done — 10 cases |
| 16 | `lib/db/supabase.ts` — server-side client factory | done — includes Node 20 `ws` polyfill |
| 17 | F1 portal-connection API + typed portal repo + picker UI | done — validated end-to-end against real Supabase + real HubSpot (POST 201, GET 200, dupe → 409, bad token → 400) |
| 18 | F2 — source ingestion (CSV/JSON upload + parse + preview) | done — validated end-to-end via curl (2 CSVs w/ auto delimiter, JSON happy + nested-object 400 w/ path, 10001-char cell → cell-too-long-text) |
| 19 | F3 — portal introspection UI (schemas, published/draft state, row counts) | done — read side + diff/provision write side both validated against the real sandbox (match / update / create / conflict all correct, then a new `f3demo_*` table provisioned to id `412491169` and re-diff flipped to match) |
| 23 | F4 — column mapping UI (auto-match, type mismatch, ignore, natural key, hs_name/hs_path) | done — lib fully unit-tested; `MappingEditor` wired under each parsed source table on `/import`; new `GET /api/portals/[id]/schema` endpoint verified against sandbox |
| 24 | F5 — foreign relationship config (source table + match key + multi + delimiter + onMissing + matching) | done — `ForeignKeyPanel` per FOREIGN_ID mapping; `countResolvable` powers a live matched/unmatched/empty preview; 5 vitest cases on the helper |
| 25 | F6 — dependency ordering (import order UI) | done — `deriveImportOrder` derives a graph from the mapping FK configs and reuses `lib/graph.ts::toposort`/`breakCycles`; `ImportOrderPanel` renders the numbered plan + deferred edges when cycles are broken; 5 vitest cases |
| 26 | F7 — dry run (create/update counts, unresolved FKs, coercion warnings, API call estimate, CSV download) | done — `lib/dry-run.ts::computeDryRun` (pure) + `POST /api/portals/[id]/dry-run` fetches existing draft rows + `DryRunPanel` on /import. 7 vitest cases. Sandbox verified: brands+products source, 1 unresolved FK caught, 4 projected API calls (matches ceil arithmetic) |
| 27 | F8 — UI wiring (execute + optional publish) | done — `lib/execution.ts::synthesizeExecution` (6 vitest cases) turns mapping state into an `importRows` triple; `POST /api/portals/[id]/execute` runs it + optional pushLive; `ExecutePanel` on /import. Sandbox verified: provisioned 3 fresh namespaced tables, executed 5 rows (2+1+2) with FK resolution, publish=foreign-only pushed the two foreign tables live and left products draft. Composite naturalKeys + multi-value FK translation are not supported in the execute path yet |
| 29 | F10 — results & logging (per-table stats, row-error log, CSV + JSON downloads) | done (minus job history) — `ExecutePanel` now renders totals dashboard, error grouping by `kind × column`, uncapped scrollable row-error list, and two download buttons (`row-errors.csv` and `import-log.json`). `RowError` gained optional first-class `column` field populated at the three construction sites (`missing-natural-key`, `missing-target-key-map`, `unresolved-fk`). Job-history list bullet deferred to F11 (needs `jobs` persistence) |
| 30 | F11 chunk 1 — mapping profiles (persist wizard state, load into fresh session) | done — migration applied; verified end-to-end via curl (GET empty, POST 201, duplicate → 409, PUT rename → 200, DELETE → cascade wipes profile + jobs). `supabase/migrations/20260918000000_mapping_profiles.sql` adds `state_json JSONB NOT NULL` to `mappings`, drops `NOT NULL` on `schema_json`, plus `job_errors.column_name` from chunk 2; `lib/db/mappings.ts` typed repo (`create/list/get/update/delete` + `DuplicateMappingNameError` on 23505); `POST/GET /api/portals/[id]/mappings` + `GET/PUT/DELETE /api/mappings/[id]`; `ProfilePanel` on `/import` with load/save/delete + mismatched-source warning |
| 31 | F11 chunk 2 — job history (persist runs, per-table totals, row-error log) | done — migration applied; verified end-to-end: executed 2 brand rows against the sandbox, `jobId` returned, `persistenceError: null`, `/jobs` list renders, `/jobs/[id]` detail renders totals + row-error section. `lib/db/jobs.ts` (`createJob / completeJob / insertJobErrors / listRecentJobs / getJobById / listJobErrors` with a join through `mappings` + `portals`); best-effort persistence in `POST /api/portals/[id]/execute` (createJob → importRows → insertJobErrors + completeJob; `persistenceError` echoed if any step fails); auto-create a `run-<timestamp>` mapping when no `profileId` is provided so the FK constraint holds; `ExecutePanel` sends `profileId` and renders a jobId link + persistenceError banner; `app/jobs/page.tsx` + `app/jobs/[id]/page.tsx` render the history + detail. Home-page nav gets a "Jobs" tile |
| 32 | F11-1 fix — `jobs.mapping_id` cascade → SET NULL | done — `supabase/migrations/20260918010000_jobs_mapping_id_set_null.sql` applied; verified end-to-end (post-delete job at HTTP 200, both list and detail pages render "(profile deleted)" / "provenance link lost"). Repo types + UI updated to treat `mappingId / mappingName / portalId / portalLabel / portalEnv` as nullable. Auto-save on execute is now safe: deleting one of those `run-<timestamp>` mappings loses only the provenance link, not the history |
| 33 | Composite naturalKey + multi-value FK in the execute path | done — `lib/resolve.ts` gains `composeCompositeKey` + `buildKeyMap(rows, string \| string[])`; `lib/hubdb/import.ts` accepts `ImportInput.fkOptions?: Record<table, Record<col, {multi, delimiter}>>` and threads it through `planRow` to split multi-value cells before resolving; `lib/execution.ts` drops the `composite-natural-key` guard, translates the array of source NK cols to target NK cols, and emits `fkOptions` from `mapping.foreignKeys[col].multi/delimiter`; execute route wires `synth.fkOptions` into the importer. 175 vitest cases green (6 new: 3 for composite `buildKeyMap`, 1 for composite-key disambiguation, 1 for import composite upsert, 1 for import multi-value FK split); `FkColumnOption` exported from the `lib/hubdb` barrel. Not yet smoke-tested against the sandbox — composite/multi source data isn't naturally shaped in the current sandbox, so relying on unit coverage |
| 34 | `onMissing` FK policies — wire `null` + reject `create-stub` at synth time | done — `FkColumnOption` gains optional `onMissing?: FkOnMissing`; `planRow` respects `null` (drops the FK column from the row, row still inserts / updates) and falls back to `skip-row` for everything else. `synthesizeExecution` emits `onMissing` in `fkOptions` when the user picks anything other than `skip-row`, and rejects `create-stub` at synth time as `unsupported-on-missing`. 179 vitest cases green (4 new: import null-drop, import skip-row explicit, synth onMissing emit + default omit, synth create-stub rejection). `FkOnMissing` type exported from `lib/hubdb` barrel |
| 36 | `onMissing='create-stub'` — mid-flight foreign-row insert | done — `planRow` gains a `pending-stub` PlannedRow variant + a `stubBucket` param (foreignTable → foreignColumn → normalizedKey → rawToken). On unresolved-FK with create-stub policy, records missing tokens into the bucket and returns pending instead of erroring. `importOneTable` drains the bucket in a two-pass loop: (1) plan all rows collecting stubs, (2) batch-insert each foreign table's stubs, splice new ids into `keyMapsByTable`, re-plan pending rows in "final" mode (`stubBucket=null`). Stubs dedupe by normalized key so `"BRAND-NOPE"` + `"brand-nope"` produce one row. `synthesizeExecution` no longer rejects create-stub — emits it in `fkOptions` like `null`. Sandbox-verified via extended `spike/14`: `post-d` with cell `"featured,unlaunched"` produced stub tags row (id `222194867969`) and post-d's tags cell had 2 refs. 182 vitest cases green (2 new: single-value stub with dedupe, multi-value stub inserting 2 tokens for a 3-token cell) |
| 35 | `onMissing='fail'` — real abort-the-run semantics | done — `ImportFailFastError` (exported from `lib/hubdb` barrel) carries the failing row + a full partial `ImportResult` (all prior tables + the fail-fast table, with subsequent tables listed as 0/0/skipped=source.length/0 errors). `planRow` returns a `"fail-fast"` kind on `unresolved-fk` when the policy is `"fail"`; `importOneTable` throws an internal `_FailFastSignal` after marking the failing row + all subsequent rows as skipped in the table's totals; `importRows` catches the signal, appends the aborted table + untouched-tables placeholders, and rethrows `ImportFailFastError`. `initialForeignKeyConfig()` now defaults `onMissing` to `"skip-row"` (matches actual executor behavior; existing "fail" profiles get real abort semantics). Execute route catches `ImportFailFastError` → **HTTP 422** with `{row, result, events, jobId, persistenceError}`; `ExecutePanel` gets a `fail-fast` stage that renders a red banner with the failing row's table/index/column + the full `ResultView` beneath it. 180 vitest cases green (1 new: fail-fast aborts mid-batch with correct partial totals and no batch-create for the aborted table) |
| 28 | Sandbox cleanup — delete-table endpoint | done — `deleteTable(client, ref)` in `lib/hubdb/tables.ts`; `DELETE /api/portals/[id]/tables/[tableId]` route; `DropTableButton` client component on `/portals/[id]/schema` with two-tier confirm (sandbox = `window.confirm`, production = `window.prompt` requiring the user to type the table name). Verified by cleaning up all 4 scratch tables (`f3demo_1789646817` + `exec_1789651861_{brands,categories,products}`) via curl — HTTP 200 across the board; sandbox now down to the 4 real Phase-0 tables |
| 20 | Node 20 → 22 upgrade (retire the `ws` polyfill) | done — `package.json`: removed `ws` + `@types/ws`, bumped `@types/node` to `^22`, added `engines.node: ">=22"`; new `.nvmrc` pins `22`; `lib/db/supabase.ts` drops the polyfill (relies on Node 22+'s native `WebSocket` global); `CLAUDE.md` scrubbed of Node-20 mentions. Verified by running `pnpm spike scripts/spike/12-supabase-ping.ts` on Node 24: all 6 tables reachable, no `"Node.js detected but native WebSocket not found"` line, no deprecation warnings. 175 vitest cases still green, tsc clean, lint clean. Vercel dashboard Node version is the only remaining bump — manual, tracked in "Next up" |
| 21 | Rate-limit stress test (still-open Phase-0 question) | done — `scripts/spike/15-rate-limit.ts` bursts 5/20/50/100 concurrent `/tables?limit=1` reads with retries disabled; **zero 429s at every burst size** against the sandbox. p95 stays under 1.2s at 100 concurrent. Wrapper's existing reactive retry (5 attempts, Retry-After honored) is sufficient for MVP scale. No proactive token bucket needed until we push beyond 100 concurrent reads per portal or start seeing high `attempts` counts in HubdbError logs |
| 22 | Long-job runner strategy (still-open Phase-0 question) | done — `docs/long-job-runner.md`. Documented the Vercel `maxDuration` ceiling per plan (Hobby 10s, Pro 300s, Enterprise 900s), the ~4–6-minute estimate at HubDB's 10k-row cap × 3 tables, and the four candidate rework paths (Inngest, Supabase queues, split-into-steps, dedicated worker). Recommendation: ship MVP with the synchronous handler + `vercel.json` `maxDuration: 300`, adopt Inngest for Phase 2 when we ship "resume from failure" — the existing `jobs` / `job_batches` / `key_maps` schema already fits Inngest's step-function model |
| 37 | Deployment prep | done — `vercel.json` at repo root sets `maxDuration: 300` on `/api/portals/[id]/execute`. `DEPLOYMENT.md` covers first-time setup (Supabase project + migrations in order + env vars + Vercel Node 22 dashboard step) and an every-deploy checklist. Deploy-time caveats spelled out: `maxDuration: 300` is a Pro-plan feature; Node 22+ required for the native `WebSocket` global |

### Archived — Phase 0 tasks
| # | Task | Status |
|---|---|---|
| 1 | Scaffold spike harness (env, tsx, client) | done |
| 2 | Confirm API path (`v3` vs dated `2026-03`) | done — both functionally equivalent at `/tables`; defaulting to `v3` |
| 3 | Provision `brands` + `categories` foreign tables | done — `brands` id `412407343`, `categories` id `412407344` (both draft) |
| 4 | Reproduce `Foreign table id must be defined` error | done — actual message: *"Foreign table id **or foreign table name** must be defined"* |
| 5 | Provision `products` main table w/ two FK cols | done — id `412407345` (draft); name-based FK accepted, server-resolved to ids |
| 6 | Batch-insert 200 rows into foreign tables, capture IDs | done — 200 rows in each of `brands` + `categories`, two 100-row batches per table, slug→id maps built |
| 7 | Batch-insert 200 linked rows into `products` | done — 200 rows, FK cells stored verbatim; UI displays blank until foreign tables published |
| 8 | Push-live all three tables in dependency order | done — all three live, 200 rows each, row ids stable draft→live |
| 9 | Document findings in `phases/phase-0-spike.md` | done — 9 findings + 6 locked-in decisions + open questions rolled into Phase 1 |

HubL render (last item in phase-0 checklist) deferred — do manually in HubSpot page editor after API chain is proven.

### Open questions still un-answered (PRD §13)
1. Single main table per mapping, or multiple?
2. Rows in HubDB but absent from source — leave / flag / delete?
3. Prod-write gating (second confirmation or approval)?
4. Verification read-back (auto-generated HubL join snippet)?
5. Schema file authoring: hand-committed vs UI-built?
6. Schema file + import mapping — one file or two?

### Decisions locked in during scaffold
- **Package manager:** pnpm (pinned in `package.json`)
- **Framework version:** Next.js **16** (PRD said 15; `create-next-app` shipped 16 — App Router surface compatible)
- **UI kit:** shadcn/ui on Base UI (`base-nova` preset)
- **Repo layout:** no `src/` — `app/` `components/` `lib/` `workers/` at root, matches PRD §9
- **Spike code shape:** standalone `scripts/spike/` CLI (tsx), not API routes
- **Test runner:** Vitest 5, colocated `*.test.ts` next to source (not a `tests/` dir). Peer warning: vitest wants `@types/node ^22 || >=24` but Next.js pins `^20` — warning only, suite runs

---

## Log

### 2026-09-18
- **HTTP Basic Auth gate shipped for Vercel Hobby deploy.** New `middleware.ts` at repo root runs on Edge, gates all pages + API routes behind `BASIC_AUTH_USER` + `BASIC_AUTH_PASSWORD` when both env vars are set. Unset (dev default) → falls through, no auth. Partial config → fail closed (every request 401) so a half-configured deploy doesn't accidentally serve open. Uses `atob` for base64 decode and a manual constant-time string compare to avoid the tiny timing-attack surface of `===`. Matcher excludes `_next/static`, `_next/image`, and `favicon.ico` so image requests don't re-prompt for creds. `.env.example` gains the two vars; `DEPLOYMENT.md` gains a Basic Auth section + the env-var table row. `WWW-Authenticate: Basic realm="HubDB Importer"` returned on 401 so browsers prompt for creds
- **Design decision — fail closed on partial config.** Alternative: fail open when either env is missing (permissive). Went with fail-closed because deploy-time env-var configuration is where the mistake would happen; a permissive fallback silently serves a "gated" app that isn't. Better UX to see 401 everywhere and check the dashboard than to think you're protected when you aren't
- **Design decision — shared credential, not per-user auth.** MVP is single-user (the deploying user). NextAuth / Clerk / Supabase Auth are Phase-3 territory when we have multiple humans on the same portal. Basic Auth over HTTPS is unfashionable but adequate for gating a personal deploy
- **Sidebar navigation shipped.** User asked for a sidebar before starting the Phase-1 polish sweep. New `app/nav.tsx` client component exports:
  - `Sidebar` — fixed left, 224px wide, visible `md:` and up. Sticky full-height, muted background, border-r. Header row shows "Phase 1 · MVP" tag + "HubDB Importer". Nav items with lucide icons: Home / Portals / Import / Jobs. Footer has the GitHub link
  - `MobileNav` — horizontal top bar shown only under `md`. Same items, tighter styling
  - Both use `usePathname()` for active-route highlight. Exact-match for Home, prefix-match for the rest (so `/portals/[id]/schema` still highlights "Portals" and `/jobs/[id]` highlights "Jobs"). `aria-current="page"` on the active item for a11y
- **`app/layout.tsx`** — body becomes `flex`; renders `<Sidebar />` + a column with `<MobileNav />` on top and `{children}` below. Existing pages keep their own `<main>` + centered `max-w-*` container and just center inside the remaining viewport
- **Design decision — minimal custom sidebar, not shadcn's `<Sidebar />` primitive.** Shadcn's version brings a `SidebarProvider`, cookies for persistent open/closed state, a trigger button, and a bunch of extra components. Overkill for MVP; the custom sidebar is ~90 lines of Tailwind + Link. If we later want collapsible + a keyboard shortcut, we can revisit
- **Design decision — kept the home-page tiles.** Sidebar handles cross-page nav; the `/` tiles serve an onboarding purpose ("which do I open first?"). Non-redundant enough to leave alone. Home stays a simple hero + 3 quick-start tiles + footer
- **Dependency note** — `lucide-react` was already in package.json (^1.45.0, i.e. the pre-`@lucide/react` name; still functional, exports 6287 icons per a smoke test)
- 182 vitest cases still green, tsc clean, lint clean (no code changes to lib — UI only)
- **Verified** — hit all 4 routes via curl (`/`, `/portals`, `/import`, `/jobs`) → 200 each; grepped the home HTML to confirm sidebar labels render

- **Phase 1 MVP feature-complete.** Closed the last three "next up" items in one pass: rate-limit stress test, long-job runner strategy, deployment prep. 4 tracked tasks (`TaskCreate` #40–#43)
- **Rate-limit stress test** — `scripts/spike/15-rate-limit.ts`. Bursts 5 / 20 / 50 / 100 concurrent `/tables?limit=1` reads with retries disabled (via `createHubdbClient({retry: {maxAttempts: 1}})` so 429s surface instead of being masked by the wrapper). Result: **zero 429s at every burst size** against the sandbox. p50 stays around 750ms, p95 under 1.2s at 100 concurrent. Full run: 4 bursts + 3× 15s cooldown = ~50s wall-clock
- **Rate-limit recommendation** — no proactive token bucket needed for MVP. The wrapper's reactive retry (5 attempts, `Retry-After` honored, exponential backoff with jitter) is sufficient. Revisit if: users complain about slow imports (visible as high `attempts` counts in `HubdbError` logs), or we push beyond 100 concurrent requests per portal. Also possible the sandbox has more permissive quotas than production — worth re-running the spike from a production portal before Phase 2
- **Long-job runner strategy** — `docs/long-job-runner.md`. New docs directory (first entry sets convention). Enumerates Vercel serverless `maxDuration` caps (Hobby 10s / Pro 300s / Enterprise 900s), the estimated ~4–6-minute worst case at HubDB's 10k-row cap × 3 tables with cross-FK resolution, and four candidate rework paths (Inngest, Supabase queues+edge-functions, split-into-steps client-driven, dedicated worker on Fly/etc). Recommendation: ship MVP with synchronous handler + `maxDuration: 300`; adopt Inngest for Phase 2 when we ship "resume from failure" (the existing `jobs` / `job_batches` / `key_maps` schema already fits Inngest's step-function model)
- **Design decision — don't build the runner rework in MVP.** Options weighed: Inngest is cheapest (durable execution as a service, generous free tier, integrates natively with Next.js on Vercel), Supabase queues would keep us single-vendor but requires edge functions running the HubSpot HTTP client which is real work, split-into-steps violates the PRD's "closing the tab must not kill the import" requirement, dedicated worker is Phase-3 ops overhead. Called out in the doc so the next person picking this up doesn't re-litigate
- **Deployment prep** — three artifacts:
  - `vercel.json` at repo root, minimal: `{functions: {"app/api/portals/[id]/execute/route.ts": {maxDuration: 300}}}`. Only the execute route needs the ceiling raise — other API routes fit in the default 10s comfortably
  - `DEPLOYMENT.md` covering first-time setup (create Supabase project, apply migrations in filename order — `20260915000000_init.sql` → `20260918000000_mapping_profiles.sql` → `20260918010000_jobs_mapping_id_set_null.sql`, generate encryption key, set 3 env vars in Vercel, dashboard-pin Node to 22.x) and an every-deploy checklist (apply new migrations, scan `.env.example` for new vars, run preview tests)
  - CLAUDE.md gains spike 15 entry in the commands list
- **Design decision — dashboard-pin Node version, not vercel.json.** Vercel doesn't support Node-version pinning via `vercel.json` (that's dashboard-only). Documented in `DEPLOYMENT.md` and the `.nvmrc` + `engines.node` fields in the repo are what git-managed callers see
- **Not in scope for this pass:** actual production deployment. `DEPLOYMENT.md` describes what the user needs to do; the click-through is left to when they're ready to ship
- 182 vitest cases still green, tsc + lint clean (no code changes this pass, only docs + a spike + config)
- **What's next after MVP:** Phase 2 items (`phases/phase-2.md`). Highest-value: F11 export/import mapping JSON, resume from failure (Inngest rework), schema inference from CSV headers, XLSX + Google Sheets sources

- **`onMissing='create-stub'` shipped.** Closes the FK policy matrix. All four policies now wired end-to-end: `skip-row` (default), `null`, `fail`, `create-stub`. 5 tracked tasks (`TaskCreate` #35–#39)
- **`lib/hubdb/import.ts`** — three-part change:
  1. New `PlannedRow` variant `"pending-stub"` (carries just `sourceIndex`; values are re-built on the second-pass replan since populating them partially would leak stale FK cells across passes)
  2. Private `StubBucket = Map<foreignTable, Map<foreignColumn, Map<normalizedKey, rawToken>>>`. `planRow` gains an optional `stubBucket` param — when non-null and create-stub policy fires, records the missing tokens into the bucket via `recordStub` (dedupes by normalized key, keeps first-seen raw form) and returns pending-stub. When null (second pass or non-stub policies), original behavior
  3. `importOneTable` refactored to a two-pass loop: (a) plan all rows collecting stubs, (b) if bucket non-empty, for each foreign table look up its `tableIdsMap` ref and batch-insert stubs 100 at a time, splice new ids into `keyMapsByTable[foreignTable]`, then re-plan pending rows via a second `planRow` call with `stubBucket=null`. Threads `tableIdsMap: Readonly<Record<string,string>>` from `importRows` through as a new parameter
- **`lib/execution.ts`** — dropped the `unsupported-on-missing` synth-time rejection and the corresponding issue variant. `create-stub` now flows through `fkOptions` like `null` — the executor handles it
- **Design decision — dedupe stubs by normalized key.** `"BRAND-NOPE"` in one row and `"brand-nope"` in another would create two stubs otherwise, and each source row's FK cell would resolve to a different id (whichever stub matched its lookup). Dedupe means both rows point at the same stub. Which raw form we insert doesn't matter downstream — the naturalKey column stores the raw and future lookups normalize before matching. Recorded first-seen wins in the bucket
- **Design decision — pending-stub carries just sourceIndex, not partial values.** First draft threaded partial `values` through pending-stub to skip re-computing non-FK columns on replan. Backed out: (a) planRow needs to re-check ALL FK columns on replan since another column might depend on a stub we just created, (b) the perf saved (one loop over table.columns per pending row) is trivial vs. the two HTTP round-trips required to insert stubs, (c) keeping the type minimal means fewer invariants to maintain across the two passes
- **Design decision — stubs go through the same `ops.batchCreateDraftRows` as regular inserts.** Alternatives: separate stub-only op or reusing existing insert plan machinery. Went with the same batch-create path because HubDB has no distinct stub concept — stubs are just rows with only the naturalKey column populated. Same 100-row chunk cap enforced via the shared `chunk()` helper
- **Design decision — all-or-nothing at the cell level (unchanged).** A multi-value cell like `"featured, new, sale"` with only `"featured"` resolved: under create-stub, stubs are created for `"new"` and `"sale"`, then the whole cell resolves to 3 refs. The existing `resolveForeignValue` doesn't need a partial-resolution mode; the two-pass structure gets us the same outcome for free
- **Tests** — 182 across 14 suites (was 180):
  - Import: single-value create-stub with dedupe (`"brand-nope"` + `"BRAND-NOPE"` → 1 stub, P-2 and P-3 point at same id)
  - Import: multi-value create-stub (`"featured, new, sale"` with only "featured" existing → 2 stubs inserted, cell has 3 refs)
  - Execution: create-stub emits `{onMissing: "create-stub"}` in fkOptions (flipped from the old "rejects at synth" test)
- **Sandbox verification via extended `spike/14`** — added a step 6 that imports `post-d` with cell `"featured,unlaunched"` under create-stub. Result: `tags` created=0 updated=2 (already synced), `posts` created=1, stub tags row appeared with id `222194867969` and slug `"unlaunched"`, post-d/v1's tags cell has 2 refs including the stub. Full pipeline (compositeNK + multi + null + fail + create-stub) runs in ~7s end-to-end
- 182 cases across 14 suites, all green. tsc clean, lint clean
- **FK policy matrix now complete.** All four PRD-specified policies (`skip-row`, `null`, `fail`, `create-stub`) work end-to-end with unit + sandbox coverage. The `MappingEditor` UI already exposes all four via the `onMissing` dropdown; the wizard state → synth → executor path is fully wired
- **Next step:** rate-limit stress test (Phase-0 open), long-job runner strategy (Phase-0 open), or the Vercel deployment step (manual)

- **Sandbox smoke test shipped for composite NK + multi-value FK + onMissing.** New spike `scripts/spike/14-composite-multi-onmissing.ts` runs end-to-end against the Dev sandbox (~5s per run). Closes the "not sandbox-verified" caveats from the composite/multi and onMissing shipments. Also serves as regression coverage for the whole lib chain going forward
- **What it does:**
  1. Provisions two namespaced tables — `smoke_<epoch>_tags` (NK: `slug`) + `smoke_<epoch>_posts` (composite NK: `[slug, variant]`, multi-value FK `tags → tags.slug`)
  2. Imports 2 tags + 3 posts with `fkOptions.posts.tags = {multi: true, delimiter: ",", onMissing: "null"}`. One post has a mixed-hit cell (`"featured,nonexistent"`) → cell dropped under `null` policy
  3. Reads back all 3 posts, verifies composite NK identity (found by `(slug, variant)`), verifies `post-a/v1` has 2 tag refs, `post-a/v2` has 1, and `post-b/v1` has empty/absent tags (the null-drop)
  4. Re-imports the same 3 posts → asserts `created: 0, updated: 3` (composite upsert works)
  5. Re-imports with `onMissing: "fail"` and an extra orphan post → expects `ImportFailFastError`; asserts the failing row is `post-b/v1` (sourceIndex 2), the partial totals show tags updated (already synced) + posts skipped=2 with 1 error
  6. Cleanup: delete both tables
- **What we learned:**
  - HubDB returns an empty/absent `tags` cell for the `null`-dropped row (i.e., the executor omitting the column from the insert body produced an empty cell server-side, exactly as intended)
  - Composite NK upsert works end-to-end. The key map stored in `existingKeyMap` uses `composeCompositeKey(row, ["slug","variant"], DEFAULT_NORMALIZE)` and the fold-back after insert uses the same, so idempotency held on real HubDB row ids
  - Multi-value FK splitting works: `"featured,new"` → 2 refs, `"featured"` (no delimiter) → 1 ref, `"featured,nonexistent"` → cell dropped (null policy)
  - Fail-fast correctly applies to ALL rows, not just newly-added ones — on the second run under `fail`, the previously-tolerated orphan post-b/v1 aborts before the newly-appended post-c is even reached. This is the correct semantic — a first-draft assertion tried to verify "post-c triggers fail-fast" and had to be corrected; the spike now has a comment explaining why post-b/v1 is the expected trigger
- **First-run bug (mine, not the code):** my initial assertion in step 5 expected `sourceIndex=3` (post-c). The spike failed with `sourceIndex=2 column=tags` — the correct answer. The lib was right; my mental model of "fail" was wrong. Comment added to the spike so a future reader doesn't repeat the same mistake. First run also left `smoke_1789734825824_*` in the sandbox — cleaned up via the F28 delete-table API
- **Sandbox verification receipts** (namespace `smoke_1789734867668_*`):
  - Provision: 2 tables in dep order (tags → posts); tableIds `412514xxx` / `412514xxx`
  - Import 1 (null policy): tags created=2 / posts created=3 / errors=0; post-a/v1 has 2 refs, post-a/v2 has 1, post-b/v1 has empty
  - Rerun idempotency: tags updated=2 / posts updated=3 (composite upsert matched)
  - Import 2 (fail policy): `ImportFailFastError`, row=post-b/v1 sourceIndex=2 column=tags, partial totals correct (tags updated=2, posts skipped=2 errors=1)
  - Cleanup: both tables deleted; sandbox back to 4 real tables
- **CLAUDE.md** — added `pnpm spike scripts/spike/14-composite-multi-onmissing.ts` to the commands list
- **Next step:** `onMissing='create-stub'` is the last unimplemented FK policy but the biggest change (resolver-mode change + stub-row shape design), rate-limit stress test, long-job runner strategy, or the Vercel deployment step (manual)

- **`onMissing='fail'` — real abort-the-run semantics shipped.** Closes the design gap flagged in the previous log entry. 3 tracked tasks (`TaskCreate` #30–#32). Only `create-stub` remains as an unimplemented FK policy
- **`lib/hubdb/import.ts`** — new `ImportFailFastError` class exported from the barrel; carries `row: RowError` + `partial: ImportResult`. Also added a module-private `_FailFastSignal` sentinel that flows from `importOneTable` up to `importRows` (kept private because callers should catch the public `ImportFailFastError`, not the throw-mechanism)
- **planRow / importOneTable / importRows** — three-hop control flow:
  1. `planRow` on `unresolved-fk` with policy `"fail"` returns `{kind: "fail-fast", ...}` instead of `{kind: "error", ...}`
  2. `importOneTable` on `fail-fast`: pushes the failing row into `result.errors`, adds `source.length - i` to `skipped` (marking the failing row + everything after as untouched), emits a `table-done` event so lifecycle listeners see the abort, then throws `_FailFastSignal(row, tableResult)`
  3. `importRows` catches the signal: appends the aborted table + a placeholder row (`{created: 0, updated: 0, skipped: rSource.length, errors: []}`) for each remaining table in the toposort, then throws `ImportFailFastError(row, {order, tables, ok: false})`
- **`lib/mapping.ts`** — flipped `initialForeignKeyConfig().onMissing` from `"fail"` → `"skip-row"`. New profiles default to skip-row (matching what the executor actually does); users who explicitly pick "fail" get real abort semantics. Existing saved profiles (there aren't any live ones, but hypothetically) with `"fail"` will now trigger the real abort — the behavior change is user-visible but small and PRD-honest
- **`app/api/portals/[id]/execute/route.ts`** — catches `ImportFailFastError` → HTTP **422** (semantic error — request well-formed, data triggered the policy). Response body: `{row, result, events, jobId, persistenceError}`. Job persistence: `insertJobErrors` gets called with all errors from the partial result, then `completeJob({status: "failed", error})`. Preflight remains 409, generic HubSpot failure remains 502
- **`app/import/execute-panel.tsx`** — new `Stage.kind = "fail-fast"` variant with its own render branch. Client-side check: `res.status === 422 && body.result && body.row` routes to the fail-fast stage instead of the generic error stage. UI: red banner ("Import aborted (onMissing="fail")") with the message and the triggering row's `table` / `sourceIndex` / `column` chips, followed by the full `ResultView` so the user sees per-table totals up to the abort point. "Execute" button re-labels to "Re-run" for both `done` and `fail-fast` (so the user can retry immediately after fixing the missing FK)
- **Design decision — 422, not 4xx-other.** Options were 409 (conflict — misleading, no state conflict), 400 (bad request — misleading, request was well-formed), 422 (unprocessable — semantically correct). Went with 422; matches how REST APIs signal "validation failed against the data, not the request shape"
- **Design decision — partial `ImportResult` in the error, not just the failing row.** The alternative was to throw with only `row: RowError` and let the caller inspect the executor's `onEvent` stream to reconstruct partial state. That's fragile — event replay would miss the "brands table's key map populated then products aborted mid-plan" nuance. The `partial` field gives the UI + job-persistence layer a complete answer for free
- **Design decision — placeholder rows for unstarted tables.** In a 3-table run where table 2 fail-fasts, table 3 never started. Rather than omit it from the result, we include it with `skipped: source.length, errors: []`. Makes the totals honest (`sum(created + updated + skipped) = sum(source.length)`) and gives the UI something to render in that row
- **Design decision — flip the wizard default, don't just fix the behavior.** With the default at `"fail"`, users who never touched the setting would now suddenly get abort semantics — surprising. With the default at `"skip-row"`, the executor default and wizard default match, and users who WANT abort explicitly pick "fail". Small user-facing change but the right one
- **Tests** — 180 across 14 suites (was 179). One new import test: 2 brands + 3 products (row 1 = ok, row 2 = orphan → fail, row 3 = ok if it had run). Asserts `ImportFailFastError` thrown; `err.row` matches the orphan row; `err.partial.tables.brands = {created: 2}`; `err.partial.tables.products.skipped = 2` (the orphan + the never-attempted row); zero batch-create calls for products (abort at plan time, before any batch fires)
- 180 cases across 14 suites, all green. tsc clean, lint clean
- **Not sandbox-verified.** Same limitation — no source data naturally shaped for `onMissing="fail"`. Unit coverage is strong; would want to smoke-test end-to-end with a fresh scratch schema
- **Next step:** `onMissing='create-stub'` is the only unimplemented FK policy but the biggest change (mid-flight foreign-row insert with a bounded stub shape). Or one of the two Phase-0 questions (rate-limit stress / long-job runner). Or sandbox smoke tests for composite NK / multi-value FK / onMissing behaviors against a fresh scratch schema

- **`onMissing` FK policies wired — `null` + `create-stub` rejection.** Closes half of the last remaining executor guard-rail flagged in the composite-NK log. 4 tracked tasks (`TaskCreate` #26–#29)
- **`lib/hubdb/import.ts`** — added `FkOnMissing = "fail" | "skip-row" | "null" | "create-stub"` type and optional `onMissing?: FkOnMissing` field on `FkColumnOption`. In `planRow`, when `resolveForeignValue` returns `!ok`, look up the per-column policy (default `skip-row`). `null` → `continue` in the column-values loop, so the FK column is dropped from the row entirely and the row still inserts / updates. `skip-row` (and `fail`, `create-stub` fallbacks) → existing `RowError` + skip. `FkOnMissing` exported from the `lib/hubdb` barrel
- **`lib/execution.ts`** — added `unsupported-on-missing` synth issue variant. When `fk.onMissing === "create-stub"`, push the issue and abort synth for that source (matches how `fk-target-column-missing` is handled). For `null` (and other non-default values), emit `onMissing` in `fkOptions[table][col]`. **`skip-row` is deliberately omitted** — the executor default is skip-row, so emitting it would just bloat the payload. Same optimization already in place for `multi` (only emitted when true)
- **Design decision — `null` is all-or-nothing at the cell level.** The v1 resolver (`resolveForeignValue`) is all-or-nothing: if any token in a multi-value cell misses, the whole cell fails. `null` policy inherits that — a multi-value cell with `hits + misses` drops the whole cell, not just the misses. Documented in the `FkColumnOption` JSDoc so a future reader doesn't assume "null drops missing tokens per element". True per-element resolution would need a resolver mode change and isn't in scope for this pass
- **Design decision — `create-stub` rejected at synth, not silently degraded.** Alternatives were: (a) synth degrades to skip-row silently, (b) executor no-ops the policy, (c) reject explicitly. Picked (c) because the user picked that policy for a reason — likely they *want* a stub row inserted. Silent degradation would let them save + run without ever seeing that HubSpot got a `null` FK where they expected a resolved stub. The synth issue makes it visible: they see it in the /import UI's disabled-reason and in the API 400 response
- **Design decision — `onMissing='fail'` currently falls back to skip-row.** Left as a known gap. PRD intent: `fail` = abort the whole run at the first missing token. Current impl: same as skip-row. Tempting to just implement it here, but it changes error surface for a full run in flight — better as its own change. Wizard default is still `"fail"` from F5, matching PRD phrasing but not executor semantics. Tracked in "Next up"
- **Tests** — 179 across 14 suites (was 175). 4 new:
  - Import: `onMissing='null'` — 1 resolved + 1 unresolved brand → 2 rows created, 0 skipped, orphan row's brand column absent from the create payload
  - Import: `onMissing='skip-row'` explicit — matches default behavior (1 row error, 1 create)
  - Synth: `onMissing='null'` emits `{onMissing: "null"}` in `fkOptions`; explicit `skip-row` config omits `fkOptions.products` entirely (payload minimization)
  - Synth: `onMissing='create-stub'` produces an `unsupported-on-missing` issue with the specific `policy` field
- **First-run test failure worth noting.** Wrote the default-omission assertion using `productsMapping` which uses `initialForeignKeyConfig()` — but that helper defaults `onMissing` to `"fail"`, not `"skip-row"`. The synth correctly emitted `{onMissing: "fail"}`, my test wrongly asserted `undefined`. Fix was to explicitly set `onMissing: "skip-row"` in the test's mapping, not touch the default. This surfaced the wizard/executor default mismatch that's now flagged as "Next up"
- 179 cases across 14 suites, all green. tsc clean, lint clean
- **Not sandbox-verified.** Same reason as the composite/multi change — no source data naturally shaped for `null` policy. Unit coverage is strong enough for MVP
- **Next step:** decide fate of `onMissing='fail'` (flip wizard default vs. implement abort semantics), sandbox smoke test with a fresh scratch schema for composite/multi/null, or one of the two Phase-0 questions (rate-limit stress / long-job runner)

- **Node 20 → 22 upgrade shipped.** Retires the `ws` polyfill that has been sitting in `lib/db/supabase.ts` since 2026-09-15 (F1). Plan file at `~/.claude/plans/swift-percolating-pelican.md`. Approved and executed under `/plan`
- **`package.json`** — dropped `ws` (`^8.21.3`) from deps and `@types/ws` (`^8.18.1`) from devDeps; bumped `@types/node` from `^20` → `^22`; added `"engines": { "node": ">=22" }`
- **`.nvmrc`** — new file with content `22` (pinned to LTS baseline; Node 24 also satisfies `engines`)
- **`lib/db/supabase.ts`** — deleted the `import { WebSocket as WsWebSocket } from "ws"` line and the whole `if (typeof globalThis.WebSocket === "undefined") ...` polyfill block. Node 22+ ships `WebSocket` as a global (introduced experimental in v21, stable in v22.4), so the CRUD client's Realtime constructor no longer needs the shim. `createSupabaseServerClient()` factory unchanged
- **`CLAUDE.md`** — stripped two Node-20 mentions: the `lib/db/supabase.ts` line that said "Contains a `ws`-based WebSocket polyfill for Node 20 (retire when we go to Node 22)" now says "Requires Node 22+ for the native `WebSocket` global that supabase-js's Realtime constructor needs"; deps list drops the `ws (Node 20 WebSocket polyfill)` entry
- **Local Node situation** — user's machine has Node 20.20.0 (default) and 24.13.0 available via nvm, but not 22. Set `.nvmrc = 22` (LTS anchor) rather than `24` (current); `engines.node: ">=22"` accepts both. Ran the entire verification chain against Node 24 (`export PATH="$HOME/.nvm/versions/node/v24.13.0/bin:$PATH"`) since that's what's installed
- **Verification** (all on Node 24):
  - `pnpm install` → removed `ws@8.21.3` + `@types/ws@8.18.1`; upgraded `@types/node` 20.19.43 → 22.20.3; no engines warning ✓
  - `pnpm test:run` → 175 / 175 green ✓
  - `pnpm exec tsc --noEmit` → clean ✓
  - `pnpm lint` → clean ✓
  - **`pnpm spike scripts/spike/12-supabase-ping.ts`** → all 6 tables reachable (portals=1, mappings=0, jobs=1 [the orphan from F11-1 verification], job_batches=0, job_errors=0, key_maps=0); zero `websocket|deprecat|warning` lines in the output ✓
- **Design decision — `engines: ">=22"` not `">=22 <23"`.** Semver-loose bound lets Node 24 (already installed on the machine) satisfy without needing to install 22 first. The `.nvmrc = 22` is the aspirational LTS anchor; the `engines` is the actual gate
- **Design decision — no README changes needed.** Grep confirmed the README doesn't mention Node 20 or the polyfill anywhere. `.env.example` also clean
- **Not yet done — Vercel dashboard Node version bump.** Manual step tracked in "Next up (unblocked)". No `vercel.json` in the repo, so the dashboard is the only source of truth on that side
- **Next step:** wire the remaining `onMissing` FK policies (`null`, `create-stub`) into the executor, run the composite NK + multi-value FK smoke test with a fresh scratch schema, or one of the two open Phase-0 questions (rate-limit stress / long-job runner)

- **Composite naturalKey + multi-value FK in execute path shipped.** 5 tracked tasks (`TaskCreate` #21–#25). This closes the two `synthesizeExecution` guard-rails flagged since F8 landed
- **`lib/resolve.ts`** — added `composeCompositeKey(row, columns, opts)` which normalizes each part and joins with a `String.fromCharCode(1)` (U+0001 SOH) separator. Returns `""` when any component is empty (caller's signal for "row has no NK"). `buildKeyMap` now accepts `string | readonly string[]` for the naturalKey — internally always operates on the array form via `composeCompositeKey`. Sample field in duplicate detection uses the raw single value for scalar NK and the array of raw values for composite (better for eyeballing conflicts)
- **Debugging aside — U+0001 → NUL corruption via Write.** First attempt embedded the SOH char inline (`const COMPOSITE_SEP = ""`) and the file landed on disk with a NUL byte inside the quotes. Confirmed via `python3 -c "open(p,'rb').read().count(b'\\x00')" → 1`. Same class of bug flagged in `STATUS.md`'s 2026-09-15 log (`lib/graph.ts` also lost bytes). Fix: use `String.fromCharCode(1)` at runtime so no control byte lives in the source file. Added a comment above the constant explaining the workaround so a future refactor doesn't undo it
- **`lib/hubdb/import.ts`** — several coordinated changes:
  - `naturalKeyColumn` (returning `string | undefined`) → `naturalKeyColumns` (always returning `string[]`, empty when NK isn't declared)
  - `planRow` gains a `fkOptionsForTable` parameter (via `importOneTable` from `importRows`). New `expandFkCell(raw, opt)` helper: passes arrays through, splits strings on `opt.delimiter` when `opt.multi === true`, otherwise wraps the raw value as `[raw]` (existing single-token behavior). Any not-yet-supported `Array.isArray(raw)` path still works — that's the round-trip case for FK cells returned from HubDB
  - `composeCompositeKey` used everywhere the old code did `normalizeKey(raw)` — including the fold-back after insert so downstream tables' FK resolution against the just-inserted rows works for composite NK too
  - `ImportInput.fkOptions?: Readonly<Record<table, Record<col, FkColumnOption>>>` added; the new `FkColumnOption` type has optional `multi` + `delimiter`
- **`lib/execution.ts`** — dropped the `composite-natural-key` issue variant entirely. `synthesizeExecution` now translates the `mapping.naturalKey: string[]` to an array of target NK cols (any component failing the mapping lookup issues a `natural-key-not-mapped` and skips the whole source-table). Emits `fkOptions` from `mapping.foreignKeys[col].multi/delimiter`. Result type gains `fkOptions: Record<table, Record<col, FkColumnOption>>`; the execute route passes it into `importRows`
- **Design decision — U+0001 as composite separator.** Options were U+0000 (NUL), U+0001 (SOH), or an unlikely-in-real-data multi-char sentinel. NUL is off-limits because of the Write-tool corruption bug and JS strings are OK with NUL but many tools aren't. SOH is safe: no normalized key can contain it (normalizeKey trims + collapses ws + casefolds; none produce a control char), matches the separator that `lib/source/validate.ts` uses for its composite duplicate-key check, and a single byte is cheaper than a multi-char sentinel. Composed via `String.fromCharCode(1)` to keep the source file free of raw control bytes. Included a test that "foo bar" + "baz" ≠ "foo" + "bar baz" (would collide with a space separator)
- **Design decision — FK options via side-channel, not schema field.** `SchemaColumn` is the shape used across parse/diff/provision/import. `multi/delimiter/onMissing` are import-time behaviors, not table-shape declarations, so they don't belong on the schema type. Went with `ImportInput.fkOptions` as a parallel structure. Preserves the "schema is portable across portals" invariant
- **Design decision — `onMissing` policies still not fully wired.** Currently the executor keeps its existing behavior (row → RowError with `unresolved-fk`, other rows continue). The user's `ForeignKeyConfig.onMissing` (fail/skip-row/null/create-stub) is captured in the profile but not yet threaded to the executor. `skip-row` is the current behavior; `null` and `create-stub` will need their own follow-up. Punted for this pass
- **Tests** — 175 across 14 suites (was 169). 3 new for composite `buildKeyMap` (upsert, duplicate detection with normalize-collision, empty-component skip), 1 for `composeCompositeKey` disambiguation, 1 for composite NK upsert in `importRows`, 1 for multi-value FK split (verifies each split token resolves individually AND the wrong delimiter produces exactly one `unresolved-fk` error). Also flipped the old `composite-natural-key` guard-rail test in `execution.test.ts` to a positive assertion that composite NK passes through as an array
- **Lint side-effect** — the pre-existing `SchemaColumn` unused-import warning in `lib/hubdb/import.ts` is finally clean; the barrel refactor removed the stale import. `pnpm lint` is 0 problems now
- **Not sandbox-verified this session.** The existing sandbox tables aren't naturally composite-keyed nor multi-value-referenced. Would need to provision a fresh scratch schema to smoke-test — punted to next session or when we ship the F11-adjacent UI catch-up
- **Next step:** Node 22 upgrade to retire the `ws` polyfill, sandbox smoke test for composite/multi with a fresh scratch schema, or the two still-open Phase-0 questions (rate-limit stress + long-job runner strategy)

- **Delete-table endpoint shipped + sandbox scrubbed.** 3 tracked tasks (`TaskCreate` #18–#20). Closes the "Sandbox cleanup" item that had been sitting on the tracked-task table since F3 landed
- **Lib** — `deleteTable(client, ref)` in `lib/hubdb/tables.ts`. `DELETE /tables/{ref}` on the v3 base; HubSpot returns 204 and the client's existing empty-body path (`isJson && text ? JSON.parse : text`) handles that cleanly — no changes needed in `client.ts`. Exported from the `lib/hubdb` barrel
- **API** — `DELETE /api/portals/[id]/tables/[tableId]`. `HubdbError.status === 404` → 404 (missing table); other HubDB failures → 502 with `hubspotStatus` + `hubspotBody` echoed back. Same portal-lookup + token-decrypt boilerplate as the other portal-scoped routes
- **UI** — `DropTableButton` in `app/portals/[id]/schema/drop-table-button.tsx`. **Two-tier confirmation** based on `portal.env`:
  - Sandbox: `window.confirm(...)` with the table label inline (single-click safety net)
  - Production: `window.prompt(...)` requiring the user to type the exact table name back — mismatch is silently cancelled with an inline "table name didn't match" error
  Rendered in each `TableCard` header (destructive-variant button). `useTransition` + `router.refresh()` on success so the list re-fetches the snapshot
- **Design decision — type-to-confirm only for production.** Every additional click reduces the chance of a rage-click accident, but also increases friction for the routine sandbox cleanup case. Type-to-confirm on prod balances the two: sandbox stays one-click, prod requires typing the exact table name (matches the `DELETE FROM` mental model in psql). If we later add a "purge orphan runs" batch flow, that should also require type-to-confirm regardless of env
- **Design decision — no soft delete / recycle bin.** HubSpot's `DELETE /tables/{id}` is the only endpoint available; there's no server-side undo. Adding a client-side "trash" would be misleading (the table is really gone in HubSpot). If we ever want recoverable deletes, the right layer is a full backup/export flow, not a UI simulation
- **Sandbox scrub via curl** (Dev sandbox, Hub 51706903):
  - Pre: 8 tables (4 real + 4 scratch)
  - `DELETE 412492316 exec_1789651861_products` → 200 ✓
  - `DELETE 412492315 exec_1789651861_categories` → 200 ✓
  - `DELETE 412493376 exec_1789651861_brands` → 200 ✓
  - `DELETE 412491169 f3demo_1789646817` → 200 ✓
  - Post: 4 tables (brands, categories, products, trst) — the 4 Phase-0 originals
- 169 cases across 14 suites still green (no new lib tests; the delete op is a thin wrapper). tsc clean. Lint clean except the pre-existing `SchemaColumn` warning
- **Next step:** composite naturalKeys + multi-value FK in the execute path (still guard-railed in `lib/execution.ts`), Node 22 upgrade to retire the `ws` polyfill, or the two still-open Phase-0 questions (rate-limit stress + long-job runner strategy)

- **F11-1 fix shipped + verified.** `jobs.mapping_id` no longer cascade-deletes with the profile. 3 tracked tasks (`TaskCreate` #15–#17)
- **Migration** — `supabase/migrations/20260918010000_jobs_mapping_id_set_null.sql`. Drops the existing `jobs_mapping_id_fkey` constraint (Supabase's standard `<table>_<column>_fkey` name — verified by inspection, no `\d jobs` needed), makes `mapping_id` nullable, adds the same constraint back with `ON DELETE SET NULL`. Two separate `ALTER TABLE` statements because Postgres won't let you drop + re-add a constraint of the same name in one statement (the DROP hasn't been committed when the ADD is planned). Applied by user via SQL Editor
- **Repo change** — `lib/db/jobs.ts`: `mappingId / mappingName / portalId / portalLabel / portalEnv` all flipped to `string | null` in both `JobSummary` and `JobWithMapping`. `toJobWithMapping` now returns `null` instead of the earlier `"?" / ""` fallbacks — those were noisy and lied about the state. JobDb row type gets `mapping_id: string | null` too
- **UI change** — `/jobs` list renders `"(profile deleted)"` in italic in place of the portal cell + em-dash in the profile cell when the join is null. `/jobs/[id]` detail renders `"profile deleted — provenance link lost"` in italic in the header line. Totals + row errors + status all render unchanged
- **Verification** (post-migration):
  - Created profile `f11-1-test` (`295eac03-…`)
  - Executed 1 brand row → job `5787f7db-…` succeeded (created: 1, existing rows now 202)
  - `DELETE /api/mappings/{profile}` → 200
  - `GET /jobs/{jobId}` → **HTTP 200** (was 404 before the fix) ✓
  - `/jobs` list still contains the row with `(profile deleted)` visible in the portal cell ✓
  - `/jobs/{jobId}` detail renders `provenance link lost` + preserved `succeeded` badge and `1 created` in the totals ✓
- **Scratch state left in sandbox:** the `brands` table now has 203 rows (200 Phase-0 + `smoke-alpha` + `smoke-beta` + `f11-alpha`) plus an orphan job in the DB (`5787f7db-…`, still visible in `/jobs` under `(profile deleted)`). Nothing surfacing — a "purge orphan jobs" affordance could be a future addition but not urgent
- **Next step:** delete-table endpoint (sandbox cleanup + `/portals/[id]/schema` affordance), composite naturalKeys + multi-value FK in execute, or Node 22 upgrade

- **F11 verified end-to-end.** User applied `20260918000000_mapping_profiles.sql` via SQL Editor. Ran the following curl chain against `pnpm dev`:
  - `GET /api/portals/{portal}/mappings` → 200 `{profiles: []}` ✓
  - `POST /api/portals/{portal}/mappings` with `{name: "smoke-test", state: {brands: {...}}}` → 201 with the profile round-tripped ✓
  - `POST` duplicate name → 409 ✓ (Postgres 23505 caught by `DuplicateMappingNameError`)
  - `PUT /api/mappings/{profile}` (rename) → 200 with `updated_at` bumped ✓
  - `POST /api/portals/{portal}/execute` with 2 fresh brand rows + `profileId` → 200, `result.ok: true`, `created: 2`, `jobId: a56bffee-...`, `persistenceError: null`, `autoSavedProfile: false` ✓
  - `GET /jobs` (HTML) → renders row with Dev sandbox · smoke-test-renamed · succeeded · jobId link ✓
  - `GET /jobs/{jobId}` (HTML) → renders detail with per-table totals for brands, "No row-level errors" section ✓
  - `DELETE /api/mappings/{profile}` → 200; subsequent `GET /jobs/{jobId}` → **404** ✓
- **First-try curl bug (mine, not the code):** I sent `columnMap: {slug: "slug"}` in the execute body, but `MappingState.columnMap` is `Record<sourceCol, {kind: "mapped" | "ignored", targetColumn?: string}>`. Got a clean `natural-key-not-mapped` synth issue (400) instead of a crash — good guard. Fixed the payload shape and it worked immediately
- **Finding F11-1 — `jobs.mapping_id ON DELETE CASCADE` wipes job history when a profile is deleted.** The original schema (`20260915000000_init.sql`) declared `jobs.mapping_id UUID NOT NULL REFERENCES mappings(id) ON DELETE CASCADE`. Confirmed live: after `DELETE /api/mappings/{profile}`, `/jobs/{jobId}` returns 404 (not just the profile — the job too). Surprising for an audit trail; users will assume "delete this profile" leaves history alone. **Not fixed this session** — mitigations for later: change the FK to `ON DELETE SET NULL` (and make `mapping_id` nullable) so job history survives, or `ON DELETE RESTRICT` to force a soft-archive. The auto-save-on-execute pattern makes this worse — every run produces a mapping row the user might casually delete
- **Scratch state left in sandbox:** 2 new rows in the Phase-0 `brands` table (`smoke-alpha` / `smoke-beta`) from the verification execute. Harmless — the table already has 200 rows
- **Next step:** address F11-1 (FK reshape migration for jobs.mapping_id), or move on to sandbox delete-table endpoint, composite naturalKeys + multi-value FK in execute, or Node 22 upgrade

- **F11 chunk 2 (job history) shipped, pending same migration.** After chunk 1 landed, immediately continued into chunk 2. 5 more tracked tasks (`TaskCreate` #10–#14). Also unlocks F10's deferred "job history list" bullet from the PRD
- **Migration bundled.** Rather than a second SQL file, folded `ALTER TABLE job_errors ADD COLUMN column_name TEXT` (nullable — table-level errors and `unmapped-table` row errors legitimately have no column) into the existing `20260918000000_mapping_profiles.sql`. Since the file hasn't been applied yet, the user pays for one round-trip through the SQL Editor instead of two. Renamed the header comment to reflect both changes
- **Repo** — `lib/db/jobs.ts` mirrors `mappings.ts` shape. Key type: `JobTotals` holds `{order, tables, ok, publish, publishedTables, durationMs}` (an audit-safe subset of `ImportResult` — row errors themselves live in `job_errors`, not on the job row). `listRecentJobs` + `getJobById` do a nested Supabase join through `mappings(name, portal_id, portals(label, env))` so the list page can render "portal · profile" without extra fetches. `JobWithMapping` denormalizes the join result. `insertJobErrors` no-ops on empty and inserts in one batch — no per-row round trip. `RowError.column` is threaded straight into `job_errors.column_name`
- **Execute route rewire** — `POST /api/portals/[id]/execute` now takes optional `profileId: z.string().uuid()`. Flow: `resolveMappingId` (uses `profileId` if it exists and belongs to the portal; else auto-creates a `run-<ISO date>` mapping row so the `jobs.mapping_id` NOT NULL constraint holds); `createJob(kind='import')` → `importRows` → `insertJobErrors + completeJob`. Every persistence call is wrapped in try/catch — if the migration isn't applied or the DB is unreachable, the import still runs and the response carries `persistenceError: string` explaining what failed. Preflight failures and mid-run errors also fire `completeJob(status='failed')` (best-effort) before returning
- **Design decision — best-effort persistence, not required.** The alternative (503 the whole endpoint if job persistence fails) means an unapplied migration or DB blip kills a run whose only downside is losing the audit trail. The user already sees the full result inline; the persistence is a "nice to have" for later inspection. Warning banner in `ExecutePanel` covers the case
- **Design decision — auto-save profile on execute-without-profile.** `jobs.mapping_id` is NOT NULL FK to `mappings.id ON DELETE CASCADE`. Rather than make that column nullable (destructive), auto-creating a `run-<timestamp>` mapping when the user runs without loading a profile keeps the FK honest AND makes the run re-loadable later. `autoSavedProfile: true` flag in the response tells `ExecutePanel` to note "profile auto-saved" inline. User can rename or delete the profile from `/import` afterward
- **UI** — `app/import/execute-panel.tsx` gains a `profileId?: string | null` prop; passes it in the request body; result view now renders a jobId link (`/jobs/[id]`) + persistenceError yellow banner + autoSavedProfile note. `app/import/profile-panel.tsx::onLoad` signature widened to `(state, profileId)` so `SourceUploader` can track `selectedProfileId`; also added `onSelectionCleared` for delete flow. `SourceUploader` threads `selectedProfileId` through `Results` into `ExecutePanel`
- **New pages** — `app/jobs/page.tsx` server component listing 50 most recent jobs with columns Started / Portal (with prod/sandbox badge) / Profile / Kind / Status / Created / Updated / Errors + details link. `app/jobs/[id]/page.tsx` renders per-table totals + row-error table (columns: Table / Row / Kind / Column / Detail) — errors table uses the new `column_name` column for the F10 `RowError.column` field. Renders duration, publish mode, and published-tables chips when applicable. Home page's disabled Runs/Results placeholders replaced with a single active "Jobs" tile
- **Design decision — `/jobs` global, not per-portal.** The audit view is more useful cross-portal (see all recent runs from staging + prod at once) than nested. Portal-scoped filtering can come later if the list gets long
- 169 cases across 14 suites still green. tsc clean. Lint clean (only the pre-existing `SchemaColumn` unused import warning)
- **Not verified against Supabase yet** — the migration is still pending. Once applied, the flow to try end-to-end: /import → configure → Execute → follow the jobId link to `/jobs/[id]` → see totals + row errors
- **Next step:** delete-table endpoint (sandbox cleanup + `/portals/[id]/schema` affordance), composite naturalKeys + multi-value FK in the execute path (still guard-railed in `lib/execution.ts`), or the Node 22 upgrade to retire the `ws` polyfill

- **F11 chunk 1 (mapping profiles) shipped, pending migration.** After F10 wrapped, user asked to continue and picked F11 mapping profiles (over job history) with the recommended "add `state_json`, keep `schema_json` nullable" migration shape. 5 tracked tasks (`TaskCreate` #5–#9)
- **Migration** — `supabase/migrations/20260918000000_mapping_profiles.sql`. `ALTER TABLE mappings ADD COLUMN state_json JSONB NOT NULL DEFAULT '{}'::jsonb` (default lets the ADD succeed if any legacy rows sneaked in), then `ALTER COLUMN state_json DROP DEFAULT` so new writes must supply a real payload. Also `ALTER COLUMN schema_json DROP NOT NULL` — the wizard is portal-first, no schema definition to store yet. Filename follows the Supabase CLI's timestamp convention. **User needs to apply this before the API works**
- **Design decision — repurposing `mappings`, not creating a new table.** The pre-existing schema for `mappings` assumed schema-first authoring (schema_json is authoritative; mapping is layered on top). Our /import wizard is portal-first — the user picks a target from the portal, so schema_json is derivable, not required. Two options considered: (a) add `state_json`, keep `schema_json` nullable, or (b) drop `schema_json` + `table_ids` entirely and rename to `state_json`. Picked (a) — non-destructive, leaves the door open for schema-first authoring later without a second migration
- **Repo** — `lib/db/mappings.ts` mirrors `portals.ts` pattern. `MappingProfile` (client-safe) vs `MappingRow` (DB shape with `state_json` snake-case). Functions: `createMapping / listMappingsForPortal / getMappingById / updateMapping / deleteMapping`. `DuplicateMappingNameError` catches Postgres 23505 on the `(portal_id, name)` unique index — kicked up as 409 by the route
- **API routes** — `POST /api/portals/[id]/mappings` + `GET /api/portals/[id]/mappings` (create + list, both guard on portal existence returning 404 if absent), `GET/PUT/DELETE /api/mappings/[id]`. `PUT` accepts partial `{ name?, state? }` with a zod `.refine` requiring at least one field. Body validation uses `z.record(z.string(), z.unknown())` for `state` — the frontend generates well-formed MappingState so we don't duplicate the shape server-side; a `.refine` requires at least one source-table key. `runtime = "nodejs"` on both (Supabase-js needs the ws polyfill)
- **UI** — `app/import/profile-panel.tsx`. Mounts below the target-portal picker (always visible when a portal is picked, even before parse). Two-column layout: Load dropdown + Load/Delete buttons on the left, Save-as input + Save button on the right. Fetches the profile list on portal change (with `startTransition` around setState calls to satisfy React 19's set-state-in-effect lint rule — same pattern as source-uploader). Load calls `onLoad(profile.state)` which replaces the whole `mappings` record. When a loaded profile references source tables not currently uploaded, shows a "Waiting for source table(s): X, Y" info line so the user knows why nothing changed visibly
- **Design decision — Load replaces, not merges.** Merging saved state with in-progress state was tempting but ambiguous when the user changes their mind mid-session. Replace-on-load is predictable; the ephemeral "Save first if you want to keep the current one" flow is one input away in the same panel
- **`source-uploader.tsx` wire-in** — one new import, one `loadProfile` callback, one derived `currentSourceNames` from the current parse state (empty array if no parse yet — the panel handles that case), and the `<ProfilePanel>` element between the portal picker and the CSV/JSON tabs
- **Lint churn** — first pass of `profile-panel.tsx` errored on `react-hooks/set-state-in-effect` (same as F4 when MappingEditor's effect landed). Wrapped all three `setStatus` calls inside the `useEffect` in `startTransition`. Fixed
- 169 cases across 14 suites still green (no new lib tests; changes are DB-integration + UI). tsc clean. Lint clean except the pre-existing `SchemaColumn` unused import warning
- **Not verified against Supabase yet** — the migration is pending. Once the user applies it, the flow is: pick portal → configure at least one mapping → type name → Save → reload page → pick from dropdown → Load
- **Next step:** F11 chunk 2 (job history — write `jobs` row + `job_errors` on execute, add a jobs-list page). `jobs` and `job_errors` tables already exist so no new migration needed. Also unlocks F10's deferred "job history list" bullet

- **F10 results & logging shipped** (minus job history — deferred to F11). Scope confirmed via AskUserQuestion: "Polish + downloads" (skip job history) + "one combined file per format" for the downloads. 4 tracked tasks (`TaskCreate` #1–#4)
- **`RowError` gained optional `column?: string`** in `lib/hubdb/import.ts`. Populated at all three construction sites: `missing-natural-key` (uses the NK column name, when the row-level "source missing NK" variant fires — the table-level "no NK defined" case still has no column), `missing-target-key-map` (FK column), `unresolved-fk` (FK column). `unmapped-table` still has no column (whole-table failure). `import.test.ts` extended to assert `column: "brand"` on the unresolved-fk case and `column: "slug"` on the missing-natural-key case
- **`ExecutePanel` rewritten.** Kept the existing publish selector + execute button + event log details. Added:
  - Totals dashboard (5-cell dl grid): Tables / Created / Updated / Skipped / Errors, color-toned (created=emerald, updated=blue, errors=destructive)
  - Per-table error grouping — chips of `kind × column × count` sorted by count desc, shown above the row-error list. Makes it easy to see "12 unresolved-fk on brand" at a glance instead of scanning 500 individual rows
  - Uncapped row-error list — replaced the `.slice(0, 50)` + "and N more" line with a scrollable `max-h-64 overflow-y-auto` list that shows every error. Each line now shows the `column` inline when present
  - Two download buttons: **row-errors.csv** (flat: table, sourceIndex, kind, column, detail; RFC-4180 quoting for cells containing `"` `,` `\n` `\r`; button disabled when 0 errors) and **import-log.json** (full envelope: `portalId`, `publish`, `startedAt`, `finishedAt`, `durationMs`, `result`, `events`, `published`). Uses `Blob` + `URL.createObjectURL` + click-and-revoke; no server round-trip
  - Timing capture — `startedAt` / `finishedAt` ISO strings on the `Stage: done` variant, used for `durationMs` in the JSON download envelope
- **Design decision — grouping key is `kind + column`, not just `kind`.** An `unresolved-fk` on `brand` and one on `category` should count separately since they're likely different fixes; grouping them together would hide the shape of the failure
- **Design decision — job history bullet from PRD F10 checklist deferred to F11.** F11 owns `jobs`-table persistence anyway; wiring a job row on every execute crosses that boundary. Explicit call-out in the tracked-task row
- 169 cases across 14 suites still green (only new assertions were on the RowError shape). tsc clean. Lint clean except the pre-existing `SchemaColumn` unused import in `lib/hubdb/import.ts` (unchanged from prior sessions)
- **Not verified against a real browser this session** — download buttons use standard `Blob`/`createObjectURL` + programmatic anchor click, well-trodden path. No chromium-cli available to script it. The download logic is pure client code so it can't affect the API contract
- **Next step:** F11 (persist mapping profiles + job history + resume-on-reload), the delete-table endpoint for sandbox cleanup, or the Node 22 upgrade to retire the `ws` polyfill

### 2026-09-17
- **F8 UI wiring shipped.** Wrote `lib/execution.ts::synthesizeExecution` — the crucial translator between our wizard state and `importRows`'s expected shape. Key insight: `lib/hubdb/import.ts::planRow` uses each schema column's `name` as BOTH the source-row lookup key AND the HubDB values key. When the mapping renames a column (e.g. source `Product Name` → target `productName`), the synthesizer keys everything in "target space": schema columns use target names, source rows are transformed to `{targetCol: source[sourceCol]}`, natural key + FK columns are translated too (FK's `foreignColumn` becomes the sibling's target column name for the picked matchKey). Returns `{schema, tableIds, source, issues}` — issues rather than throws so the endpoint can 400 cleanly
- 6 vitest cases: column renaming happy path, FK translation to sibling's target column, `fk-target-column-missing` when sibling doesn't map the matchKey, missing-target / composite-natural-key / natural-key-not-mapped guardrails
- Wrote `POST /api/portals/[id]/execute` — decrypts token → fetches portal → `synthesizeExecution` (400 if issues) → `importRows` (409 on `ImportPreflightError`, 502 on generic HubSpot errors, both echo the event log so the UI can show what happened before failure) → optional `pushLive` in `result.order` filtered by publish mode. `publish: "foreign-only"` publishes everything except the last table in dep order (rough heuristic that fits products→brands+categories; richer selection lives in F9)
- Wrote `ExecutePanel` client component. Publish selector (`none | foreign-only | all`), Execute button, per-table result cards showing `N created / N updated / N skipped / N errors` with a collapsible row-errors list (first 50), publish results list with per-table success/failure, collapsible event log. Guard `disabled` prop plus a `disabledReason` hint — the API also refuses execution independently if synthesis issues remain
- Wired into `SourceUploader`: shows below the `DryRunPanel` under the same "has target + natural key" condition. `showDryRun` is reused as the gate
- **Sandbox verification via curl** (Dev sandbox portal):
  - Provisioned 3 fresh namespaced tables via `POST /api/portals/[id]/provision`: `exec_1789651861_brands` (id `412493376`) + `_categories` (`412492315`) + `_products` (`412492316`)
  - Called `POST /api/portals/[id]/execute` with 2 brands + 1 category + 2 products (each product FKing brand + category by slug), `publish: "foreign-only"`
  - Response: `ok: true`, dep order `[brands, categories, products]`, 5 rows created total (2/1/2), 0 errors, 12 events emitted, publish result: brands + categories published live, products left draft ✓
- **Scope-limited choices in this pass:** (a) composite natural keys still unsupported (guard-railed via issue); (b) multi-value FKs aren't split in the transformer yet — the executor path assumes single-value cells. Both flagged as followups. Job persistence + rate-limit throttle also still open per PRD
- **Scratch state left in sandbox:** the `exec_1789651861_*` tables from this verification join the earlier `f3demo_1789646817` — no delete endpoint yet. Added to the "next up" list as a small follow-up item
- 169 cases across 14 files, all green. tsc + lint clean

- **F7 dry run shipped** (earlier this session). Wrote `lib/dry-run.ts::computeDryRun` — pure function that classifies source rows as create vs update by natural-key intersection with existing draft rows, tallies unresolved FKs by looking up each cell value (with multi-value split when configured) against the sibling source's match-key set, emits coercion warnings via `detectTypeMismatch`, and estimates API calls (list-page GET + create batches + update batches). Reuses `deriveImportOrder` from F6 for the traversal order so composite v5/6/7 stays consistent
- 7 vitest cases covering: create/update classification, natural-key normalization (BETA↔beta), unresolved FKs (single + multi-value split), coercion warning, dep-order propagation, API-call estimate (250 rows → 1 GET + 3 POST batches). Suite now 163 across 13 files
- Wrote `POST /api/portals/[id]/dry-run` — reads portal, decrypts token, `fetchPortalSchema` for target-table lookup, then parallel `listAllDraftRows` for every table referenced by a mapping (uses `Promise.all`; per-target errors captured so one 403 doesn't nuke the whole report), calls `computeDryRun`, returns the report + optional `readErrors`
- Wrote `DryRunPanel` client component. "Run dry run" button; per-table cards with 4-metric grid (source rows / existing draft rows / will-create / will-update), coercion warnings inline, collapsible unresolved-FK list showing first 100 rows + a "…and N more" line for larger sets. CSV download button assembles a client-side blob (`escape()` quotes any value containing `,`, `"`, or `\n`) and drops the file as `unresolved-fks.csv`
- Wired into `SourceUploader`: appears above the per-table cards whenever any mapping has both a target and a natural key picked. Passes `portalId + allSources + mappings` down; the panel POSTs everything on Run
- **End-to-end verification via curl** against the sandbox (Dev sandbox portal, 200 existing brands + 200 existing products from Phase-0): brands source (2 rows) + products source (3 rows, one row's `brand` = "unknown"), FK config `brand` → brands.slug default matching. Response: order `["brands", "products"]`, brands planned `create: 2, update: 0` (source slugs `acme`/`beta` don't match Phase-0's `brand-1..brand-200`), products planned `create: 3` + `unresolved: 1` (the `unknown` value), API estimate `4` = 1 GET brands + 1 batch-create brands + 1 GET products + 1 batch-create products ✓. `ok: false` because of the unresolved FK
- 163 cases across 13 files, all green. tsc + lint clean
- **Next step:** F8 UI wiring (turn the mapping + FK config + dry-run pass into a click-to-run execute + push-live), or F11 mapping-profile persistence so the state survives page reload

- **F6 dependency-ordering UI shipped** (earlier this session). User picked F6 next. Tracked 4 subtasks (`TaskCreate` #18–#21). Small refactor first: `MappingState` + `initialMappingState` moved out of `app/import/mapping-editor.tsx` into `lib/mapping.ts` so a shared derivation can consume it. `mapping-editor.tsx` re-exports both for local callers, no changes needed at any call site
- Added `deriveImportOrder(sourceNames, mappings)` to `lib/mapping.ts`. Builds `GraphNode[]` from each mapping's `foreignKeys` (a source depends on any sibling listed as a `sourceTable` on any FK config), then delegates to `lib/graph.ts::toposort` / `breakCycles`. Returns `{nodes, order, cycles, deferred, cycleBreakOrder, ok}`. Self-references and out-of-set FK targets are ignored — clean semantics
- 5 vitest cases: happy-path products→brands+categories ordering, identity order when no FKs, out-of-set target ignored, 2-cycle detected + deferred edges emitted + `cycleBreakOrder` populated, self-ref ignored
- Wrote `ImportOrderPanel` — numbered vertical list of the resolved order. Each row shows its dependencies (as inline chips) and any deferred edges callouts. Header badge is green "no cycles" or yellow "two-phase write". When cycles exist, an extra callout section enumerates each cycle as `a → b → c → a` and notes the deferred-edge count
- Wired into `SourceUploader`: appears above the per-table cards only when at least one mapping has FK configs (so the panel doesn't render for the pure-single-table upload case). Reuses the existing `mappings` state and `allSources` list — no new state, no extra fetches
- 156 cases across 12 files, all green. tsc clean, lint clean
- **Next step:** F7 (dry run — the config we already have has everything it needs; dry run reports what would happen without writing) or F8 UI wiring (hook `/import` up to `importRows` + push-live for a click-to-run pipeline; lib layer already proven by the sandbox spike)

- **End-to-end importer spike passed** (earlier this session). Wrote `scripts/spike/13-import-end-to-end.ts` — the full `provision → importRows → push-live` pipeline against the sandbox, using unique namespaced table names (`spike_it_<epoch>_{brands,categories,products}`) to avoid Phase-0 collision. 7 steps: diff → provision → import (5+5+20 rows) → verify FK cell → push-live in dep order → verify live row-id stability → re-import for idempotency check → cleanup
- **First run failed with 409 → F0-13 finding:** creating a table with label `"Brands"` fails when any existing table in the portal already has that label — HubDB uniqueness applies to LABEL, not just name. Error body: `TableValidationError.DUPLICATE_NAME_AND_LABEL`, `duplicateTables: ["412407343"]` (the Phase-0 brands table). Fix: namespace both `name` and `label`. Rolled the finding into the current-state block; provision UI should probably surface this if a user picks a label that collides with an existing table
- **Second run: all steps green.** Provision created 3 tables in dep order (foreign first), `products.brand` came back with `foreignTableId=412492145 foreignColumnId="1"` — the "slug" column of the new brands table (F0-4 in action: column ids are per-table sequential, so both FK targets landed on id `1` of their respective tables). Import: 5+5+20 rows, zero row errors. `sku-001.brand[0].id === brand-1.id` (FK resolution correct). push-live promoted all three (~600 ms each); live product row-id `222107301655` matched the draft id — publish is promotion not duplication (reconfirms F0-8). Live product row's `brand` cell now includes the resolved `name: "brand-1"` field, since the display column is `foreignColumnId=1` = `slug` and live introspection joins it. Re-import: 0 created, 30 updated across the three tables → fully idempotent. Cleanup deleted all three namespaced tables cleanly
- Added the new spike script to CLAUDE.md's command list

- **F5 foreign-relationship config shipped** (earlier this session). User picked F5 to continue. Tracked 5 subtasks (`TaskCreate` #11–#15). Design decision: F5 lives inside `MappingEditor` as a per-column sub-panel rather than a separate route — `foreignKeys: Record<sourceColumn, ForeignKeyConfig>` on `MappingState` keeps everything cohesive, and the editor auto-syncs configs on target change and per-row assignment change (drops orphans, seeds defaults). Also needed access to sibling parsed sources, so `SourceUploader` now passes `allSources` down alongside the single `source` prop
- Extended `lib/mapping.ts` with `ForeignKeyConfig` (sourceTable / matchKey / multi / delimiter / onMissing / matching), `initialForeignKeyConfig()`, `normalizeOptionsFor(matching)` (bridges the UI's `default | strict` to `lib/resolve`'s NormalizeOptions — `strict` disables trim + collapseWhitespace + casefold), and `countResolvable(mainRows, mainColumn, foreignRows, matchKey, cfg)` returning `{matched, unmatched, empty, totalValues, missingValues}` for the live preview. Reuses `normalizeKey` + `splitMultiValue` from `lib/resolve` — no duplication
- 5 vitest cases on `countResolvable`: single-value case-insensitive default matching (all-hits), single-value with mixed empty/missing/hit, strict rejects casefold matches, multi-value comma-split with per-cell hit-and-miss (verified `totalValues` counts every value across every row), multi-value pipe delimiter
- Bug shaken out during test-writing: my first multi-value expectation was wrong (`totalValues: 4`) — actually `2 + 2 + 1 = 5`. The failing test caught that my mental model was undercounting the split cells. Fixed the expectation, not the code
- Wrote `ForeignKeyPanel` in `mapping-editor.tsx`. One card per FOREIGN_ID mapping. Two-row layout: foreign source + match key on top, then 4 columns (multi / delimiter / onMissing / matching). Delimiter select is disabled until multi is on. Live resolvability preview shows matched/unmatched/empty + `totalValues` + `missingValues`, and calls out unresolved count in yellow when non-zero. If source or matchKey unset, we render a placeholder line prompting the user
- Threaded `allSources` from `SourceUploader` into each `MappingEditor` — one small refactor of the render loop to build the list once and hand each editor its slice
- **Suite now 151 cases across 12 files, all green.** tsc clean, lint clean (the pre-existing `SchemaColumn` warning in `lib/hubdb/import.ts` still there)
- Interactive UI can't be scripted here (no chromium-cli / playwright). Verification via unit tests + tsc + prior curl checks. When the user drives it in a real browser: pick a portal → upload brands.csv + products.csv → for the mapped `brand` column in products (target FOREIGN_ID), the FK panel appears and offers `brands` as the sibling source and `slug` as the match key
- **Next step:** end-to-end importer spike (`provision → importRows → push-live`) to prove the pipeline with real FK resolution, or F6/F7 (dependency-order display + dry run — dry run is where the F5 config becomes user-visible instead of just captured state)

- **F4 column mapping UI shipped** (earlier this session). After F3 write side landed the user asked to continue and picked F4 via AskUserQuestion. Tracked five subtasks (`TaskCreate` #6–#10). Big design decision up front: `MappingEditor` lives inside the existing `SourceUploader` on `/import` (matches PRD §9 `/import/[id]` wizard shape) rather than a separate `/mapping` route — no persistence layer yet, so a single-page state machine keeps everything in sync
- Wrote `GET /api/portals/[id]/schema` — a client-consumable JSON version of the F3 introspection page. Same `fetchPortalSchema` under the hood; used by the mapping UI to seed the target-table dropdown when the user picks a portal
- Wrote `lib/mapping.ts` — pure functions, no DOM. `normalizeIdent` (lowercase + strip whitespace/underscore/hyphen/dot; distinct from `normalizeKey` because identifier folding shouldn't collapse to spaces — `product_name` and `productName` must both fold to `productname`), `autoMap(sourceHeaders, targetColumns)` returning `mapped/ignored/unmapped` per source with **claim-once** semantics (if two source headers fold to the same target, only the first wins; later ones stay `unmapped` — prevents silent double-assignment), `detectTypeMismatch(sample, targetType)` (only fires on high-confidence mismatches: NUMBER/CURRENCY require numeric, BOOLEAN accepts true/false/yes/no/1/0 case-insensitively, DATE requires `YYYY-MM-DD` **and** `Date.parse`-able, DATETIME just `Date.parse`-able, TEXT/RICHTEXT/URL permissive), `validatePathColumn(rows, column)` for `hs_path` (lowercase + URL-safe chars + duplicate + empty)
- 16 vitest cases for `lib/mapping.ts` — happy paths + claim-once + all TypeMismatch kinds + all PathIssue kinds + empty/whitespace tolerance. Suite now 146 across 12 files
- Modified `POST /api/sources/csv` + `/json` to include full `rows` in the response (not just the first-20 `preview`) so the client can re-run `validateSource` when the user picks a natural key or required columns. Trade-off: payloads grow ~1KB/row (10 MB at the 10k cap) — fine for MVP; F11 persistence would replace this with a source-id + server-side re-validation
- Wrote `app/import/mapping-editor.tsx` — the client component. Target-table dropdown → auto-map → per-row source column table: source col name + sample values (first 3 non-empty, truncated) + three-way assignment select (`— unmapped —` / `— ignored —` / target col name, with claimed targets disabled) + type badge showing target column type + mismatch badge + natural-key checkbox (only enabled when mapped). Below: `hs_name` + `hs_path` fields appear when target `useForPages`; a summary panel showing mapped/ignored/unmapped counts + chosen natural key + page fields; a live warnings panel that re-runs `validateSource` on every state change (so `duplicate-natural-key` and `empty-required-column` fire the moment a key is picked) plus `validatePathColumn` bucketed by issue kind when `hs_path` is set
- Wired the editor into `SourceUploader`: parent now owns `portalTables` state, fetches `/api/portals/[id]/schema` in a `useEffect` on portal-id change (with `startTransition` around the setStates to satisfy React 19's new `set-state-in-effect` lint rule — earlier attempt without transitions failed lint), stores `mappings` keyed by source-table name, and passes each `MappingEditor` its own slice
- **End-to-end verification** — API side clean:
  - `GET /api/portals/{portal}/schema` → 200, 5 tables with columns (all 4 sandbox tables + the leftover `f3demo_*` from F3 verification)
  - `POST /api/sources/csv` → 200 with the new `rows` field present, `totalRows === rows.length === 4` on a 4-row brands.csv (including 2 duplicated slugs, so the F2 duplicate-natural-key warning is ready to fire once the user picks `slug` as the key)
  - UI screenshot of `/import` renders unchanged when no upload has happened — MappingEditor mounts on parse success, which needs a user click I can't script without chromium-cli / playwright installed. Compensating with the 16 unit tests on `lib/mapping.ts`, tsc-verified prop wiring, and the parent's `useEffect` fetch verified via curl
- Lint churn: `react-hooks/set-state-in-effect` errored on the fetch-then-setState pattern until I wrapped the state updates in `startTransition`; also had to prune an unused `useState` import in mapping-editor.tsx after refactoring. Both clean now
- **Suite now 146 cases across 12 files, all green.** tsc clean. lint clean (pre-existing `SchemaColumn` unused warning still there)
- **Next step:** F5 (foreign relationship config — the actual "solve the manual-linking problem" feature) slots naturally into `MappingEditor` beneath each `FOREIGN_ID` target column. Or the end-to-end importer spike (`provision → importRows → push-live`) to prove the pipeline before more UI layers land

- **F3 diff + provision shipped** (earlier in the day). After the read-side landed the user asked to continue; picked "F3 diff-plan UI" via AskUserQuestion. Set up 5 tracked tasks (`TaskCreate`) since this had discrete steps and I wanted the progress visible
- Wrote `POST /api/portals/[id]/diff` — validates body has `{ schema }`, `parseSchema` (400 on `SchemaValidationError` with the issue list echoed back), `fetchPortalSchema` (502 wrapped), `diffSchema`, returns `{ portal, fetchedAt, schema, plan }`
- Wrote `POST /api/portals/[id]/provision` — same pipeline then `provision(opsFromClient(client), plan, { onEvent })`. Captures each `ProvisionEvent` in a local array and echoes them back for the UI to render. `ProvisionConflictError` → 409 with `conflictTables` + the offending plan; other errors → 502 with the plan + partial events so the UI can show what happened before failure
- Wrote `app/portals/[id]/schema/schema-planner.tsx` — client component with a state machine (`idle / diffing / planned / provisioning / provisioned / error`). Accepts JSON via textarea or `.json` file upload; parses locally first (fast error surface); POSTs to diff, renders per-table cards with action badges (create=green, update=blue, match=muted, conflict=red), and shows a Provision button when `plan.ok && (create+update) > 0`. Provision result includes toposort order, deferred edges count, new table ids, and the event log in a collapsible `<details>`; after success, `router.refresh()` re-fetches the introspection snapshot so the newly-provisioned table appears at the top of the page
- Wired the planner into `app/portals/[id]/schema/page.tsx` between the table list and the footer. Also updated the existing schema page — no changes needed to `page.tsx` beyond one import + one component invocation
- **End-to-end verification against the real sandbox (Dev sandbox, Hub 51706903):**
  - **match**: schema of `brands + categories + products` (with explicit `foreignColumn: "name"` matching the Phase-0 setup) → `plan.ok: true`, all three `match`. First attempt with `resolveDefaults` filling `foreignColumn: "slug"` from naturalKey surfaced a real conflict — products' actual FK display column is `name` (col id 1), not slug. Good validation of the resolver + conflict logic against portal reality
  - **update**: schema adding a `logo_url URL` column to brands → `plan.ok: true`, `action: "update", addColumns: ["logo_url"]`
  - **create**: schema declaring a new `f3demo_<epoch>` table → `plan.ok: true`, `action: "create"`
  - **conflict**: schema declaring `brands.slug` as NUMBER (portal has TEXT) → `plan.ok: false`, one `column-type-mismatch` conflict emitted
  - **provision**: POSTed the create schema → HTTP 200, `order: ["f3demo_1789646817"]`, `tableIds: {"f3demo_1789646817": "412491169"}`, 2 events. Re-diff of the same schema → `action: "match"`. Refreshed screenshot showed the new table at the top of the introspection list with the DRAFT ONLY badge
- **Suite still 130 cases across 11 files.** Provisioning + diff routes are integration-tested end-to-end but not unit-tested — the underlying ops are already covered
- **Scratch state left in the sandbox**: `f3demo_1789646817` (draft, 0 rows) is still there. No delete endpoint in v1; harmless. Could be a followup to add "Drop table" to the introspection UI, though PRD is silent on this
- **Next step:** F4 (column mapping UI — unlocks F2's deferred natural-key / required-col warnings and starts wiring the import wizard shape) or the end-to-end importer spike (`provision → importRows → push-live`) to prove the full lib pipeline before F4 stacks more UI on top

### 2026-09-17-morning
- **F3 read side shipped.** After F2 landed the user asked to continue; picked F3 (portal introspection UI) via AskUserQuestion. Scope: read/render only — the diff-plan + provisioning trigger remain
- Wrote `lib/hubdb/portal-schema.ts::fetchPortalSchema(client)` — `listTables` for enumeration + `getDraftTable(id)` per table in parallel (`Promise.all`) to pick up pending draft columns. Per-table failures are caught and stashed in `draftFetchError` so a single 403 doesn't nuke the snapshot; the whole thing returns `{ tables, fetchedAt }`. Injected `now` factory keeps the vitest deterministic
- 3 vitest cases for portal-schema (draft columns merged over live, per-table error isolation with the failing table falling back to live columns, empty-portal). Suite now 130 cases across 11 files
- Wrote `app/portals/[id]/schema/page.tsx` — server component. Fetches portal → decrypts token via `getPortalToken` → `createHubdbClient` → `fetchPortalSchema`. Renders each table as a card with published/draft badge (uses `isPublished` from lib), row count, and a column table showing name / label / type / FK target ids. `dynamic = "force-dynamic"` so refresh always hits fresh data. Empty-portal + fetch-error branches handled
- Wrote `app/portals/[id]/schema/refresh-button.tsx` — client. `useTransition` + `router.refresh()` for the F3 "manual refresh" requirement. No explicit per-session cache — the tradeoff is a re-fetch per view for correctness; if portals grow past a few dozen tables we can add `unstable_cache` here
- Added a `schema →` link on each row of `/portals` so the introspection view is reachable from the portals list
- **End-to-end verification against the real sandbox portal (Dev sandbox, Hub 51706903):**
  - GET `/portals/{id}/schema` → **200** in ~3s (list + 4 draft fetches in parallel), rendered all 4 tables
  - **Products** (id 412407345, 200 rows, PUBLISHED): sku + title as TEXT; brand + category as FOREIGN_ID with target-table ids `412407343` / `412407344` and display-column id `1` on each — visually confirms F0-4 (column ids are per-table sequential, both FKs point at column id 1 of their respective target tables)
  - **Categories** (412407344, 200 rows, PUBLISHED), **Brands** (412407343, 200 rows, PUBLISHED), and **trst** (377110628, 0 rows, DRAFT ONLY — the pre-existing table from before the spike, showing the badge works)
- **Font bug fix (Times New Roman → Geist).** User flagged that everything was rendering in Times. `app/globals.css` had `--font-sans: var(--font-sans)` in the `@theme` block — a self-reference that resolved to undefined, so `font-sans` in Tailwind fell back to serif. Changed to `--font-sans: var(--font-geist-sans)` (the CSS variable that `next/font/google`'s `Geist({variable: "--font-geist-sans"})` actually exports in `layout.tsx`). Also updated the layout `metadata.title` from "Create Next App" → "HubDB Importer"
- **Suite now 130 cases across 11 files, all green.** tsc clean. lint clean (pre-existing `SchemaColumn` unused warning still there)
- **Next step:** F3 completion — diff-plan UI (upload a schema definition → `diffSchema` + render per-table verdicts + provision button). Or F4 (column mapping UI, which unlocks the natural-key / required-col warnings F2 defers)

---

- **F2 shipped end-to-end** (earlier in the day). User asked to proceed with what phase 1 has planned next; picked F2 (source ingestion) via AskUserQuestion. Aligned on scope up front (three questions): in-memory only (no Supabase persistence yet — that's F11's problem), papaparse for CSV, new `/import` route
- Installed `papaparse` + `@types/papaparse`. `pnpm install` had to re-link first — snap `code/263` → `code/264` (VS Code snap bump, same failure mode as 2026-09-15's `263` bump). One-time relink, harmless
- Wrote `lib/source/csv.ts` — byte-in, `{ headers, rows, detected, warnings }` out. BOM sniff for UTF-8 / UTF-16 LE/BE with manual encoding override; papaparse's auto-delimiter with manual override (`,` `;` `\t` `|`); header dedup with `_N` suffixes and empty-header filler `column_N` (both emit warnings); `headerRow` override for preamble rows; short rows padded with empty strings so every row has every header key. Throws `CsvParseError` on empty input or header row past EOF
- Wrote `lib/source/json.ts` — accepts either `{ tableName: rows[] }` or `[{ table, rows }]`; scalars stringified; nested objects **or arrays** rejected as `nested-value` with `path: "table[i].col"` (PRD F2: "cells are scalars or delimited strings" — arrays too, since multi-value is authored as delimited strings). Backfills missing columns per row
- Wrote `lib/source/validate.ts` — `Warning` union (`row-count-cap` / `cell-too-long-text` / `cell-too-long-richtext` / `duplicate-natural-key` / `empty-required-column`). Reuses `HUBDB_MAX_ROWS_PER_TABLE` + `normalizeKey` from `lib/resolve`. natural-key + required-col checks are generic — F2 stage doesn't know these so they stay quiet; F4 will pass them in
- Wrote three vitest suites (39 cases): CSV happy paths + BOM/encoding + header dedup + preamble rows + RFC-4180 quoted fields + error paths; JSON both shapes + scalar coercion + nested-value rejection with path pointers + malformed-JSON handling; validate cap + length + required-col + natural-key (single + composite) + empty-key-skip
- Convention snag: first draft imported `@/lib/resolve` (works in Next but no vitest config → import error). Rest of `lib/` uses relative imports, so switched. Also worth noting for future: no `vitest.config.ts` exists; adding a `resolve.alias` for `@/` would let both worlds share the same import style
- Wrote `POST /api/sources/csv` (multipart, one or more `file` fields, 20 MB cap each, global delimiter/encoding/headerRow overrides) and `POST /api/sources/json` (`{ payload: string }` body). Both return the same per-table shape: `{ name, filename, headers, preview (20), totalRows, detected, parseWarnings, validationWarnings }`. JSON errors surface `path` for nested-value, `kind` for others
- Wrote `app/import/page.tsx` (server component, pre-fetches portals + empty-state) and `app/import/source-uploader.tsx` (client). Tabs for CSV/JSON, form controls for delimiter/encoding/header row, per-table `TableCard` with detected encoding/delimiter, warning list, preview grid (first 20 rows, cell values truncated at 120 chars). Home page updated to link to `/import` alongside `/portals`
- **End-to-end verification against a running `pnpm dev`:**
  - GET `/import` → 200, page renders
  - POST `/api/sources/csv` with `brands.csv` + `products.csv` → 200; brands auto-detected as comma, products as semicolon, both preview + totalRows correct
  - POST `/api/sources/csv` with `-F 'delimiter=;'` override on the semicolon file → 200 with detected: `";"` echoed
  - POST `/api/sources/json` with `{brands:[…], categories:[…]}` → 200, two tables
  - POST `/api/sources/json` with a nested-object cell → **400** `kind: "nested-value"`, `path: "products[0].meta"` ✓
  - POST `/api/sources/csv` with no `file` field → 400 with clear message
  - POST `/api/sources/csv` with a 10001-char cell → 200 with `validationWarnings: [{ kind: "cell-too-long-text", … }]` ✓
- Bug shaken out during verification: `validateSource({ table: file.name })` was passing the raw filename (`toolong.csv`), while the response used the derived name (`toolong`). Fixed to hoist `tableName` and reuse it. One-line change
- **Suite now 127 cases across 10 files, all green.** tsc clean. lint clean (pre-existing `SchemaColumn` unused warning still there, not from this change)
- **Next step:** F3 (portal introspection UI) or F4 (column mapping UI — would also unlock the natural-key/required-col validation warnings that F2 defers). The end-to-end importer spike is still pending too

### 2026-09-15
- Kicked off Phase 1. Started with the item STATUS+CLAUDE.md called out first: `lib/hubdb/` typed wrapper hardening
- Wrote `lib/hubdb/client.ts` — `createHubdbClient({ token, ... })` factory (per-portal, no global env read). Retries 429 + 5xx with `Retry-After` honored, else exponential backoff with jitter capped at 30s (default 5 attempts). `HubdbError` exposes status, parsed rate-limit headers, and attempt count
- Wrote `lib/hubdb/types.ts` — normalized shapes. Table `id`, row `id`, `foreignTableId`, `foreignColumnId` are all `string` (F0-4 wrapper contract). Added `isPublished()` that treats the `1970-01-01T00:00:00Z` epoch as "never published" (F0-9)
- Wrote `lib/hubdb/tables.ts` (`listTables/getTable/createTable/patchTable/pushLive`) and `lib/hubdb/rows.ts` (`listAllDraftRows/listAllLiveRows`, `batch{Create,Update,Purge}DraftRows`). Batch mutation paths are hard-coded to `/rows/draft/batch/{create,update,purge}` (F0-5). 100-row cap asserted before the call (F0-6)
- Migrated all 7 spike scripts onto the new wrapper. `scripts/spike/client.ts` shrank to ~30-line shim that builds one `HubdbClient` from `HUBSPOT_TOKEN` and re-exports `log`/`runSpike`/`HubdbError`. Spikes now act as regression checks of the wrapper — any break in id normalization, retry, or batch paths surfaces when re-running them
- Smoke-tested `pnpm spike scripts/spike/00-ping.ts` against the sandbox. Green: both `v3` and `dated` bases return 4 tables (~800ms each), all ids as strings
- Wrote `lib/graph.ts` — the topological sort shared by provisioner + importer + publisher. Kahn's for order, iterative Tarjan's for SCC / cycle detection. Three public entry points: `toposort()` (partial order + cycles report), `toposortOrThrow()` (F6 "reject with clear message"), `breakCycles()` (F6 "offer two-phase write"). Converters: `nodesFromTableInputs` (name-based FK, the F0-2 happy path) and `nodesFromHubdbTables` (id-based). Convention: `GraphNode.dependencies` = prereqs; `GraphEdge {from, to}` mirrors that ("from depends on to")
- Wrote `scripts/graph-check.ts` — 25 hand-verified assertions across linear DAG, spike scenario, self-loop, 2/3-node cycles with dangling dependents, `toposortOrThrow` cycle payload, `breakCycles` deferred-edge output, disjoint tie-break stability, both converters, and duplicate-name rejection. Two bugs shaken out during the check: (a) edge direction inverted (adj stored `n→dep` but Kahn's needed `prereq→dependent`); (b) `toposort` emitted cycle nodes when they had no non-cycle prereqs — semantic decision to filter them out and force callers to `breakCycles` if they want an order that covers cycle nodes
- **Caveat — not a test runner.** `scripts/graph-check.ts` is a one-shot assertion runner, not Vitest. No CI, no watch mode, no `describe/it`. Fine for shaking bugs out at write-time; needs to be replaced by a proper test runner before the module has real callers. Phase 1 task #7 tracks this
- **Wrote-time artifact worth watching.** First Write of `lib/graph.ts` landed with `U+0000` (null bytes) in two template literals where the source had spaces (`` `${e.from} ${e.to}` `` → `` `${e.from}\0${e.to}` ``). Detected via `cat -A` when an Edit failed to match. Recovered by rewriting the file. Unclear whether the corruption was in the Write tool, the terminal bridge, or my own emission. Did not recur in `lib/schema.ts` or `lib/hubdb/provision.ts` — one-off so far
- Installed **Vitest 5** and ported `scripts/graph-check.ts` to `lib/graph.test.ts` (12 `describe/it` blocks). Deleted the smoke script. `pnpm install` had to run first — pnpm store path drifted from snap `code/261` → `code/263` (VS Code snap version bump) and node_modules was linked to the old store. One-time relink, harmless. Peer warning: vitest wants `@types/node ^22 || >=24` but Next 16 pins `^20`; warning only, suite runs
- Wrote `lib/schema.ts` — zod schema for structural parse, then a manual cross-ref pass that surfaces *all* issues at once (duplicate table/column names, dangling FK targets, FK missing `foreignTable`, `foreignColumn` not on target, `naturalKey` referencing an undefined column). `resolveDefaults` fills FK `foreignColumn` from the target's single-column `naturalKey` when omitted; composite `naturalKey` leaves the caller to be explicit. `diffSchema(schema, portal)` returns per-table `create | match | update | conflict` with three conflict shapes (`column-type-mismatch`, `fk-target-mismatch`, `fk-column-mismatch`); never emits drop/retype (F3). Extra portal columns not in the schema are ignored — schema is additive, not authoritative-over-portal. 21 vitest cases covering parse failure modes + every diff verdict
- Wrote `lib/hubdb/provision.ts` — takes an injectable `ProvisionOps` adapter (not the raw client) so tests can plug in a fake with internal state instead of MSW. `opsFromClient(client)` builds the real adapter. Dep graph nodes are built only from `create` + `update` actions; a table depends on another *only if the target is also being created* (updates/matches already exist in the portal). Self-references are included so `breakCycles` catches them. Phase 2 groups deferred edges by source and PATCHes the missing FK columns per table
- **PATCH semantics conservatism.** The provisioner sends `portal.columns + new columns` on every PATCH (existing ids preserved). Safe assumption if HubDB PATCH is either full-replace *or* merge — but not yet validated against the sandbox. Added a Phase-1 task to spike this before broader wiring
- **Bug shaken out in provisioner:** initial version filtered `col.foreignTable !== name` when building deps, so self-loops were invisible to the graph and got sent to `createTable` with the self-referencing FK column intact. Caught by the self-loop test on first run; fix was one line
- **Next step:** validate PATCH semantics against the sandbox, then either `lib/resolve.ts` (F8) or start the F1 portal-connection API + Supabase scaffolding

- Ran the PATCH-semantics validation. **Three new findings** (F0-10 through F0-12, documented in the Phase-1 addendum of `phases/phase-0-spike.md`):
  - **F0-10** — `PATCH /tables/{id}` returns HTTP **401** with a misleading "service-to-service not engaged" body (no hint that the path is wrong). The correct endpoint is `PATCH /tables/{id}/draft` — schema mutations live under the draft namespace, extending F0-5's rows pattern to tables. `PUT` and dated-base PATCH cleanly 405; only v3 PATCH on the root gives the confusing 401
  - **F0-11** — `PATCH /tables/{id}/draft` is **full-replace** on the `columns` array. Sending only new columns drops the existing ones. Sending a subset drops the omitted ones. Push-live promotes whatever the draft says. **This confirms the provisioner's `portal.columns + new columns` approach was correct — sending only new columns would silently destroy schemas.** The PRD §F3 "never drop or retype" rule is enforced by our code, not the API
  - **F0-12** — `GET /tables/{id}` returns the LIVE view, not the draft. After PATCH /draft, GET stays on pre-PATCH state until push-live. Callers that need to see pending schema changes (diff, verify) must use `GET /tables/{id}/draft`
- **Live bug fixed in `lib/hubdb/tables.ts::patchTable`** — was hitting `/tables/{ref}` (guaranteed 401 in production). Now `/tables/{ref}/draft`. Added `getDraftTable(client, ref)` and exported it from the barrel. All 40 vitest cases still green (fake ops don't care about paths)
- **End-to-end integration test**: `scripts/spike/11-provision-update.ts` creates a throwaway table with 2 columns, parses a 3-column schema, diffs against the draft, runs `provision(opsFromClient(client), plan)`, verifies the draft ends up with the expected columns. Passed on first run after the path fix
- **Spike scripts 06–11 added** as the trace of the investigation. Kept in-tree for future reference; they're throwaway but tell the debugging story
- **Next step:** `lib/resolve.ts` (F8, key-map builder + FK resolver) or start F1 portal-connection API + Supabase scaffolding — no more open assumptions in the lib layer

- Wrote `lib/resolve.ts` — the low-level FK resolver. Kept scope tight: only normalization + key-map building + value resolution + a multi-value splitter. Everything execution-level (onMissing policies, stale-ID re-resolve on write error, row-cap enforcement at pass-1) lives at the caller. `resolveForeignValue` is all-or-nothing: any single missing value fails the whole cell and returns `{ok:false, missing, cell}` (partial cell + missing list) so the caller can apply its policy per-row. Empty input → `{ok:true, cell:[]}` per PRD's "empty FK cell → empty array (not null)"
- Design choice: `Delimiter` typed as `"," | "|" | ";" | "\n"` per PRD F5. Union rather than free-string keeps mapping-file validation cheap downstream
- 24 vitest cases (normalize defaults + opt-outs + coercion + null handling; split per delimiter; buildKeyMap happy/duplicate/empty-value-skip/coercion; resolve single/empty/whitespace/dedupe/multi-value-hit/multi-value-miss/every-distinct-missing + composition with `splitMultiValue`). Suite now 64 across 4 files
- **Next step:** `lib/hubdb/import.ts` composes wrapper + schema + graph + resolve into the F8 execution: pass-1 upsert foreign tables (batch) + build key maps (with dupe detection + 10k cap check), pass-2 main table with resolved FKs, retry on 429/5xx, cancel at batch boundary. Or pivot to F1 portal-connection API + Supabase scaffolding if we want to bring the app layer up first

- Wrote `lib/hubdb/import.ts` — the F8 execution layer. Topologically iterates schema tables via `toposortOrThrow(nodesFromTableInputs(...))`, and for each: fetches existing draft rows, builds a normalized keyMap on `naturalKey` (aborts with `ImportPreflightError` on dupes or if existing + source would exceed the 10k cap), splits source rows into insert vs update by naturalKey lookup, resolves FK columns via previously-populated tables' key maps, and batch-writes at 100/call (updates then inserts). Newly-inserted row ids fold back into the local key map so downstream tables that FK-reference this one resolve against the fresh ids
- **Scope-limited choices**: (a) `onMissing='fail'` per-row — the row is skipped and logged as a `RowError`, other rows continue; other policies (skip/null/stub) are follow-ups; (b) assumes `foreignColumn === target.naturalKey` (which `resolveDefaults` guarantees for single-column naturalKeys) — composite naturalKeys are unsupported in v1; (c) no stale-ID re-resolve on write error, no cancel signal (retries handled by wrapper); (d) doesn't call `push-live` — that's F9
- **Injectable `ImportOps`** (list + batchCreate + batchUpdate) mirrors the provisioner's testability pattern. Tests use a `FakeOps` with an internal Map<ref, HubdbRow[]> store and a call log. 11 vitest cases (all-new insert with cross-table FK resolution, PATCH-update by naturalKey, casefold matching, dupe abort, 10k-cap abort, per-row unresolved-fk skip, missing-naturalKey source, 100-row chunking on a 250-row batch, empty-source no-writes, unmapped-table error path, lifecycle event ordering). Suite now 75 across 5 files
- **Wrote-time artifact false-positive**: my usual `cat -A | grep '\^@'` check reported 1 match on this file, but `python3 -c "open(p).read().count(b'\\x00')"` returned 0 — the "hit" was `M-^@` inside the UTF-8 sequence for `…`. Retiring the cat-based check in favor of the python one
- **Ticked off in `phases/phase-1-mvp.md`**: F8's 5 code items + F8's "retry on 429/5xx" (wrapper-level), 5 of 7 items in "Foreign key resolution correctness" (Section 8), and 3 of 4 constraint-enforcement items (Section 10). Remaining F8 items are execution-environment concerns (Supabase persistence, throttle, cancel button, worker separation)
- **Next step:** end-to-end sandbox spike (`provision → importRows → push-live`) analogous to `11-provision-update.ts`, then either F1 portal-connection API + Supabase scaffolding or start on results/logging (F10) surface

- **Pivoted to phase-1-mvp order.** User asked "we still don't have UI?" and then "let's proceed to what has planned on phase 01" — so I switched from further lib work to the phase-1 checklist starting at "Project scaffolding" → F1. Picked "Cloud project" for the Supabase-mode question via AskUserQuestion
- Wrote `supabase/migrations/20260915000000_init.sql` — full DDL for `portals`, `mappings`, `jobs`, `job_batches`, `job_errors`, `key_maps`. Enables `pgcrypto` for `gen_random_uuid()`. Shared `set_updated_at()` trigger on `portals` + `mappings`. Job-side tables carry the cursor state (`job_batches`) and per-row failure log (`job_errors`) that F8/F10 will need. `key_maps.entries` is a JSONB `{normKey: rowId}` snapshot per (job, table) for resume without re-fetching. Filename follows the Supabase CLI's timestamp convention so we can `supabase link` this later without renaming
- Wrote `lib/crypto.ts` — AES-256-GCM. `encrypt(plaintext) → base64` returning a single string that packs `iv || tag || ciphertext`. Key read from `PORTAL_TOKEN_ENCRYPTION_KEY` (32 bytes, base64) with a clear error if missing or wrong length. `generateEncryptionKey()` helper. 10 tests (roundtrip, unicode, IV freshness, bit-flip → auth tag catches, truncation, wrong-key rejection, missing env, wrong length)
- Installed `@supabase/supabase-js` (2.116.0). Wrote `lib/db/supabase.ts` — server-only factory using the service-role key, `persistSession: false`, `autoRefreshToken: false`. Untyped `SupabaseClient` for now; typed repos will layer on top when F1 lands
- Rewrote `.env.example` — SUPABASE_* + PORTAL_TOKEN_ENCRYPTION_KEY blocks with generation instructions; the old HUBSPOT_* vars are re-scoped as "spike scripts only" since app code will read tokens from the `portals` table once F1 is wired
- **Blocked on user action for full validation:** creating the Supabase cloud project + applying the SQL migration + pasting env vars. Code side is unblocked — F1 API routes can be written and typechecked against this scaffolding; they'll fail with a clear message at runtime if Supabase isn't configured
- 85 cases across 6 suites still green. tsc clean
- **Next step:** F1 API routes (`POST /api/portals`, `GET /api/portals`) + `lib/db/portals.ts` typed repo + portal picker page

- **Supabase applied, F1 shipped end-to-end.** User created the cloud project + applied the migration via the SQL Editor. After a small speed bump (`25006 read-only transaction` — root cause was the SQL Editor's role selector being on a non-`postgres` role), all 6 tables came up green
- Wrote `scripts/spike/12-supabase-ping.ts` — counts rows in each of the 6 phase-1 tables via the server client. First run failed with `Node.js detected but native WebSocket not found` from supabase-js's Realtime constructor. Fix: installed `ws` (+`@types/ws`) and set `globalThis.WebSocket = WsWebSocket` at the top of `lib/db/supabase.ts` when the global is missing. Ping then reported all 6 tables reachable, 0 rows each. Deprecation warning for Node 20 surfaces on every Supabase call — not fatal; upgrade to Node 22 tracked as task #20
- Wrote `lib/hubdb/introspection.ts` — `validateHubdbToken(token)` hits `/tables?limit=1` with retry disabled. 200 → `{ok:true, scopes:["hubdb"]}`. 401 → `{ok:false, status:401, error:"…invalid or missing hubdb scope"}`. Other errors → generic status + body preview. Can't introspect private-app scopes via the HubSpot API, so the `["hubdb"]` scope is inferred from success. Fetch-injection tested (3 cases)
- Wrote `lib/db/portals.ts` — typed repo. Split `PortalRow` (server-only, includes `token_ciphertext`) from `PortalSummary` (client-safe, no token). Functions: `createPortal` (encrypts token via `lib/crypto`), `listPortals`, `getPortalById`, `getPortalToken` (server-only decrypt), `deletePortal`. All take a `SupabaseClient` as first arg
- Wrote `app/api/portals/route.ts` — `runtime = "nodejs"`. GET returns `{ portals }`. POST validates body with zod, calls `validateHubdbToken` (400 with the HubSpot status echoed on failure), inserts. Unique-index violation on `(label, env)` is caught and rewritten to a 409 with a friendly message
- Wrote `app/portals/page.tsx` (server component, `dynamic = "force-dynamic"`) + `app/portals/portal-form.tsx` (client). Env badge is red for production per F1. Form posts to `/api/portals`, then `router.refresh()` — no client-side state duplication. Replaced the create-next-app template on `app/page.tsx` with a real home page linking to `/portals` (ready) + disabled placeholders for Mappings / Runs / Results
- **End-to-end verification against real Supabase + real HubSpot:**
  - GET `/api/portals` → 200, empty list
  - POST `/api/portals` with the real `HUBSPOT_TOKEN` → **201**, portal created with `scopes: ["hubdb"]`, row landed in Supabase
  - GET `/api/portals` → 200, one portal (token not echoed)
  - POST with fake token → **400**, `"Token is invalid or missing the required hubdb scope"`
  - POST duplicate `(label, env)` → **409**, friendly message from unique-index catch
  - GET `/portals` (HTML) → 200, portal rendered with sandbox badge + Hub 51706903
- **Next step:** F2 (source ingestion — CSV / JSON upload + parse + preview + validation warnings), then either F3 (portal introspection UI) or the end-to-end importer spike

### 2026-09-14
- `.env.local` provisioned with `HUBSPOT_TOKEN` — spike unblocked
- Extended `00-ping.ts` to hit both `/cms/v3/hubdb/tables` and `/cms/hubdb/2026-03/tables` in parallel with timing
- Both paths returned identical results (1 existing table `trst`, ~730–760ms). Task #2 resolved — defaulting to `v3`
- Wrote `01-provision-foreign.ts` — idempotent create of `brands` + `categories` (both with `name` + `slug` TEXT columns), plus a deliberate bad-FK POST to capture the error surface
- Ran it: `brands` (`412407343`) and `categories` (`412407344`) created as draft. Bad FK returned HTTP 400 with the message *"Foreign table id or foreign table name must be defined"*
- **Finding — revisit PRD §6/§8:** HubSpot accepts `foreignTableName` as an alternative to `foreignTableId` on `FOREIGN_ID` columns. Schema files could reference foreign tables by name directly, skipping the schema-local-id → HubSpot-id translation step at provision time. Still need to confirm the same holds for `foreignColumnName` vs `foreignColumnId`
- Wrote and ran `02-provision-main.ts` — created `products` (id `412407345`, draft) with two `FOREIGN_ID` columns. The name-based path **succeeded on the first attempt** — no fallback needed. HubSpot resolved `foreignTableName: "brands"` + `foreignColumnName: "name"` server-side and returned `foreignTableId: 412407343`, `foreignColumnId: 1`

**Architectural impact — PRD §6 and §8 need revision:**
> "The provisioner translates to `foreignTableId` / `foreignColumnId` integers at write time so schema files are portable across portals."

That translation step is now **optional**. Schema files can be authored with `foreignTable: "brands"` + `foreignColumn: "name"` (names) and passed through to the API unchanged; HubSpot resolves in-portal at write time. Cross-portal portability comes for free. The provisioner still needs the id-translation code path as a fallback (e.g. for topology inspection, cycle-breaking PATCHes, or if we ever hit a scenario name-resolution can't handle), but it's no longer the primary path.

**Additional API observations to bake into the wrapper:**
- HubDB column ids are **per-table sequential integers** (both `brands.name` and `categories.name` are id `1`) — not globally unique. Any code comparing column ids across tables is a bug
- Response type inconsistency: `FOREIGN_ID` column echoes `foreignTableId`/`foreignColumnId` as **numbers**, while `/tables` list returns table `id` as a **string**. `lib/hubdb/` wrapper should normalize to strings on ingest

- Wrote `03-insert-foreign.ts` — generates deterministic rows (`brand-001`…`brand-200`, `category-001`…`category-200`), reads existing draft rows for idempotency, batches at 100 rows/call, builds `slug → row-id` maps
- First run 404'd on `POST /tables/{name}/rows/batch/create` with an HTML body (edge/gateway 404). **Correct endpoint is `/rows/draft/batch/create`** — batching is scoped under the draft namespace, not the top-level rows path. Fixed and re-ran successfully
- 200 rows inserted into each of `brands` + `categories` via two 100-row batches. Throughput ~800–1020ms per 100-row batch (~100–125 rows/sec, single-threaded). No dropped rows. Row IDs are 12-digit global HubSpot ids (`"221835620680"`), reinforcing the wrapper's id-normalization TODO
- **API surface additions for `lib/hubdb/`:**
  - Only `/rows/draft/batch/{create,update,purge}` is valid — hardcode the `draft` segment
  - Read draft rows from `/rows/draft` (with `limit` + `after` cursor for pagination — PRD §10 says 1000 default)
- Wrote and ran `04-insert-main.ts` — read `brands` + `categories` draft rows in parallel, built slug→id maps, generated 200 products with 1:1 FK assignment (`product-001` → `brand-001` + `category-001`), inserted via two 100-row batches to `/rows/draft/batch/create`. Timings ~1024ms + ~797ms
- **Finding — FK cells round-trip verbatim.** Sent `{id, type: "foreignid"}` and got back the same. No server-side normalization, no `foreignTableId` bleed onto the cell, no join expansion. Write and read shapes are symmetric — the resolver stays simple. The target table is pinned by the *column definition*, not the cell
- Row IDs across all three tables share one global integer namespace (`221835620680` brand → `221835620868` product, adjacent). Not per-table. Wrapper should never assume table-scoping on row ids
- Wrote `99-inspect.ts` to spot-check state — confirmed 200 draft / 0 live rows in each of `brands`, `categories`, `products`. `publishedAt: "1970-01-01T00:00:00Z"` is HubSpot's null-sentinel for "never published"
- **Finding — UI display of FK columns requires the foreign table to be published.** In the HubSpot HubDB editor, `products.brand` cells show blank even though the id is stored, because the display value is resolved from the foreign table's `name` column (`foreignColumnId: 1`) and only reads the *live* version. Publishing `brands` + `categories` first should make products' FK cells render — this is exactly what task #8 tests
- Wrote and ran `05-publish.ts` — sequential `POST /tables/{name}/draft/push-live` for `brands` → `categories` → `products`. All three returned `published: true` with fresh `publishedAt`. Timings: `brands` 959ms, `categories` 1736ms, `products` 923ms (~3.6s wall-clock). Re-ran `99-inspect.ts` — live counts now 200 in each table, row ids stable across draft/live (publish promotes rather than duplicates)
- **Finding — topological order is a UI/HubL requirement, not an API requirement.** Publishing `products` before `brands` would still succeed at the API level, but products' FK display columns would be blank in the HubSpot UI and HubL joins would return nothing until foreign tables catch up. The dependency order stands, but the rationale is "user-visible correctness" not "API validation"
- Categories publish was ~2× the others (1736ms vs ~940ms). Could be variance or FK-constraint re-check on the second foreign table publish. Worth watching in longer runs
- Rewrote `phases/phase-0-spike.md` — checked off all completed items with the artifact ids inline (`brands` `412407343`, etc.), documented 9 findings (F0-1 through F0-9), locked in 6 decisions for Phase 1, and rolled the two un-answered items (rate-limit ceiling, long-job runner strategy) forward as Phase-1 open questions
- **Phase 0 closed.** Remaining Phase-0 checkbox (HubL page render) is a manual editor test that does not gate Phase 1 code work
- **Next step:** kick off Phase 1 — start `lib/hubdb/` (typed wrapper hardening: id normalization to strings, `/rows/draft/batch/*` path enforcement, 429 backoff with `Retry-After`), then `lib/graph.ts` (topological sort shared by provisioner + importer). See `phases/phase-1.md` for the full task list

### 2026-09-12
- Wrote PRD `hubdb-importer-prd.md`
- Broke PRD §12 into per-phase task checklists under `phases/`
- Wrote `CLAUDE.md` guidance for future Claude Code sessions
- Scaffolded Next.js 16 app via `create-next-app`, added shadcn/ui, pushed to GitHub
- Started Phase 0: added `tsx` + `zod` deps, wrote `scripts/spike/client.ts` (typed HubSpot fetch wrapper with `HubdbError` surfacing rate-limit headers) and `00-ping.ts` (list tables). Added `pnpm spike` script
